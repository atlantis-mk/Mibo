---
phase: 16-catalog-api-search-progress-cutover
plan: 04
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
  - 16-03
files_modified:
  - mibo-media-server/internal/httpapi/handlers_catalog_items.go
  - mibo-media-server/internal/httpapi/handlers_catalog_governance.go
  - mibo-media-server/internal/httpapi/catalog_api_router_test.go
  - mibo-media-server/internal/httpapi/router.go
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
must_haves:
  truths:
    - "Clients can list and search catalog items from additive `/api/v1/items` routes without relying on legacy MediaItem handlers."
    - "Clients can fetch catalog item detail and series seasons from catalog hierarchy routes."
    - "Administrators can read governance workspace state and mutate locks, images, asset links, and provider refresh actions through catalog routes."
    - "Clients can read and update progress using catalog item and asset IDs through additive progress routes."
  artifacts:
    - path: mibo-media-server/internal/httpapi/handlers_catalog_items.go
      provides: read routes for catalog items, seasons, and catalog progress
    - path: mibo-media-server/internal/httpapi/handlers_catalog_governance.go
      provides: governance read and mutation routes for catalog items and assets
    - path: mibo-media-server/internal/httpapi/catalog_api_router_test.go
      provides: authenticated router coverage for additive catalog routes
    - path: mibo-media-server/internal/httpapi/router.go
      provides: route registration for additive catalog endpoints
  key_links:
    - from: mibo-media-server/internal/httpapi/handlers_catalog_items.go
      to: mibo-media-server/internal/catalog/query_items.go
      via: ListItems/GetItemDetail/ListSeriesSeasons
      pattern: "ListItems|GetItemDetail|ListSeriesSeasons"
    - from: mibo-media-server/internal/httpapi/handlers_catalog_governance.go
      to: mibo-media-server/internal/catalog/governance.go
      via: GetGovernanceWorkspace/UpdateFieldLock/SelectImage/LinkAsset/UnlinkAsset
      pattern: "GetGovernanceWorkspace|UpdateFieldLock|SelectImage|LinkAsset|UnlinkAsset"
    - from: mibo-media-server/internal/httpapi/handlers_catalog_items.go
      to: mibo-media-server/internal/progress/catalog_progress.go
      via: UpdateCatalogProgress/GetCatalogProgressState
      pattern: "UpdateCatalogProgress|GetCatalogProgressState"
---

<objective>
Expose the additive catalog HTTP surface for items, seasons, governance, and progress.

Purpose: turn the new catalog query, governance, and progress methods into public routes that Phase 18 can consume without repointing legacy endpoints yet.
Output: new catalog handlers, route registrations, and router integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-catalog-api-search-progress-cutover/16-RESEARCH.md
@.planning/phases/16-catalog-api-search-progress-cutover/16-PATTERNS.md
@.planning/phases/15-series-level-metadata-governance-engine/15-03-PLAN.md
@.planning/phases/16-catalog-api-search-progress-cutover/16-01-PLAN.md
@.planning/phases/16-catalog-api-search-progress-cutover/16-02-PLAN.md
@.planning/phases/16-catalog-api-search-progress-cutover/16-03-PLAN.md

<interfaces>
From Wave 1 plans:

```go
func (s *Service) ListItems(ctx context.Context, input ListItemsInput) ([]CatalogListItem, error)
func (s *Service) GetItemDetail(ctx context.Context, itemID uint) (CatalogItemDetail, error)
func (s *Service) ListSeriesSeasons(ctx context.Context, seriesID uint) ([]CatalogSeasonDetail, error)

func (s *Service) GetGovernanceWorkspace(ctx context.Context, itemID uint) (CatalogGovernanceWorkspace, error)
func (s *Service) UpdateFieldLock(ctx context.Context, input FieldLockInput) (database.MetadataFieldState, error)
func (s *Service) SelectImage(ctx context.Context, itemID uint, imageID uint) error
func (s *Service) LinkAsset(ctx context.Context, input LinkAssetInput) (database.AssetItem, error)
func (s *Service) UnlinkAsset(ctx context.Context, assetID uint, itemID uint) error

func (s *Service) UpdateCatalogProgress(ctx context.Context, userID uint, input CatalogProgressInput) (CatalogProgressState, error)
func (s *Service) GetCatalogProgressState(ctx context.Context, userID uint, itemID uint) (CatalogProgressState, error)
```

From `mibo-media-server/internal/metadata/catalog_series.go` (Phase 15 target contract):

```go
func (s *Service) MatchCatalogSeries(ctx context.Context, itemID uint) error
func (s *Service) RefreshCatalogSeriesMetadata(ctx context.Context, itemID uint) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing router coverage for additive catalog routes</name>
  <read_first>
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/handlers_catalog_migration.go
    - mibo-media-server/internal/httpapi/handlers_media.go
    - mibo-media-server/internal/httpapi/catalog_migration_backfill_router_test.go
    - .planning/phases/16-catalog-api-search-progress-cutover/16-01-PLAN.md
    - .planning/phases/16-catalog-api-search-progress-cutover/16-02-PLAN.md
    - .planning/phases/16-catalog-api-search-progress-cutover/16-03-PLAN.md
  </read_first>
  <files>
    - mibo-media-server/internal/httpapi/catalog_api_router_test.go
    - mibo-media-server/internal/httpapi/handlers_catalog_items.go
    - mibo-media-server/internal/httpapi/handlers_catalog_governance.go
    - mibo-media-server/internal/httpapi/router.go
  </files>
  <behavior>
    - Test 1: `GET /api/v1/items` and `GET /api/v1/items/{id}` return catalog DTOs, while `GET /api/v1/series/{id}/seasons` returns catalog seasons with nested episode availability.
    - Test 2: `GET /api/v1/items/{id}/governance` requires auth and returns governance workspace state; `PATCH /api/v1/items/{id}/field-locks`, `POST /api/v1/items/{id}/images/select`, `POST /api/v1/assets/{asset_id}/link-item`, and `DELETE /api/v1/assets/{asset_id}/link-item/{item_id}` all mutate catalog-owned rows.
    - Test 3: `GET /api/v1/items/{id}/progress` and `POST /api/v1/me/item-progress` read and write progress by catalog item and asset IDs.
  </behavior>
  <action>Create `catalog_api_router_test.go` first using the same real-router + auth-header pattern as `catalog_migration_backfill_router_test.go`. Seed one movie, one series root with season/episode children, one metadata source row, one field-state row, two image candidates, one asset link, and one user progress row. Add failing tests named `TestCatalogItemRoutes`, `TestCatalogGovernanceRoutes`, and `TestCatalogProgressRoutes`. In `handlers_catalog_items.go` and `handlers_catalog_governance.go`, add compile-target handler stubs for the new routes, and add route stubs in `router.go` for `/api/v1/items`, `/api/v1/items/{id}`, `/api/v1/series/{id}/seasons`, `/api/v1/items/{id}/governance`, `/api/v1/items/{id}/field-locks`, `/api/v1/items/{id}/images/select`, `/api/v1/items/{id}/match`, `/api/v1/items/{id}/refresh-metadata`, `/api/v1/assets/{asset_id}/link-item`, `/api/v1/assets/{asset_id}/link-item/{item_id}`, `/api/v1/items/{id}/progress`, and `/api/v1/me/item-progress`, but leave implementation incomplete so this test step fails first.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/catalog_api_router_test.go` contains `TestCatalogItemRoutes`.
    - `mibo-media-server/internal/httpapi/catalog_api_router_test.go` contains `TestCatalogGovernanceRoutes`.
    - `mibo-media-server/internal/httpapi/catalog_api_router_test.go` contains `TestCatalogProgressRoutes`.
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/items`.
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/items/{id}`.
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/series/{id}/seasons`.
    - `mibo-media-server/internal/httpapi/router.go` contains `POST /api/v1/me/item-progress`.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/httpapi -run 'TestCatalog(ItemRoutes|GovernanceRoutes|ProgressRoutes)' -count=1</automated>
  </verify>
  <done>Router tests exist that define the additive catalog API surface and auth/mutation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive catalog item, governance, and progress handlers</name>
  <read_first>
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/catalog_api_router_test.go
    - mibo-media-server/internal/httpapi/handlers_catalog_items.go
    - mibo-media-server/internal/httpapi/handlers_catalog_governance.go
    - .planning/phases/16-catalog-api-search-progress-cutover/16-01-PLAN.md
    - .planning/phases/16-catalog-api-search-progress-cutover/16-02-PLAN.md
    - .planning/phases/16-catalog-api-search-progress-cutover/16-03-PLAN.md
  </read_first>
  <files>
    - mibo-media-server/internal/httpapi/handlers_catalog_items.go
    - mibo-media-server/internal/httpapi/handlers_catalog_governance.go
    - mibo-media-server/internal/httpapi/catalog_api_router_test.go
    - mibo-media-server/internal/httpapi/router.go
  </files>
  <behavior>
    - Test 1: `GET /api/v1/items` parses `library_id`, `parent_id`, `q`, `type`, `availability`, `sort`, and `limit`, then returns frozen catalog list DTOs.
    - Test 2: `GET /api/v1/items/{id}` and `GET /api/v1/series/{id}/seasons` return hierarchy data from catalog queries only.
    - Test 3: governance mutation routes are auth-gated, validate request bodies strictly, and delegate business rules to `catalog.Service` / `metadata.Service` instead of mutating rows inline.
    - Test 4: catalog progress routes use `UpdateCatalogProgress` / `GetCatalogProgressState` with `item_id` and optional `asset_id`; legacy `/api/v1/me/progress` remains untouched.
  </behavior>
  <action>Implement `handlers_catalog_items.go` and `handlers_catalog_governance.go` as thin handlers. `GET /api/v1/items` must parse `library_id`, optional `parent_id`, `q`, `type`, `availability`, `sort`, and `limit`, then call `r.catalog.ListItems`. `GET /api/v1/items/{id}` must call `r.catalog.GetItemDetail`. `GET /api/v1/series/{id}/seasons` must call `r.catalog.ListSeriesSeasons`. `GET /api/v1/items/{id}/progress` must require auth and call `r.progress.GetCatalogProgressState`. `POST /api/v1/me/item-progress` must require auth, decode `{item_id, asset_id?, position_seconds, duration_seconds?, completed?}`, then call `r.progress.UpdateCatalogProgress`. `GET /api/v1/items/{id}/governance` must require auth and call `r.catalog.GetGovernanceWorkspace`. `PATCH /api/v1/items/{id}/field-locks` must decode `{field_key, locked, reason}` and call `r.catalog.UpdateFieldLock`. `POST /api/v1/items/{id}/images/select` must decode `{image_id}` and call `r.catalog.SelectImage`. `POST /api/v1/items/{id}/match` and `POST /api/v1/items/{id}/refresh-metadata` must require auth and delegate to the Phase 15 metadata-service methods so source evidence/external ID refreshes stay in `internal/metadata`. `POST /api/v1/assets/{asset_id}/link-item` must decode `{item_id, role, segment_index, start_seconds?, end_seconds?, confidence?, source}` and call `r.catalog.LinkAsset`; `DELETE /api/v1/assets/{asset_id}/link-item/{item_id}` must call `r.catalog.UnlinkAsset`. Register all additive routes in `router.go` without removing or changing any legacy route registrations.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/handlers_catalog_items.go` contains `ListItems(`, `GetItemDetail(`, `ListSeriesSeasons(`, `GetCatalogProgressState(`, and `UpdateCatalogProgress(`.
    - `mibo-media-server/internal/httpapi/handlers_catalog_governance.go` contains `GetGovernanceWorkspace(`, `UpdateFieldLock(`, `SelectImage(`, `LinkAsset(`, and `UnlinkAsset(`.
    - `mibo-media-server/internal/httpapi/handlers_catalog_governance.go` contains `MatchCatalogSeries(` and `RefreshCatalogSeriesMetadata(` or explicit metadata-service delegation equivalents.
    - `mibo-media-server/internal/httpapi/router.go` contains all additive route strings from Task 1.
    - `mibo-media-server/internal/httpapi/router.go` still contains `POST /api/v1/me/progress` and `GET /api/v1/media-items/{id}` unchanged.
    - `cd mibo-media-server && go test ./internal/httpapi -run 'TestCatalog(ItemRoutes|GovernanceRoutes|ProgressRoutes)' -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/httpapi -run 'TestCatalog(ItemRoutes|GovernanceRoutes|ProgressRoutes)' -count=1</automated>
  </verify>
  <done>Additive catalog item, governance, and progress routes exist and are backed by catalog/progress/metadata services instead of legacy media-item handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP clients -> catalog item/governance/progress handlers | untrusted requests enter the API here |
| handler layer -> catalog/progress/metadata services | route payloads are translated into durable catalog and progress mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | E | governance and progress mutation routes | mitigate | require `requireUser` before decoding or mutating any catalog governance/progress state |
| T-16-08 | T | field lock/image/asset mutation handlers | mitigate | validate exact request bodies and delegate to catalog service methods that scope row updates by item/image/asset identity |
| T-16-09 | T | progress route payloads | mitigate | accept only `{item_id, asset_id?, position_seconds, duration_seconds?, completed?}` and route them through `UpdateCatalogProgress`, which validates item and asset ownership before persistence |
</threat_model>

<verification>
- `cd mibo-media-server && go test ./internal/httpapi -run 'TestCatalog(ItemRoutes|GovernanceRoutes|ProgressRoutes)' -count=1`
</verification>

<success_criteria>
- Additive catalog read routes expose list/detail/season DTOs.
- Governance routes expose workspace state and controlled mutations through catalog/metadata services.
- Progress routes use catalog item and asset IDs without changing legacy progress endpoints yet.
</success_criteria>

<output>
After completion, create `.planning/phases/16-catalog-api-search-progress-cutover/16-04-SUMMARY.md`
</output>
