---
phase: 12-catalog-kernel-contracts-migration-guards
plan: 05
type: execute
wave: 1
depends_on:
  - 12-01
files_modified:
  - mibo-media-server/internal/catalog/contracts.go
  - mibo-media-server/internal/catalog/contracts_test.go
autonomous: true
gap_closure: true
requirements:
  - KERN-01
must_haves:
  truths:
    - Catalog DTO source-evidence payloads expose curated summaries instead of full provider blobs.
    - Catalog DTO field-state payloads expose only scalar canonical values or omit unsupported raw JSON.
  artifacts:
    - path: mibo-media-server/internal/catalog/contracts.go
      provides: Curated contract projection helpers for source evidence and field-state values
      exports:
        - BuildCatalogItemDetail
        - BuildCatalogSeasonDetail
        - BuildCatalogEpisodeDetail
        - BuildCatalogGovernanceWorkspace
    - path: mibo-media-server/internal/catalog/contracts_test.go
      provides: Regression coverage that forbids raw summary/value leakage
      contains: TestCatalogJSONContractShapeAndMapperBehavior
  key_links:
    - from: mibo-media-server/internal/catalog/contracts.go
      to: mibo-media-server/internal/catalog/contracts_test.go
      via: JSON contract assertions over source_evidence.summary and field_states.value
      pattern: summary|value
---

<objective>
Close the frozen-contract gap in Phase 12 by replacing raw JSON pass-through with curated DTO-safe projections.

Purpose: downstream cutover phases must not couple to provider-specific metadata blobs.
Output: hardened catalog contract helpers plus regression tests that lock approved summary/value behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-RESEARCH.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-VERIFICATION.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-01-SUMMARY.md
@mibo-media-server/internal/catalog/contracts.go
@mibo-media-server/internal/catalog/contracts_test.go
@mibo-media-server/internal/catalog/service.go

<interfaces>
From `mibo-media-server/internal/catalog/service.go`:
```go
const (
    ItemTypeSeries          = "series"
    AvailabilityNoLocalMedia = "no_local_media"
)
```

From `mibo-media-server/internal/catalog/contracts.go`:
```go
type CatalogSourceEvidence struct {
    SourceType string `json:"source_type"`
    SourceName string `json:"source_name"`
    Summary    any    `json:"summary,omitempty"`
}

type CatalogFieldState struct {
    FieldKey string `json:"field_key"`
    Value    any    `json:"value,omitempty"`
}

func BuildCatalogItemDetail(input CatalogItemDetailInput) CatalogItemDetail
func BuildCatalogSeasonDetail(input CatalogSeasonDetailInput) CatalogSeasonDetail
func BuildCatalogEpisodeDetail(input CatalogEpisodeDetailInput) CatalogEpisodeDetail
func BuildCatalogGovernanceWorkspace(input CatalogGovernanceWorkspaceInput) CatalogGovernanceWorkspace
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing contract regressions for curated summary and value output</name>
  <files>mibo-media-server/internal/catalog/contracts_test.go</files>
  <action>Expand `TestCatalogJSONContractShapeAndMapperBehavior` so Phase 12 gap coverage explicitly proves KERN-01 stays frozen: seed `MetadataSource.PayloadJSON` with extra nested/provider-specific keys and seed `MetadataFieldState.ValueJSON` with both scalar and object payloads; assert marshaled DTOs keep approved `summary`/`value` behavior only. Require scalar field-state values to round-trip, require nested/object payloads to be omitted, and require source-evidence summaries to expose only the curated allowlist (`title`, `name`, `original_title`, `overview`, `release_date`, `first_air_date`, `last_air_date`, `runtime`, `status`, `season_number`, `episode_number`) when present as scalars. Do not loosen the existing forbidden-field assertions.</action>
  <verify>
    <automated>cd mibo-media-server &amp;&amp; go test ./internal/catalog -run 'TestCatalog(JSONContractShapeAndMapperBehavior|DTOContractExportsRequiredTypes|FileContractUsesJSONTagsOnly|TypeContractKeepsSeriesCanonical)' -count=1</automated>
  </verify>
  <done>The test suite fails before implementation and now defines exactly what `summary` and `value` are allowed to expose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace raw JSON pass-through with curated contract projections</name>
  <files>mibo-media-server/internal/catalog/contracts.go, mibo-media-server/internal/catalog/contracts_test.go</files>
  <behavior>
    - Test 1: source-evidence summaries include only allowlisted scalar fields and never return the full decoded provider payload.
    - Test 2: field-state values keep scalar canonical values but omit object/array payloads instead of leaking raw JSON.
  </behavior>
  <action>Replace `summarizeJSON()` usage in `buildCatalogSourceEvidence()` and `buildCatalogFieldStates()` with explicit projection helpers that close the verification gap from `12-VERIFICATION.md`. For source evidence, decode JSON once and emit only the approved scalar allowlist described in Task 1; if none of those keys are present, omit `Summary`. For field states, decode JSON and return only scalar values (`string`, number, boolean, null); if the stored JSON is an object or array, omit `Value` rather than forwarding raw structure. Remove the fallback behavior that returns the raw string or full decoded object. Keep all existing DTO shapes, canonical `series` normalization, and availability/governance normalization unchanged.</action>
  <verify>
    <automated>cd mibo-media-server &amp;&amp; go test ./internal/catalog -run 'TestCatalog(JSONContractShapeAndMapperBehavior|DTOContractExportsRequiredTypes|FileContractUsesJSONTagsOnly|TypeContractKeepsSeriesCanonical)' -count=1</automated>
  </verify>
  <done>`source_evidence.summary` and `field_states.value` no longer leak provider/value blobs, and the focused contract suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database metadata JSON → catalog DTO response | Stored provider payloads and field-state blobs cross into API-facing contracts here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | I | `internal/catalog/contracts.go` | mitigate | Replace raw JSON pass-through with allowlisted scalar projections so provider-specific internals never leave the contract boundary |
</threat_model>

<verification>
Run the focused catalog contract suite from this plan and confirm `12-VERIFICATION.md` gap 1 is no longer reproducible from marshaled DTO payloads.
</verification>

<success_criteria>
- Catalog DTO builders no longer return full decoded provider/value blobs under `summary` or `value`.
- Regression tests explicitly guard curated summary/value output.
- KERN-01 remains satisfied with no raw `database.*` leakage added back.
</success_criteria>

<output>
After completion, create `.planning/phases/12-catalog-kernel-contracts-migration-guards/12-05-SUMMARY.md`
</output>
