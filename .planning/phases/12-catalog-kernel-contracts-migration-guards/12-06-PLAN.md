---
phase: 12-catalog-kernel-contracts-migration-guards
plan: 06
type: execute
wave: 1
depends_on:
  - 12-03
files_modified:
  - mibo-media-server/internal/catalog/projections.go
  - mibo-media-server/internal/catalog/projections_test.go
autonomous: true
gap_closure: true
requirements:
  - PROD-01
must_haves:
  truths:
    - Projection rebuilds persist canonical `series` item types for legacy `show` rows.
    - Projection rebuilds persist canonical availability values when source rows are blank.
  artifacts:
    - path: mibo-media-server/internal/catalog/projections.go
      provides: Canonicalized catalog search document rebuild logic
      exports:
        - RefreshItemProjection
        - RefreshLibraryProjection
    - path: mibo-media-server/internal/catalog/projections_test.go
      provides: Regression coverage for legacy type and blank availability normalization
      contains: TestCatalogRefreshLibraryProjectionRebuildsTargetedRows
  key_links:
    - from: mibo-media-server/internal/catalog/projections.go
      to: mibo-media-server/internal/catalog/projections_test.go
      via: search-document rebuild assertions over `item_type` and `availability_status`
      pattern: ItemType|AvailabilityStatus
---

<objective>
Close the projection canonicalization gap in Phase 12 so rebuild jobs always write cutover-safe search documents for both legacy and new catalog rows.

Purpose: later scanner, metadata, and API cutover phases must rebuild one canonical projection shape even when legacy rows still exist.
Output: normalized search-document persistence plus regression tests for legacy `show` and blank-status inputs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-RESEARCH.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-VERIFICATION.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-03-SUMMARY.md
@mibo-media-server/internal/catalog/projections.go
@mibo-media-server/internal/catalog/projections_test.go
@mibo-media-server/internal/catalog/service.go

<interfaces>
From `mibo-media-server/internal/catalog/service.go`:
```go
const (
    ItemTypeSeries          = "series"
    AvailabilityNoLocalMedia = "no_local_media"
)
```

From `mibo-media-server/internal/catalog/projections.go`:
```go
type ProjectionRefreshRequest struct {
    ItemID    uint   `json:"item_id,omitempty"`
    LibraryID uint   `json:"library_id,omitempty"`
    RootPath  string `json:"root_path,omitempty"`
}

func (s *Service) RefreshItemProjection(ctx context.Context, itemID uint) error
func (s *Service) RefreshLibraryProjection(ctx context.Context, libraryID uint, rootPath string) error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing regression coverage for legacy type and blank availability rebuilds</name>
  <files>mibo-media-server/internal/catalog/projections_test.go</files>
  <action>Extend projection coverage so the failed truth in `12-VERIFICATION.md` is directly reproducible: seed a library projection graph whose root row uses legacy `show` type and at least one row has blank `AvailabilityStatus`, run projection refresh, and assert the persisted `CatalogSearchDocument` rows use canonical `series` and `no_local_media` values. Keep the existing targeted-row assertions, but add explicit checks on stored `item_type` and `availability_status` for both legacy and canonical inputs.</action>
  <verify>
    <automated>cd mibo-media-server &amp;&amp; go test ./internal/catalog -run 'TestCatalogRefresh(Item|Library)Projection' -count=1</automated>
  </verify>
  <done>The projection test suite fails before implementation and now proves the expected canonicalized search-document output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize search document fields before projection persistence</name>
  <files>mibo-media-server/internal/catalog/projections.go, mibo-media-server/internal/catalog/projections_test.go</files>
  <behavior>
    - Test 1: rebuilding a legacy `show` row stores `series` in `catalog_search_documents.item_type`.
    - Test 2: rebuilding a row with blank availability stores `no_local_media` in `catalog_search_documents.availability_status`.
  </behavior>
  <action>Update `buildCatalogSearchDocuments()` to route `item.Type` through `normalizeCatalogType()` and `item.AvailabilityStatus` through `normalizeAvailabilityStatus()` before writing `database.CatalogSearchDocument` rows. Preserve the existing targeted scope loading, text aggregation, and rollup logic; this fix is only for canonical persistence at the search-document boundary. Ensure the new regression tests cover both legacy and already-canonical rows so refreshes stay stable for mixed migration-era data.</action>
  <verify>
    <automated>cd mibo-media-server &amp;&amp; go test ./internal/catalog -run 'TestCatalogRefresh(Item|Library)Projection' -count=1</automated>
  </verify>
  <done>Projection rebuilds always persist canonical type and availability values, and the focused projection suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy catalog rows → `catalog_search_documents` | Legacy or incomplete item values are transformed into cutover-facing search projections here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | T | `internal/catalog/projections.go` | mitigate | Canonicalize type and availability before persistence so legacy `show`/blank values cannot corrupt projection reads during migration |
</threat_model>

<verification>
Run the focused projection suite from this plan and confirm `12-VERIFICATION.md` gap 2 is no longer reproducible from rebuilt `catalog_search_documents` rows.
</verification>

<success_criteria>
- Projection rebuilds write `series` instead of legacy `show` into search documents.
- Blank availability values are normalized to `no_local_media` before persistence.
- PROD-01 projection hardening remains covered by targeted regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/12-catalog-kernel-contracts-migration-guards/12-06-SUMMARY.md`
</output>
