---
phase: 12-catalog-kernel-contracts-migration-guards
plan: 04
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
  - 12-03
files_modified:
  - mibo-media-server/internal/database/catalog_models.go
  - mibo-media-server/internal/database/inventory_models.go
  - mibo-media-server/internal/database/database.go
  - mibo-media-server/internal/database/catalog_models_test.go
  - mibo-media-server/internal/database/database_open_test.go
autonomous: true
requirements:
  - PROD-01
must_haves:
  truths:
    - "Critical catalog hierarchy and projection lookup indexes exist before cutover work accelerates."
    - "Backend startup still works on empty databases and legacy-only databases after additive migration hardening."
  artifacts:
    - path: "mibo-media-server/internal/database/catalog_models.go"
      provides: "Composite index definitions for catalog hierarchy and search projection reads"
    - path: "mibo-media-server/internal/database/database_open_test.go"
      provides: "Empty and legacy DB startup regression tests"
  key_links:
    - from: "mibo-media-server/internal/database/catalog_models.go"
      to: "mibo-media-server/internal/database/database.go"
      via: "AutoMigrate and explicit migrator/index creation"
      pattern: "idx_catalog_items_|idx_catalog_search_documents_"
    - from: "mibo-media-server/internal/database/database_open_test.go"
      to: "mibo-media-server/internal/database/database.go"
      via: "Open(config.DatabaseConfig{...}) startup regression coverage"
      pattern: "Open\(config\.DatabaseConfig"
---

<objective>
Harden the schema boot path with the minimum indexes and compatibility checks needed before backfill and cutover phases begin.

Purpose: satisfy PROD-01 and the roadmap success criteria by adding additive composite indexes plus startup regression tests for empty and legacy databases.
Output: explicit composite indexes for catalog hierarchy/search lookups and DB-open tests proving migration safety across fresh and legacy startup states.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-RESEARCH.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-PATTERNS.md

<interfaces>
From `mibo-media-server/internal/database/catalog_models.go`:

```go
type CatalogItem struct {
    LibraryID uint
    Type string
    ParentID *uint
    RootID *uint
    SortKey string
    ParentIndexNumber *int
    IndexNumber *int
    AvailabilityStatus string
}

type CatalogSearchDocument struct {
    LibraryID uint
    ItemType string
    AvailabilityStatus string
    Title string
}
```

From GORM migration guidance in research:

```text
AutoMigrate adds missing indexes/constraints but does not delete unused columns.
Composite indexes should use a shared index name across participating fields.
Migrator().CreateIndex / CreateConstraint is the explicit additive path when needed.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add minimum composite indexes for catalog hierarchy and projection queries</name>
  <read_first>
    - mibo-media-server/internal/database/catalog_models.go
    - mibo-media-server/internal/database/inventory_models.go
    - mibo-media-server/internal/database/database.go
    - .planning/phases/12-catalog-kernel-contracts-migration-guards/12-RESEARCH.md
  </read_first>
  <files>mibo-media-server/internal/database/catalog_models.go, mibo-media-server/internal/database/inventory_models.go, mibo-media-server/internal/database/database.go</files>
  <behavior>
    - Test 1: schema migration creates composite indexes for list and hierarchy traversal queries.
    - Test 2: existing unique guarantees for provider identity, field states, asset links, and inventory files remain intact.
    - Test 3: migration remains additive and does not remove legacy tables or columns.
  </behavior>
  <action>Add the minimum cutover-era composite indexes called out in Phase 12 research. In `catalog_models.go` add shared index definitions for `catalog_items(library_id,type,availability_status,sort_key)`, `catalog_items(parent_id,parent_index_number,index_number)`, `catalog_items(root_id,type,parent_index_number,index_number)`, and `catalog_search_documents(library_id,item_type,availability_status,title)`. Keep existing uniqueness indexes on `CatalogExternalID`, `MetadataFieldState`, `AssetItem`, `InventoryFile`, and `AssetFile`. If tags alone cannot reliably create the composite indexes in both SQLite and Postgres, extend `database.go` with explicit additive `Migrator().CreateIndex(...)` calls after `AutoMigrate`; do not introduce destructive migration SQL and do not remove legacy tables in this phase.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/database/catalog_models.go` contains shared composite index names for all four required lookup patterns.
    - Existing unique indexes for external IDs, field state, asset links, asset files, and inventory files remain present.
    - `mibo-media-server/internal/database/database.go` still migrates both legacy and catalog tables in one startup path.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/database -run 'Test(CatalogKernelTablesAreMigrated|DatabaseOpen.*Catalog)' -count=1</automated>
  </verify>
  <done>Catalog cutover paths have the minimum additive index coverage required for hierarchy reads and projection rebuilds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove empty-database and legacy-database startup compatibility after schema hardening</name>
  <read_first>
    - mibo-media-server/internal/database/catalog_models_test.go
    - mibo-media-server/internal/database/database.go
    - mibo-media-server/internal/httpapi/router_test.go
    - .planning/ROADMAP.md
  </read_first>
  <files>mibo-media-server/internal/database/catalog_models_test.go, mibo-media-server/internal/database/database_open_test.go</files>
  <behavior>
    - Test 1: a fresh SQLite database starts and migrates all legacy + catalog tables.
    - Test 2: a database seeded with legacy-only tables can still open and migrate without error.
    - Test 3: repeated startup is idempotent and does not fail because indexes already exist.
  </behavior>
  <action>Extend `catalog_models_test.go` and add `database_open_test.go` to cover the roadmap success criteria explicitly. Write one test that opens a fresh SQLite DB and asserts the catalog kernel tables plus legacy tables still migrate. Write a second test that creates a SQLite DB containing only representative legacy tables/rows, then calls `database.Open(...)` and proves the new startup path succeeds. Add a repeated-open assertion so the same DB can be opened twice without duplicate-index or duplicate-constraint failures. Keep the tests focused on additive migration safety rather than business logic.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/database/database_open_test.go` contains fresh-start, legacy-start, and repeated-open coverage.
    - The legacy-start test creates legacy schema state before calling `database.Open(...)`.
    - Tests fail if additive index creation breaks repeat startup.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/database -run 'Test(CatalogKernelTablesAreMigrated|DatabaseOpen.*Catalog)' -count=1 && go test ./internal/httpapi -run TestReadyz -count=1</automated>
  </verify>
  <done>Phase 12 hardening keeps backend startup safe for both empty and legacy databases, matching the roadmap success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| startup migration path → production database | Additive schema/index changes run automatically during boot |
| query layer → catalog tables | Read performance and uniqueness assumptions depend on index integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | D | startup migration path | mitigate | Keep phase-12 schema work additive only and prove fresh, legacy, and repeated-open startup with dedicated database tests. |
| T-12-11 | T | catalog hierarchy/search lookup performance | mitigate | Add explicit composite indexes for hierarchy and projection queries rather than relying on scattered single-column indexes alone. |
| T-12-12 | T | legacy compatibility | mitigate | Preserve the single `database.Open` migration boundary and keep legacy tables in the AutoMigrate list for this phase. |
</threat_model>

<verification>
- `go test ./internal/database -run 'Test(CatalogKernelTablesAreMigrated|DatabaseOpen.*Catalog)' -count=1`
- `go test ./internal/httpapi -run TestReadyz -count=1`
</verification>

<success_criteria>
- Required composite indexes exist for hierarchy and projection lookups.
- Empty DB startup, legacy DB startup, and repeated startup all pass automated tests.
- The schema path remains additive and compatible with both legacy and catalog tables.
</success_criteria>

<output>
After completion, create `.planning/phases/12-catalog-kernel-contracts-migration-guards/12-04-SUMMARY.md`
</output>
