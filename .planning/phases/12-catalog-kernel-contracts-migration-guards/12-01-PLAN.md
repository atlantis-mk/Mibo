---
phase: 12-catalog-kernel-contracts-migration-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mibo-media-server/internal/catalog/contracts.go
  - mibo-media-server/internal/catalog/contracts_test.go
autonomous: true
requirements:
  - KERN-01
must_haves:
  truths:
    - "Developer has explicit catalog DTOs for list, detail, season, episode, asset, and governance responses."
    - "Catalog read phases can import stable response contracts without serializing raw GORM models."
  artifacts:
    - path: "mibo-media-server/internal/catalog/contracts.go"
      provides: "Explicit catalog DTO and mapper layer"
      exports: ["CatalogListItem", "CatalogItemDetail", "CatalogSeasonDetail", "CatalogEpisodeDetail", "CatalogAssetDetail", "CatalogGovernanceWorkspace"]
    - path: "mibo-media-server/internal/catalog/contracts_test.go"
      provides: "DTO contract regression tests"
  key_links:
    - from: "mibo-media-server/internal/catalog/contracts.go"
      to: "mibo-media-server/internal/database/catalog_models.go"
      via: "builder functions that map database rows into DTOs"
      pattern: "database\\.(CatalogItem|ItemRollup|CatalogExternalID|MetadataFieldState|ItemImage|MediaAsset|AssetItem)"
---

<objective>
Freeze the catalog DTO layer before any API or frontend cutover work starts.

Purpose: satisfy KERN-01 by giving later phases stable, explicit contracts that do not leak raw GORM model shapes.
Output: exported catalog DTO structs, builder/mapping helpers, and contract tests covering JSON shape and allowed status semantics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-RESEARCH.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-PATTERNS.md

<interfaces>
From `mibo-media-server/internal/catalog/service.go`:

```go
const (
    ItemTypeMovie   = "movie"
    ItemTypeSeries  = "series"
    ItemTypeSeason  = "season"
    ItemTypeEpisode = "episode"
    ItemTypeExtra   = "extra"

    AvailabilityAvailable    = "available"
    AvailabilityMissing      = "missing"
    AvailabilityUnaired      = "unaired"
    AvailabilityNoLocalMedia = "no_local_media"

    GovernancePending     = "pending"
    GovernanceMatched     = "matched"
    GovernanceNeedsReview = "needs_review"
    GovernanceLocked      = "locked"
    GovernanceManual      = "manual"
    GovernanceUnmatched   = "unmatched"
)
```

From `mibo-media-server/internal/database/catalog_models.go`:

```go
type CatalogItem struct {
    ID uint
    LibraryID uint
    Type string
    ParentID *uint
    RootID *uint
    Title string
    AvailabilityStatus string
    GovernanceStatus string
}

type ItemRollup struct {
    ItemID uint
    ChildCount int
    AvailableCount int
    MissingCount int
    UnairedCount int
    InProgressCount int
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define explicit catalog DTO structs and nested contract value objects</name>
  <read_first>
    - mibo-media-server/internal/catalog/service.go
    - mibo-media-server/internal/database/catalog_models.go
    - mibo-media-server/internal/database/inventory_models.go
    - mibo-media-server/internal/library/service.go
  </read_first>
  <files>mibo-media-server/internal/catalog/contracts.go</files>
  <behavior>
    - Test 1: exported DTO names exactly include `CatalogListItem`, `CatalogItemDetail`, `CatalogSeasonDetail`, `CatalogEpisodeDetail`, `CatalogAssetDetail`, and `CatalogGovernanceWorkspace`.
    - Test 2: DTO file contains `json` tags but no `gorm:` tags and does not embed `database.CatalogItem` or `database.MediaAsset` directly.
    - Test 3: canonical TV root type remains `series`, never `show`.
  </behavior>
  <action>Create `mibo-media-server/internal/catalog/contracts.go` with plain exported structs and builder inputs for KERN-01. Include exact DTOs `CatalogListItem`, `CatalogItemDetail`, `CatalogSeasonDetail`, `CatalogEpisodeDetail`, `CatalogAssetDetail`, and `CatalogGovernanceWorkspace`, plus nested value objects for selected images, external identities, source evidence, field states, child summaries, and asset links. Reuse catalog enum strings from `internal/catalog/service.go` (`movie|series|season|episode|extra`, availability states, governance states) and keep JSON keys in snake_case. Do not add `gorm` tags, do not expose `ParentID`, `RootID`, `DeletedAt`, or raw row structs as API contracts, and do not reintroduce legacy `show` type naming.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/catalog/contracts.go` exports all six required DTO names.
    - `mibo-media-server/internal/catalog/contracts.go` contains `json:"availability_status"` and `json:"governance_status"` fields on the relevant DTOs.
    - `mibo-media-server/internal/catalog/contracts.go` contains no `gorm:"` substrings.
    - `mibo-media-server/internal/catalog/contracts.go` contains the literal string `series` and does not contain a DTO field/value contract that names the TV root `show`.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/catalog -run 'TestCatalog.*Contract' -count=1</automated>
  </verify>
  <done>Later catalog API work can import explicit DTOs from `internal/catalog` without serializing raw database models.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock DTO JSON shape and mapper behavior with contract tests</name>
  <read_first>
    - mibo-media-server/internal/catalog/contracts.go
    - mibo-media-server/internal/catalog/service_test.go
    - mibo-media-server/internal/database/catalog_models.go
    - mibo-media-server/internal/database/inventory_models.go
  </read_first>
  <files>mibo-media-server/internal/catalog/contracts_test.go</files>
  <behavior>
    - Test 1: marshaled DTO JSON contains required keys for item, season/episode status, asset identifiers, and governance workspace sections.
    - Test 2: mapper/build helper output excludes raw DB-only fields such as `deleted_at` and raw `payload_json` unless intentionally summarized under source evidence.
    - Test 3: season/episode DTO coverage preserves `available`, `missing`, and `unaired` semantics.
  </behavior>
  <action>Create `mibo-media-server/internal/catalog/contracts_test.go` with table-driven tests that instantiate representative `database.CatalogItem`, `database.ItemRollup`, `database.CatalogExternalID`, `database.MetadataFieldState`, `database.ItemImage`, and `database.MediaAsset` inputs, then assert the DTO/builders from Task 1 serialize the expected JSON fields. Use exact assertions for `type`, `availability_status`, `governance_status`, `assets`, `source_evidence`, and `field_states`. Fail the tests if raw DB-only fields leak into marshaled JSON or if `series`/`season`/`episode` hierarchy contracts collapse back to legacy `MediaItem` semantics.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/catalog/contracts_test.go` contains at least one JSON serialization assertion for each required DTO family.
    - Contract tests assert the presence of `availability_status`, `governance_status`, `assets`, and `field_states` in the marshaled payloads where applicable.
    - Contract tests fail if raw DB-only fields such as `deleted_at` are emitted.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/catalog -run 'TestCatalog.*Contract' -count=1</automated>
  </verify>
  <done>Catalog DTOs are protected by regression tests, so later API/front-end cutover changes cannot silently drift the contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service layer → HTTP/API consumers | Internal catalog rows become externally consumable contract data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | I | `internal/catalog/contracts.go` | mitigate | Replace raw `database.*` exposure with explicit DTO structs so internal-only fields are not serialized by accident. |
| T-12-02 | T | DTO status fields | mitigate | Reuse catalog enum strings from `internal/catalog/service.go` and lock them with contract tests for `series`, availability, and governance states. |
| T-12-03 | R | contract evolution | mitigate | Add JSON-shape tests so future changes leave an auditable failing test instead of silent contract drift. |
</threat_model>

<verification>
- `go test ./internal/catalog -run 'TestCatalog.*Contract' -count=1`
- Confirm no `gorm:"` tags exist in `mibo-media-server/internal/catalog/contracts.go`
</verification>

<success_criteria>
- All six required catalog DTOs exist and are exported.
- DTOs expose explicit JSON contracts without embedding raw DB rows.
- Contract tests cover list/detail/season/episode/asset/governance JSON shape.
</success_criteria>

<output>
After completion, create `.planning/phases/12-catalog-kernel-contracts-migration-guards/12-01-SUMMARY.md`
</output>
