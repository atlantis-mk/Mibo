---
phase: 02-library-async-sync-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - web/src/lib/mibo-api.ts
  - web/src/features/app/components/source-drawer.tsx
  - web/src/features/app/components/library-drawer.tsx
  - web/src/features/app/components/jobs-list.tsx
  - web/src/components/settings/settings-shell.tsx
autonomous: false
requirements:
  - LIBR-01
  - LIBR-02
  - LIBR-03
  - LIBR-04
  - CATA-06
must_haves:
  truths:
    - Administrators can create a source and library without extra V1-only fields beyond the agreed minimal inputs.
    - Library cards show current scan state at a glance and failed work can be inspected in a dedicated jobs surface.
    - Administrators can review and update the global refresh interval from the same settings workflow.
  artifacts:
    - path: web/src/lib/mibo-api.ts
      provides: Typed frontend contract for jobs and scan settings endpoints
    - path: web/src/features/app/components/jobs-list.tsx
      provides: Jobs monitoring and retry UI
    - path: web/src/components/settings/settings-shell.tsx
      provides: Status badge, jobs tab, and refresh interval controls
    - path: web/src/features/app/components/source-drawer.tsx
      provides: Minimal source configuration flow aligned with D-04
    - path: web/src/features/app/components/library-drawer.tsx
      provides: Library creation flow bound to source + root path
  key_links:
    - from: web/src/components/settings/settings-shell.tsx
      to: web/src/features/app/components/jobs-list.tsx
      via: Jobs tab composition
      pattern: JobsList
    - from: web/src/features/app/components/jobs-list.tsx
      to: web/src/lib/mibo-api.ts
      via: listJobs / retryJob client calls
      pattern: listJobs|retryJob
    - from: web/src/features/app/components/source-drawer.tsx
      to: web/src/lib/mibo-api.ts
      via: create source submit handler
      pattern: createMediaSource
---

<objective>
Finish the Phase 2 admin experience so sources/libraries stay minimal to configure, scan state is visible immediately, and background work is observable from the web settings shell.

Purpose: Turn the backend async foundation into an admin flow that satisfies LIBR-01 through LIBR-04 without exposing storage-provider internals.
Output: Updated typed API client, minimal source/library forms, jobs list UI, status badges, and a human verification checkpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-library-async-sync-foundation/02-CONTEXT.md
@.planning/phases/02-library-async-sync-foundation/02-RESEARCH.md
@.planning/phases/02-library-async-sync-foundation/02-VALIDATION.md
@.planning/phases/01-access-platform-boundary/01-02-SUMMARY.md

<interfaces>
From `web/src/lib/mibo-api.ts`:
```ts
export type Job = {
  id: number
  kind: string
  status: string
}

listJobs(options?: { status?: string; kind?: string; limit?: number })
retryJob(jobId: number)
```

From `web/src/features/app/components/jobs-list.tsx`:
```tsx
type JobsListProps = {
  apiBaseUrl: string
  token: string
}

export function JobsList({ apiBaseUrl, token }: JobsListProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep source and library creation flows minimal and typed</name>
  <files>web/src/lib/mibo-api.ts, web/src/features/app/components/source-drawer.tsx, web/src/features/app/components/library-drawer.tsx</files>
  <read_first>web/src/lib/mibo-api.ts; web/src/features/app/components/source-drawer.tsx; web/src/features/app/components/library-drawer.tsx</read_first>
  <action>Update the frontend client and creation drawers so Phase 2 admin setup stays aligned with D-04: local sources only collect provider plus root path, and library creation only binds source plus root path/name without adding exclude-path, watch-folder, or scan-depth fields. Keep all requests routed through `createMiboApi()` and add typed client methods for scan settings and jobs if they are still missing from `mibo-api.ts`. Preserve the existing `StorageProvider` boundary by describing provider choices in product terms instead of leaking OpenList implementation details.</action>
  <acceptance_criteria>
    - `web/src/lib/mibo-api.ts` contains typed methods for scan settings retrieval/update and jobs list/retry.
    - `web/src/features/app/components/source-drawer.tsx` does not introduce extra local-source fields beyond provider/root path per D-04.
    - `web/src/features/app/components/library-drawer.tsx` submits source selection and root path as the library binding contract.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm build</automated>
  </verify>
  <done>Admins can still create sources and libraries with the smallest agreed configuration surface while the frontend gains typed access to the new async-sync endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Add scan status, jobs monitoring, and refresh controls to settings</name>
  <files>web/src/features/app/components/jobs-list.tsx, web/src/components/settings/settings-shell.tsx, web/src/lib/mibo-api.ts</files>
  <read_first>web/src/features/app/components/jobs-list.tsx; web/src/components/settings/settings-shell.tsx; web/src/lib/mibo-api.ts</read_first>
  <action>Implement the D-01 hybrid observability flow inside `web/src/components/settings/settings-shell.tsx`: keep quick feedback on library cards with a status badge derived from `library.status`, and compose a dedicated Jobs tab that renders `JobsList` for detailed monitoring and retry. In `web/src/features/app/components/jobs-list.tsx`, fetch jobs through the typed API client, support `status` and `kind` filters, render failed-job retry actions, and render any job error text as plain text only. Add a global refresh interval editor in settings that calls the scan settings endpoints from Plan 02-01, reflects the current persisted value, and shows success/error toast feedback per D-02.</action>
  <acceptance_criteria>
    - `web/src/components/settings/settings-shell.tsx` contains `JobsList` composition and a library status badge helper.
    - `web/src/features/app/components/jobs-list.tsx` calls `api.listJobs(...)` and `api.retryJob(...)`.
    - `web/src/components/settings/settings-shell.tsx` contains controls bound to `refresh_interval_hours`.
    - No file in this task uses `dangerouslySetInnerHTML` for job errors or status text.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm build</automated>
  </verify>
  <done>Settings exposes both quick and deep async-sync feedback: card badges, jobs monitoring, retry, and global refresh interval management.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the admin async-sync flow works end-to-end</name>
  <read_first>web/src/components/settings/settings-shell.tsx; web/src/features/app/components/jobs-list.tsx; web/src/features/app/components/source-drawer.tsx; web/src/features/app/components/library-drawer.tsx</read_first>
  <what-built>Minimal source/library creation, status badges, jobs monitoring, retry controls, and refresh interval editing in the web settings shell.</what-built>
  <acceptance_criteria>
    - Settings shows the agreed minimal source/library fields from D-04.
    - A triggered scan produces matching status badge and Jobs tab state per D-01.
    - Refresh interval edits survive a page refresh because the backend persisted the value from D-02.
  </acceptance_criteria>
  <how-to-verify>
    1. Start the backend with local demo media: `cd mibo-media-server && MIBO_STORAGE_PROVIDER=local MIBO_LOCAL_ROOT_PATH=/Users/atlan/Desktop/IdeaProjects/Mibo/demo-media go run ./cmd/mibo-media-server`.
    2. Start the frontend: `cd web && pnpm dev`, then sign in with `admin` / `admin123`.
    3. In Settings, create or edit a local source and library. Confirm the forms only ask for the agreed minimal fields per D-04.
    4. Trigger a scan. Confirm the library card status changes (`pending`/`syncing`/`active`/`error`) and the Jobs tab shows the matching queued/running/completed job.
    5. Change the global refresh interval and confirm the UI shows the persisted value after refresh.
  </how-to-verify>
  <verify>
    <automated>cd web && pnpm build</automated>
  </verify>
  <done>Human approval confirms the settings flow matches the planned admin experience, or issues are captured for follow-up.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → typed API client | User-controlled browser state drives authenticated admin requests |
| API response data → DOM | Job errors and status text cross from server data into rendered UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | S | `source-drawer.tsx` / `library-drawer.tsx` | mitigate | Keep all create/update actions routed through authenticated `createMiboApi()` calls instead of ad hoc fetches or unauthenticated endpoints |
| T-02-04 | I | `jobs-list.tsx` / `settings-shell.tsx` | mitigate | Render server-provided job errors and statuses as plain text React nodes; do not use raw HTML injection |
| T-02-05 | E | settings refresh controls | mitigate | Limit refresh interval editing to the existing authenticated settings shell and only call the backend scan settings endpoints |
</threat_model>

<verification>
- `cd web && pnpm build`
- Human verification of the Settings flow using the checkpoint above
</verification>

<success_criteria>
- Source/library creation remains minimal and aligned with D-04.
- Library cards expose current sync health while Jobs tab provides detailed async observability and retry.
- Global refresh interval can be read and updated from the web admin surface.
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-async-sync-foundation/02-02-SUMMARY.md`
</output>
