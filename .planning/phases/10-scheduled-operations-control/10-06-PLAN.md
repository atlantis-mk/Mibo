---
phase: 10-scheduled-operations-control
plan: 06
type: execute
wave: 5
depends_on:
  - 10-04
  - 10-05
files_modified:
  - web/src/lib/mibo-api.ts
  - web/src/lib/mibo-query.ts
  - web/src/routes/_app.schedules.index.tsx
  - web/src/features/schedules/index.tsx
  - web/src/features/schedules/workspace.tsx
autonomous: true
requirements:
  - SJOB-01
  - SJOB-02
  - SJOB-03
  - SJOB-04
  - SJOB-05
  - SJOB-06
  - SJOB-07
  - SJOB-08
must_haves:
  truths:
    - Admins have a dedicated schedules workspace that acts as the primary management surface per D-01/D-03.
    - The workspace list shows enabled state, next run, latest result, and schedule type/scope at a glance per D-13.
    - Schedule data is loaded through typed API/query layers, not ad hoc fetches.
  artifacts:
    - path: web/src/lib/mibo-api.ts
      provides: typed schedule API contract.
    - path: web/src/lib/mibo-query.ts
      provides: schedule query keys/helpers.
    - path: web/src/features/schedules/workspace.tsx
      provides: dedicated schedules workspace list/detail shell.
  key_links:
    - from: web/src/features/schedules/workspace.tsx
      to: web/src/lib/mibo-query.ts
      via: TanStack Query schedule data loading
      pattern: useQuery|useMutation
    - from: web/src/routes/_app.schedules.index.tsx
      to: web/src/features/schedules/index.tsx
      via: dedicated admin workspace route
      pattern: createFileRoute
---

<objective>
Ship the primary schedule-management workspace and typed frontend contract on top of the finished backend schedule APIs.

Purpose: Phase 10’s admin UX is locked to a dedicated workspace, not a settings-tab overload, and it must surface schedule state through typed API/query contracts.
Output: Schedule route, typed frontend API/query layer, and a schedule workspace showing list/status/history-friendly structure.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-scheduled-operations-control/10-CONTEXT.md
@.planning/phases/10-scheduled-operations-control/10-RESEARCH.md
@.planning/phases/07-metadata-governance-matching/07-SUMMARY.md

<interfaces>
From `web/src/lib/mibo-query.ts`:

```ts
export const miboQueryKeys = {
  homeData: (token: string) => ['home', 'hero', token] as const,
  mediaItemDetail: (token: string, mediaItemId: number) =>
    ['media', 'detail', token, mediaItemId] as const,
}

export function createAuthedMiboApi(token: string) {
  return createMiboApi({ baseUrl: getApiBaseUrl(), token })
}
```

From metadata-governance workspace pattern:

```tsx
export default function MetadataGovernancePage({ mediaItemId }: { mediaItemId?: number })
export function MetadataGovernanceWorkspace({ token }: { token: string })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the typed web schedule contract and query helpers</name>
  <files>web/src/lib/mibo-api.ts, web/src/lib/mibo-query.ts</files>
  <action>Add typed schedule request/response models and client methods for list, create, update, toggle, run-now, and history endpoints. Mirror the backend schedule-centric shape directly so the workspace can render D-13 through D-15 without parsing `payload_json` or falling back to `/api/v1/jobs`. Keep all server-state loading inside `mibo-query.ts` helpers and TanStack Query patterns already used elsewhere in the app.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>`mibo-api.ts` and `mibo-query.ts` expose typed schedule contracts and query helpers aligned with the new backend APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dedicated schedules workspace route and list/detail shell</name>
  <files>web/src/routes/_app.schedules.index.tsx, web/src/features/schedules/index.tsx, web/src/features/schedules/workspace.tsx</files>
  <action>Create a dedicated schedules workspace route following the metadata-governance admin workspace pattern per D-01 and D-03. The workspace must make the schedule list the main management surface and show each schedule’s kind, scope, frequency summary, enabled state, next run time, latest result, and a detail-layer entry for recent runs per D-13/D-14/D-15. Keep the route file thin, keep the page feature-owned under `web/src/features/schedules`, and use existing shadcn primitives rather than custom one-off markup.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>The app has a dedicated `/schedules` workspace route with a schedule-first list/detail shell and no raw fetch logic in feature components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed API responses → workspace UI | The workspace must render trusted fields directly and avoid reconstructing meaning from raw payload JSON. |
| admin interactions → schedule mutations | UI controls must call authenticated API helpers, not bypass typed contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | T | `web/src/lib/mibo-api.ts` | mitigate | Define explicit TypeScript types for schedule/list/history payloads so the UI cannot drift into payload-json parsing or cron-text assumptions. |
| T-10-06-02 | I | `workspace.tsx` | mitigate | Show only schedule-centric fields (`enabled`, `next_run_at`, latest result, recent runs) and keep raw backend execution payloads out of the UI surface. |
| T-10-06-03 | D | route/workspace UX | mitigate | Keep the dedicated route as the primary workspace per D-01/D-03 so schedule management does not get buried in settings tabs. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm build`
- Confirm the workspace route is independent from settings and data comes from typed query helpers.
</verification>

<success_criteria>
- Admins have a dedicated schedules workspace route.
- The workspace surfaces enabled state, next run, latest result, and history entry points directly.
- Frontend schedule data flows through typed API + TanStack Query helpers only.
</success_criteria>

<output>
After completion, create `.planning/phases/10-scheduled-operations-control/10-06-SUMMARY.md`
</output>
