---
phase: 10-scheduled-operations-control
plan: 07
type: execute
wave: 6
depends_on:
  - 10-06
files_modified:
  - web/src/features/schedules/workspace.tsx
  - web/src/features/schedules/components/schedule-form-dialog.tsx
  - web/src/features/schedules/components/schedule-list.tsx
  - web/src/features/schedules/components/schedule-run-history-drawer.tsx
  - web/src/features/settings/index.tsx
autonomous: true
requirements:
  - SJOB-01
  - SJOB-02
  - SJOB-03
  - SJOB-04
  - SJOB-05
  - SJOB-06
  - SJOB-07
  - SJOB-08
must_haves:
  truths:
    - Admins can create/edit schedules, enable/disable them, run them now, and inspect recent history from the workspace.
    - The settings “通知与任务” area acts as a summary/jump-off surface, not the main workspace, per D-02.
    - Run history is shown in a detail layer instead of being stuffed into the main list, per D-14.
  artifacts:
    - path: web/src/features/schedules/components/schedule-form-dialog.tsx
      provides: create/edit schedule flow.
    - path: web/src/features/schedules/components/schedule-run-history-drawer.tsx
      provides: schedule-centric history detail layer.
    - path: web/src/features/settings/index.tsx
      provides: auxiliary schedule summary / entry point.
  key_links:
    - from: web/src/features/schedules/workspace.tsx
      to: web/src/features/schedules/components/schedule-form-dialog.tsx
      via: create/edit/toggle/run-now interactions
      pattern: useMutation|Dialog
    - from: web/src/features/settings/index.tsx
      to: web/src/routes/_app.schedules.index.tsx
      via: auxiliary summary CTA
      pattern: Link
---

<objective>
Finish the admin UX by adding schedule mutations, detail-layer history, and the settings summary entry that points into the dedicated workspace.

Purpose: Phase 10’s product promise is a schedule management surface, not just read-only status. Admins must be able to mutate schedules and jump in from settings without settings becoming the primary workspace.
Output: Schedule create/edit/toggle/run-now UI, history drawer, and settings summary bridge.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-scheduled-operations-control/10-CONTEXT.md
@.planning/phases/10-scheduled-operations-control/10-RESEARCH.md
@web/src/features/settings/index.tsx

<interfaces>
From Plan 10-06 frontend contract:

```ts
type Schedule = {
  id: number
  kind: string
  scope_kind: 'global' | 'library'
  enabled: boolean
  next_run_at?: string
  latest_run_status?: 'queued' | 'running' | 'completed' | 'failed'
}
```

From shadcn/form rules already used in settings:

```tsx
<FieldGroup>
  <Field>
    <FieldLabel />
  </Field>
</FieldGroup>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement schedule create/edit/toggle/run-now and history detail interactions</name>
  <files>web/src/features/schedules/workspace.tsx, web/src/features/schedules/components/schedule-form-dialog.tsx, web/src/features/schedules/components/schedule-list.tsx, web/src/features/schedules/components/schedule-run-history-drawer.tsx</files>
  <action>Turn the workspace into a full management surface: add create/edit form flows, enable/disable controls, run-now actions, and a detail-layer history drawer that shows recent runs per D-14/D-15. Keep the schedule model product-shaped per D-04/D-05 by presenting daily/weekly/monthly templates instead of any cron text. Use existing shadcn form primitives and mutation/toast patterns; do not push management complexity back into settings or invent raw fetch/mutation glue outside the typed API layer.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>The schedules workspace supports create/edit/toggle/run-now/history flows using product-friendly frequency templates and detail-layer history.</done>
</task>

<task type="auto">
  <name>Task 2: Add the settings summary entry and run integrated frontend verification</name>
  <files>web/src/features/settings/index.tsx</files>
  <action>Update the existing “通知与任务” area so it acts as an auxiliary schedule summary / CTA per D-02 without replacing the dedicated workspace. Show concise schedule-related summary content and a clear path into `/schedules`, but keep the full list, mutations, and history management inside the dedicated workspace from D-01/D-03. Finish by running the integrated frontend validation commands and fixing any route/build/type issues they surface.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Settings now offers a lightweight schedule summary/jump-off entry, and the full frontend schedule flow passes build verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin form input → schedule mutation requests | Frequency/template input must stay within the typed product contract. |
| settings summary → dedicated workspace routing | Settings must not accidentally become the full management surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | T | schedule form dialog | mitigate | Use explicit daily/weekly/monthly form controls and typed mutations so the UI cannot send cron text or item-level scope data. |
| T-10-07-02 | D | settings summary UX | mitigate | Keep full CRUD/history in the dedicated workspace and limit settings to summary + CTA per D-02, preventing workspace regression into a crowded tab. |
| T-10-07-03 | I | history drawer | mitigate | Present schedule-run status/error summaries only; do not expose raw payload JSON or internal execution details in the drawer. |
</threat_model>

<verification>
- `pnpm typecheck`
- `pnpm build`
- Confirm the settings page links into the dedicated workspace instead of hosting the full schedule management surface.
</verification>

<success_criteria>
- Admins can create/edit/toggle/run schedules and inspect recent history from the dedicated workspace.
- The settings page provides only summary/entry behavior.
- Frontend schedule flows compile cleanly and follow the locked information architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/10-scheduled-operations-control/10-07-SUMMARY.md`
</output>
