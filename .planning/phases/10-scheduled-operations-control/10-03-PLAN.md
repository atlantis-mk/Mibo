---
phase: 10-scheduled-operations-control
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - mibo-media-server/internal/metadata/schedule_jobs.go
  - mibo-media-server/internal/metadata/schedule_jobs_test.go
autonomous: true
requirements:
  - SJOB-02
  - SJOB-03
  - SJOB-06
must_haves:
  truths:
    - Metadata refetch, trailer sync, and artwork refresh schedules each have real batch executors.
    - Trailer sync stays metadata-owned and continues to use persisted Mibo detail data instead of direct client fetches.
    - Artwork refresh does not become a generic “full metadata rewrite everything” placeholder.
  artifacts:
    - path: mibo-media-server/internal/metadata/schedule_jobs.go
      provides: metadata-owned scheduled maintenance executors.
    - path: mibo-media-server/internal/metadata/schedule_jobs_test.go
      provides: regression proof for metadata/trailer/artwork schedule work.
  key_links:
    - from: mibo-media-server/internal/metadata/schedule_jobs.go
      to: MatchItem / RefetchItem / trailer helpers
      via: batch maintenance executor methods
      pattern: MatchItem|RefetchItem|trailer
---

<objective>
Implement the metadata-owned maintenance executors that scheduled operations need for metadata refetches, trailer syncs, and artwork refreshes.

Purpose: Phase 10 must ship the six locked maintenance kinds as real operations, and the metadata/trailer/artwork half belongs in `internal/metadata`, not in the scheduler or HTTP layer.
Output: Batch metadata maintenance executors plus tests proving metadata refetch, trailer sync, and artwork refresh stay within metadata ownership.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scheduled-operations-control/10-CONTEXT.md
@.planning/phases/10-scheduled-operations-control/10-RESEARCH.md
@.planning/phases/09-trailer-discovery-playback/09-SUMMARY.md

<interfaces>
From existing `internal/metadata` signatures:

```go
func (s *Service) MatchItem(ctx context.Context, mediaItemID uint) error
func (s *Service) RefetchItem(ctx context.Context, mediaItemID uint) error
```

From Phase 9 established pattern:

```text
Trailer contract pattern: backend normalizes one playable trailer and omits the field entirely when unavailable.
```

From Plan 10-01 (`internal/schedule` contract to create):

```go
type DueSchedule struct {
    Kind      string
    ScopeKind ScopeKind
    LibraryID *uint
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add library-scoped metadata refetch and trailer sync executors</name>
  <files>mibo-media-server/internal/metadata/schedule_jobs.go, mibo-media-server/internal/metadata/schedule_jobs_test.go</files>
  <behavior>
    - Test 1: `metadata_refetch` fans out across the selected scope and refreshes persisted metadata through existing metadata ownership paths.
    - Test 2: `trailer_sync` refreshes persisted trailer state while preserving the Phase 9 single-trailer contract.
    - Test 3: both executors work for `global` and `library` scope only per D-08.
  </behavior>
  <action>Create metadata-owned batch executors that iterate the selected schedule scope and call existing metadata primitives rather than inventing a parallel TMDB client flow. `metadata_refetch` should reuse metadata refresh semantics across the selected libraries; `trailer_sync` should remain a distinct maintenance kind per D-09 even if it shares fetch helpers with metadata refresh. Keep the frontend contract safe by ensuring the output still lands on persisted `MediaItem` fields and the Phase 9 trailer selection rules remain authoritative.</action>
  <verify>
    <automated>go test ./internal/metadata -run 'Test.*(ScheduledMetadata|TrailerSync)'</automated>
  </verify>
  <done>Metadata refetch and trailer sync exist as real batch maintenance executors with scope-aware tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add artwork refresh executor and regression proof</name>
  <files>mibo-media-server/internal/metadata/schedule_jobs.go, mibo-media-server/internal/metadata/schedule_jobs_test.go</files>
  <behavior>
    - Test 1: `artwork_refresh` updates poster/backdrop/logo-related fields for the selected scope.
    - Test 2: artwork refresh does not silently become a generic destructive metadata overwrite beyond the artwork-related surface.
  </behavior>
  <action>Add a dedicated `artwork_refresh` executor under `internal/metadata` that refreshes artwork-oriented fields while keeping schedule type identity explicit per D-09. Reuse existing metadata/provider helpers where possible, but make the batch entry point deliberate so future schedules and history rows can report “artwork refresh” as a first-class operation rather than a mislabeled generic refetch.</action>
  <verify>
    <automated>go test ./internal/metadata -run 'Test.*ArtworkRefresh'</automated>
  </verify>
  <done>`artwork_refresh` is a first-class metadata maintenance executor with regression coverage proving its scope and field intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedule scope → metadata batch execution | A bad scope fan-out could rewrite metadata across the wrong libraries. |
| TMDB/provider fetch → persisted artwork/trailer fields | External data must be normalized before it becomes product-owned state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | T | `internal/metadata/schedule_jobs.go` | mitigate | Resolve eligible items by validated `global` / `library` scope only and reuse existing metadata ownership boundaries. |
| T-10-03-02 | I | trailer/artwork persistence | mitigate | Preserve Phase 9’s normalized single-trailer contract and only persist sanitized artwork/trailer fields through existing helpers. |
| T-10-03-03 | D | batch metadata work | mitigate | Add package tests covering scoped fan-out so one bad executor cannot flood the worker with uncontrolled rewrites. |
</threat_model>

<verification>
- `go test ./internal/metadata -run 'Test.*(ScheduledMetadata|TrailerSync|ArtworkRefresh)'`
- Confirm schedule executors accept only `global` / `library` scope and keep trailer/artwork behavior metadata-owned.
</verification>

<success_criteria>
- Metadata refetch, trailer sync, and artwork refresh are real backend maintenance operations.
- Trailer sync preserves the Phase 9 persisted-trailer contract.
- Later schedule/API plans can trigger these kinds without inventing domain logic on the fly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-scheduled-operations-control/10-03-SUMMARY.md`
</output>
