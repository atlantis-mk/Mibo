---
phase: 10-scheduled-operations-control
plan: 05
type: execute
wave: 4
depends_on:
  - 10-04
files_modified:
  - mibo-media-server/internal/schedule/service.go
  - mibo-media-server/internal/worker/worker.go
  - mibo-media-server/internal/worker/worker_test.go
autonomous: true
requirements:
  - SJOB-01
  - SJOB-02
  - SJOB-03
  - SJOB-04
  - SJOB-05
  - SJOB-06
  - SJOB-07
  - SJOB-08
must_haves:
  truths:
    - Due schedules are claimed and turned into standard queued jobs by the existing worker lifecycle.
    - Schedule run history transitions through queued/running/completed/failed in lockstep with job execution.
    - The old scan interval no longer acts as the primary recurring-maintenance product path.
  artifacts:
    - path: mibo-media-server/internal/worker/worker.go
      provides: due-schedule polling plus schedule-managed job lifecycle updates.
    - path: mibo-media-server/internal/worker/worker_test.go
      provides: regression proof for due-run enqueueing and schedule-run status propagation.
    - path: mibo-media-server/internal/schedule/service.go
      provides: schedule-due claiming and legacy scan migration helpers.
  key_links:
    - from: mibo-media-server/internal/worker/worker.go
      to: mibo-media-server/internal/schedule/service.go
      via: claim due schedules and mark run status
      pattern: ClaimDue|MarkRun
    - from: mibo-media-server/internal/worker/worker.go
      to: mibo-media-server/internal/jobs/service.go
      via: enqueue due schedule work on the same queue
      pattern: Enqueue|ClaimNext|Complete|Fail
---

<objective>
Wire schedule lifecycle into the existing worker so recurring maintenance executes through one async path and produces trustworthy run history.

Purpose: Phase 10 is incomplete until due schedules are actually claimed, enqueued, executed, and reflected back into schedule-centric history and latest-result snapshots.
Output: Worker-level due-schedule polling, schedule-run status propagation, and legacy scan-setting migration handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-scheduled-operations-control/10-CONTEXT.md
@.planning/phases/10-scheduled-operations-control/10-RESEARCH.md

<interfaces>
From existing `worker.Runner`:

```go
type Runner struct {
    jobs     *jobs.Service
    library  *library.Service
    metadata *metadata.Service
    probe    *probe.Service
    settings *settings.Service
}

func (r *Runner) Run(ctx context.Context)
func (r *Runner) RunOnce(ctx context.Context)
```

From schedule orchestration created in Plan 10-04:

```go
// schedule.Service must provide due-claiming and run-status hooks
// used by the worker before and after queued job execution.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the old scan-only timer with due-schedule worker polling</name>
  <files>mibo-media-server/internal/schedule/service.go, mibo-media-server/internal/worker/worker.go, mibo-media-server/internal/worker/worker_test.go</files>
  <behavior>
    - Test 1: due schedules are claimed and translated into queued maintenance jobs using the existing jobs queue.
    - Test 2: manual and recurring triggers both use the same schedule-run status language (`queued`, `running`, `completed`, `failed`) per D-12.
    - Test 3: the old scan interval no longer acts as the primary execution path, only as migration/compatibility input per D-17.
  </behavior>
  <action>Extend `worker.Runner` to poll due schedule rows, enqueue the matching maintenance job via the schedule service, and then continue processing jobs through the existing `ClaimNext -> handleJob -> Complete/Fail` path. Remove the old “scheduled scans only” behavior as the primary recurring model; instead, preserve legacy scan settings only as compatibility/migration input per D-17. Do not create a second executor loop or out-of-band maintenance dispatcher.</action>
  <verify>
    <automated>go test ./internal/worker -run 'Test.*(Schedule|LegacyScan|Migration)'</automated>
  </verify>
  <done>The worker polls due schedules, enqueues schedule-managed jobs on the existing queue, and no longer treats the old scan interval as the main product path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate worker outcomes back into schedule run history and latest-result snapshots</name>
  <files>mibo-media-server/internal/schedule/service.go, mibo-media-server/internal/worker/worker.go, mibo-media-server/internal/worker/worker_test.go</files>
  <behavior>
    - Test 1: queued jobs move the corresponding schedule run from `queued` to `running` to `completed` or `failed`.
    - Test 2: schedule latest-result snapshot fields update when the worker finishes a schedule-managed job.
  </behavior>
  <action>Hook schedule-run lifecycle updates into the worker’s job handling so every schedule-triggered maintenance job feeds D-13 through D-15: latest result, recent run history, and standard status language. Store only concise operator-facing error summaries in history. Keep the linkage server-side via schedule-run/job metadata; never trust client-submitted history rows or status updates.</action>
  <verify>
    <automated>go test ./internal/worker -run 'Test.*Schedule' && go test ./internal/worker</automated>
  </verify>
  <done>Schedule runs reflect real worker outcomes and the schedule latest-result snapshot stays aligned with async execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| due schedules → jobs queue | Recurring triggers can flood execution if due-claim logic is wrong. |
| worker outcome → schedule history | Run-history integrity depends on correct status propagation from real job execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | D | `worker.Runner` due polling | mitigate | Claim only schedules whose `next_run_at` is due and persist claim/run rows before enqueueing to prevent duplicate flood behavior. |
| T-10-05-02 | R | schedule run history | mitigate | Update `ScheduleRun` and latest-result snapshots only from real worker state transitions, not from client assumptions. |
| T-10-05-03 | T | legacy scan migration path | mitigate | Keep legacy scan settings as compatibility input only; formal schedule rows remain the authoritative recurring-maintenance source per D-17. |
</threat_model>

<verification>
- `go test ./internal/worker -run 'Test.*(Schedule|LegacyScan|Migration)'`
- `go test ./internal/worker`
- Confirm schedule-run/latest-result fields change only through worker-executed outcomes.
</verification>

<success_criteria>
- Due schedules are truly executable through the existing jobs/worker path.
- Schedule history and latest-result snapshots reflect real worker outcomes.
- The old scan interval is demoted to migration/compatibility status, not product ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/10-scheduled-operations-control/10-05-SUMMARY.md`
</output>
