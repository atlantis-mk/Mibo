---
phase: 10-scheduled-operations-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mibo-media-server/internal/database/models.go
  - mibo-media-server/internal/database/database.go
  - mibo-media-server/internal/schedule/frequency.go
  - mibo-media-server/internal/schedule/service.go
  - mibo-media-server/internal/schedule/service_test.go
autonomous: true
requirements:
  - SJOB-01
  - SJOB-02
  - SJOB-03
  - SJOB-04
  - SJOB-05
  - SJOB-06
  - SJOB-07
  - SJOB-08
must_haves:
  truths:
    - Admin schedules have persisted definitions separate from raw job rows.
    - Every schedule can express a product-friendly daily, weekly, or monthly recurrence without exposing cron text.
    - Every schedule can project next run time and keep schedule-centric latest-result/history data.
  artifacts:
    - path: mibo-media-server/internal/database/models.go
      provides: schedule and schedule-run persistence models.
    - path: mibo-media-server/internal/schedule/frequency.go
      provides: typed frequency templates and next-run calculation.
    - path: mibo-media-server/internal/schedule/service.go
      provides: validated schedule CRUD/history foundation.
  key_links:
    - from: mibo-media-server/internal/schedule/frequency.go
      to: mibo-media-server/internal/schedule/service.go
      via: validated next_run_at computation during create/update
      pattern: NextRun|ComputeNextRun
    - from: mibo-media-server/internal/database/database.go
      to: mibo-media-server/internal/database/models.go
      via: AutoMigrate schedule tables
      pattern: AutoMigrate
---

<objective>
Create the persisted schedule domain and typed recurrence rules that every later Phase 10 slice builds on.

Purpose: Phase 10 cannot stay product-native or schedule-centric without first-class schedule rows, run-history rows, and next-run math that honors D-04 through D-09 and D-13 through D-15.
Output: Migrated `Schedule` / `ScheduleRun` tables plus an `internal/schedule` service that validates scope/kind/frequency and computes `next_run_at`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scheduled-operations-control/10-CONTEXT.md
@.planning/phases/10-scheduled-operations-control/10-RESEARCH.md

<interfaces>
From `mibo-media-server/internal/jobs/service.go`:

```go
const (
    StatusQueued    = "queued"
    StatusRunning   = "running"
    StatusCompleted = "completed"
    StatusFailed    = "failed"
)

func (s *Service) Enqueue(ctx context.Context, kind string, payload any) (database.Job, error)
func (s *Service) EnqueueUnique(ctx context.Context, kind, jobKey string, payload any) (database.Job, error)
```

From `mibo-media-server/internal/settings/service.go`:

```go
type ScanSettings struct {
    RefreshIntervalHours int `json:"refresh_interval_hours"`
}

func (s *Service) GetScanSettings(ctx context.Context) (ScanSettings, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define persisted schedule contracts and recurrence math</name>
  <files>mibo-media-server/internal/database/models.go, mibo-media-server/internal/database/database.go, mibo-media-server/internal/schedule/frequency.go, mibo-media-server/internal/schedule/service_test.go</files>
  <behavior>
    - Test 1: creating a daily schedule computes the next valid `next_run_at` from a `HH:MM` template per D-04/D-05.
    - Test 2: weekly schedules require weekday, monthly schedules require day-of-month, and invalid scope/kind combinations are rejected.
    - Test 3: persisted schedule rows expose `enabled`, `next_run_at`, `latest_run_status`, and `latest_job_id` fields needed for D-13 through D-15.
  </behavior>
  <action>Create first-class `Schedule` and `ScheduleRun` models in `models.go` and add them to `database.Open` auto-migration. In `internal/schedule/frequency.go`, define the product-facing recurrence contract using typed templates (`daily`, `weekly`, `monthly`) instead of cron text per D-04, D-05, and D-06. The model must encode the locked maintenance kinds from D-09 and the locked scope model from D-07/D-08 (`global` and `library` only). Write tests first in `service_test.go` that prove next-run calculation, validation rules, and persisted latest-result fields.</action>
  <verify>
    <automated>go test ./internal/schedule -run 'Test.*(Frequency|Validate)'</automated>
  </verify>
  <done>`Schedule` / `ScheduleRun` tables exist, only the six locked maintenance kinds are accepted, only `global` / `library` scopes are accepted, and tests prove daily/weekly/monthly next-run calculation without cron exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement schedule service foundations for CRUD and history projection</name>
  <files>mibo-media-server/internal/schedule/service.go, mibo-media-server/internal/schedule/service_test.go</files>
  <behavior>
    - Test 1: create/update flows persist validated schedule definitions and refresh `next_run_at`.
    - Test 2: list/detail flows project latest-result snapshots and recent run history in schedule-centric shape per D-13/D-15.
    - Test 3: disabled schedules clear due execution eligibility without deleting history.
  </behavior>
  <action>Implement the core `schedule.Service` with validated create, list, get, update, and enable/disable behavior. Keep this plan focused on domain foundations only: no HTTP routes, no worker dispatch, no frontend types yet. The service must store schedule-centric latest-result snapshots and return recent `ScheduleRun` rows so later plans can satisfy D-14/D-15 without reconstructing history from global job lists. Preserve D-17 by reserving a compatibility seam for the legacy scan interval, but do not keep the old settings model as the primary product contract.</action>
  <verify>
    <automated>go test ./internal/schedule -run 'Test.*(Create|Update|History)'</automated>
  </verify>
  <done>`internal/schedule/service.go` exposes schedule CRUD/history foundations, computes `next_run_at`, tracks latest-result snapshot fields, and has regression coverage for create/update/disable/history behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin payload → schedule persistence | Untrusted schedule definitions cross into durable state. |
| schedule definition → due-run calculation | Bad recurrence math could trigger work at the wrong time or never trigger it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | T | `internal/schedule/service.go` | mitigate | Validate maintenance kind, scope kind, and frequency field combinations before persistence; reject cron text entirely per D-04/D-06. |
| T-10-01-02 | D | `internal/schedule/frequency.go` | mitigate | Add deterministic tests for daily/weekly/monthly next-run math so malformed rules cannot starve or flood execution. |
| T-10-01-03 | I | `ScheduleRun` snapshots | accept | Persist only operator-facing error summaries and status metadata; execution payloads stay in jobs/domain services, not in broad schedule list responses. |
</threat_model>

<verification>
- `go test ./internal/schedule -run 'Test.*(Frequency|Validate|Create|Update|History)'`
- Confirm `database.Open` migrates `Schedule` and `ScheduleRun` along with existing tables.
</verification>

<success_criteria>
- The phase has a durable schedule domain independent from raw job rows.
- Product-facing recurrence rules are daily/weekly/monthly templates, not cron strings.
- Later plans can build APIs, worker wiring, and UI against stable schedule/history contracts without redefining the model.
</success_criteria>

<output>
After completion, create `.planning/phases/10-scheduled-operations-control/10-01-SUMMARY.md`
</output>
