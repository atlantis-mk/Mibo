---
phase: 10-scheduled-operations-control
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - mibo-media-server/internal/library/schedule_jobs.go
  - mibo-media-server/internal/library/schedule_jobs_test.go
autonomous: true
requirements:
  - SJOB-01
  - SJOB-04
  - SJOB-05
must_haves:
  truths:
    - Scan, cleanup, and invalid-link schedules have real backend executors instead of placeholder labels.
    - Global and per-library scope behave consistently for library-owned maintenance.
    - Invalid-link checks surface actionable failures through the async maintenance path.
  artifacts:
    - path: mibo-media-server/internal/library/schedule_jobs.go
      provides: library-owned scheduled maintenance executors.
    - path: mibo-media-server/internal/library/schedule_jobs_test.go
      provides: regression proof for scan, cleanup, and invalid-link behaviors.
  key_links:
    - from: mibo-media-server/internal/library/schedule_jobs.go
      to: existing scan/cleanup helpers
      via: library-owned executor methods
      pattern: RunSyncLibrary|cleanupMissing|ResolveStorage
---

<objective>
Implement the library-owned maintenance executors that scheduled operations can trigger for scan, cleanup, and invalid-link checks.

Purpose: Phase 10 cannot honestly offer scan, cleanup, and invalid-link schedule types unless those types map to real reusable backend work.
Output: `internal/library` executors for scheduled scan, library cleanup, and invalid-link checks plus focused regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scheduled-operations-control/10-CONTEXT.md
@.planning/phases/10-scheduled-operations-control/10-RESEARCH.md

<interfaces>
From Plan 10-01 (`internal/schedule` contracts to create):

```go
type ScopeKind string
const (
    ScopeGlobal  ScopeKind = "global"
    ScopeLibrary ScopeKind = "library"
)

type DueSchedule struct {
    ID        uint
    Kind      string
    ScopeKind ScopeKind
    LibraryID *uint
}
```

From existing `internal/library` code:

```go
func (s *Service) ListActiveLibraries(ctx context.Context) ([]database.Library, error)
func (s *Service) RunSyncLibrary(ctx context.Context, job database.Job) error
func (s *Service) scanLibraryWithMode(ctx context.Context, provider storage.Provider, library database.Library, rootPath string, mode scanMode) (SyncResult, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scheduled scan and cleanup executors with scope fan-out</name>
  <files>mibo-media-server/internal/library/schedule_jobs.go, mibo-media-server/internal/library/schedule_jobs_test.go</files>
  <behavior>
    - Test 1: `scan` schedules run against either all active libraries or one selected library per D-07/D-08.
    - Test 2: `library_cleanup` schedules execute explicit cleanup/reconciliation behavior instead of becoming a no-op label.
    - Test 3: executor logic never escapes the selected library root.
  </behavior>
  <action>Create library-owned scheduled maintenance executors in a new `schedule_jobs.go` file. Reuse existing library scan and missing-record cleanup primitives instead of cloning scan logic. Keep the mapping explicit: `scan` and `library_cleanup` are distinct schedule kinds in the product model per D-09 even if they share lower-level traversal helpers. Support both `global` and `library` scope per D-07/D-08, and keep path/root validation inside library ownership boundaries rather than in the future scheduler or HTTP layer.</action>
  <verify>
    <automated>go test ./internal/library -run 'Test.*(ScheduledScan|Cleanup)'</automated>
  </verify>
  <done>Library-owned schedule executors exist for scan and cleanup, respect scope boundaries, and have focused regression coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement invalid-link checks as scheduled library maintenance</name>
  <files>mibo-media-server/internal/library/schedule_jobs.go, mibo-media-server/internal/library/schedule_jobs_test.go</files>
  <behavior>
    - Test 1: `invalid_link_check` walks the selected scope and reports broken link/storage resolution failures through a deterministic summary.
    - Test 2: successful checks do not mutate unrelated library metadata.
  </behavior>
  <action>Add an explicit invalid-link-check executor under `internal/library`, using provider/storage validation rather than client-side probing. Keep the output compatible with schedule-run latest-result/history snapshots so later plans can show failures from D-13 through D-15. Do not expose this as a synchronous HTTP action or invent a second async channel; it must be future worker-invoked maintenance work per D-10/D-11.</action>
  <verify>
    <automated>go test ./internal/library -run 'Test.*InvalidLink'</automated>
  </verify>
  <done>`invalid_link_check` is a real backend maintenance executor with automated proof, ready to be triggered by schedule runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedule scope → library traversal | Scheduled maintenance can touch many files/items if scoping is wrong. |
| storage/provider validation → run result summary | Broken links must surface as safe summaries, not uncontrolled errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | T | `internal/library/schedule_jobs.go` | mitigate | Resolve every scheduled scope through existing library/provider root helpers so scheduled work cannot escape the selected library. |
| T-10-02-02 | D | scan/cleanup executors | mitigate | Reuse existing scan and cleanup primitives instead of ad hoc traversal so scheduled cleanup cannot silently diverge from manual/library behavior. |
| T-10-02-03 | I | invalid-link summaries | mitigate | Return concise failure counts/messages for later schedule history projection; avoid raw provider secrets or giant payload dumps. |
</threat_model>

<verification>
- `go test ./internal/library -run 'Test.*(ScheduledScan|Cleanup|InvalidLink)'`
- Confirm no executor introduces item-level targeting or non-library scopes.
</verification>

<success_criteria>
- Schedules for scan, cleanup, and invalid-link checks map to real library-owned backend work.
- Global and per-library scope fan-out is proven by tests.
- These maintenance kinds are ready for schedule orchestration without additional domain design work.
</success_criteria>

<output>
After completion, create `.planning/phases/10-scheduled-operations-control/10-02-SUMMARY.md`
</output>
