---
phase: 18-frontend-catalog-item-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/mibo-api.ts
  - web/src/lib/mibo-query.ts
  - web/src/lib/media-presentation.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "The shared frontend data layer can call additive catalog item, season, progress, and playback routes without carrying legacy `media_item_id` or `media_file_id` contracts into new UI code."
    - "Shared presentation helpers expose catalog availability labels, selected-image lookup, and watched-state derivation so feature pages stop reimplementing `show` / `source_path` heuristics."
  artifacts:
    - path: web/src/lib/mibo-api.ts
      provides: catalog-native TypeScript DTOs plus endpoint helpers for item, season, progress, and playback routes
    - path: web/src/lib/mibo-query.ts
      provides: shared React Query keys and query options for catalog item detail, seasons, progress, and playback
    - path: web/src/lib/media-presentation.ts
      provides: catalog label, image, watched-state, and route/view helper functions reused by later feature waves
  key_links:
    - from: web/src/lib/mibo-api.ts
      to: mibo-media-server/internal/catalog/contracts.go
      via: additive TypeScript mirrors of catalog list/detail/season/episode/asset DTOs
      pattern: "CatalogListItem|CatalogItemDetail|CatalogSeasonDetail|CatalogEpisodeDetail|CatalogAssetDetail"
    - from: web/src/lib/mibo-query.ts
      to: web/src/lib/mibo-api.ts
      via: createAuthedMiboApi catalog methods and stable query keys
      pattern: "listCatalogItems|getCatalogItem|listCatalogSeriesSeasons|getCatalogItemProgress|getCatalogPlayback"
    - from: web/src/lib/media-presentation.ts
      to: web/src/lib/mibo-api.ts
      via: catalog DTO-aware formatting helpers
      pattern: "availability_status|selected_images|asset_id"
---

<objective>
Build the shared catalog-native frontend contract layer before any page-level UI cutover starts.

Purpose: give later browse/detail/playback plans stable frontend types, API helpers, query keys, and presentation helpers that point at the additive Phase 16/17 routes instead of legacy `MediaItem` / `MediaFile` contracts.
Output: additive catalog DTOs in `mibo-api.ts`, shared query options in `mibo-query.ts`, and catalog presentation helpers in `media-presentation.ts`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-frontend-catalog-item-migration/18-RESEARCH.md
@.planning/phases/18-frontend-catalog-item-migration/18-UI-SPEC.md
@.planning/phases/18-frontend-catalog-item-migration/18-PATTERNS.md
@.planning/phases/16-catalog-api-search-progress-cutover/16-03-PLAN.md
@.planning/phases/16-catalog-api-search-progress-cutover/16-04-PLAN.md
@.planning/phases/17-playback-item-to-asset-cutover/17-01-PLAN.md
@.planning/phases/17-playback-item-to-asset-cutover/17-02-PLAN.md

<interfaces>
Expected additive frontend route contracts:

```text
GET  /api/v1/items
GET  /api/v1/items/{id}
GET  /api/v1/series/{id}/seasons
GET  /api/v1/items/{id}/progress
POST /api/v1/me/item-progress
GET  /api/v1/items/{id}/playback?client_profile=web&asset_id={optional}
```

Required additive frontend types in `web/src/lib/mibo-api.ts`:

```ts
export type CatalogSelectedImage = { image_type: string; url: string }
export type CatalogAssetDetail = { id: number; asset_type: string; display_name?: string; status: string; probe_status: string }
export type CatalogListItem = { id: number; library_id: number; type: string; title: string; availability_status: string; selected_images: CatalogSelectedImage[] }
export type CatalogItemDetail = CatalogListItem & { seasons: CatalogSeasonDetail[]; episodes: CatalogEpisodeDetail[]; assets: CatalogAssetDetail[] }
export type CatalogProgressState = { user_id: number; item_id: number; asset_id?: number; position_seconds: number; duration_seconds?: number; watched: boolean }
export type CatalogPlaybackSource = { item_id: number; asset_id: number; inventory_file_id: number; playable: boolean; decision: PlaybackDecision }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive catalog DTOs and API helpers</name>
  <read_first>
    - web/src/lib/mibo-api.ts
    - mibo-media-server/internal/catalog/contracts.go
    - .planning/phases/16-catalog-api-search-progress-cutover/16-04-PLAN.md
    - .planning/phases/17-playback-item-to-asset-cutover/17-02-PLAN.md
  </read_first>
  <files>
    - web/src/lib/mibo-api.ts
  </files>
  <behavior>
    - Type 1: additive catalog DTOs mirror the backend item/detail/season/episode/asset contracts and keep selected images, external identities, availability, governance, and asset metadata explicit.
    - Type 2: catalog progress and playback contracts use `item_id`, optional `asset_id`, and `inventory_file_id` instead of legacy media-file identifiers.
    - Method 3: additive API helpers call `/api/v1/items`, `/api/v1/items/{id}`, `/api/v1/series/{id}/seasons`, `/api/v1/items/{id}/progress`, `/api/v1/me/item-progress`, and `/api/v1/items/{id}/playback` without removing the legacy helpers yet.
  </behavior>
  <action>Extend `web/src/lib/mibo-api.ts` with additive catalog types named exactly `CatalogSelectedImage`, `CatalogExternalIdentity`, `CatalogChildSummary`, `CatalogAssetLink`, `CatalogAssetDetail`, `CatalogListItem`, `CatalogSeasonDetail`, `CatalogEpisodeDetail`, `CatalogItemDetail`, `CatalogProgressState`, and `CatalogPlaybackSource`. Also add helper input types `CatalogItemsQuery` and `CatalogPlaybackOptions`. Add new API methods named exactly `listCatalogItems(queryOptions?: CatalogItemsQuery)`, `getCatalogItem(itemId: number)`, `listCatalogSeriesSeasons(seriesId: number)`, `getCatalogItemProgress(itemId: number)`, `updateCatalogProgress(input: { item_id: number; asset_id?: number; position_seconds: number; duration_seconds?: number; completed?: boolean })`, and `getCatalogPlayback(itemId: number, playbackOptions: { assetId?: number; clientProfile: ClientProfile })`. Keep the existing legacy `discoverMedia`, `getMediaItem`, `getPlayback`, and `updateProgress` methods untouched in this wave so later feature migrations can cut over incrementally.</action>
  <acceptance_criteria>
    - `web/src/lib/mibo-api.ts` contains `export type CatalogListItem = {`.
    - `web/src/lib/mibo-api.ts` contains `export type CatalogItemDetail = {`.
    - `web/src/lib/mibo-api.ts` contains `export type CatalogProgressState = {`.
    - `web/src/lib/mibo-api.ts` contains `export type CatalogPlaybackSource = {`.
    - `web/src/lib/mibo-api.ts` contains `listCatalogItems(`.
    - `web/src/lib/mibo-api.ts` contains `getCatalogItem(`.
    - `web/src/lib/mibo-api.ts` contains `listCatalogSeriesSeasons(`.
    - `web/src/lib/mibo-api.ts` contains `getCatalogItemProgress(`.
    - `web/src/lib/mibo-api.ts` contains `updateCatalogProgress(`.
    - `web/src/lib/mibo-api.ts` contains `getCatalogPlayback(`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>The frontend has additive catalog-native DTOs and route helpers ready for page-level migration work.</done>
</task>

<task type="auto">
  <name>Task 2: Add catalog query keys and presentation helpers</name>
  <read_first>
    - web/src/lib/mibo-query.ts
    - web/src/lib/media-presentation.ts
    - web/src/features/home/home-sections.tsx
    - web/src/features/media/index.tsx
  </read_first>
  <files>
    - web/src/lib/mibo-query.ts
    - web/src/lib/media-presentation.ts
  </files>
  <behavior>
    - Query 1: shared query keys are available for catalog item list/detail, series seasons, progress, and playback.
    - Helper 2: presentation helpers format catalog availability and type labels, choose selected images by image type, and derive watched-state labels from catalog progress.
    - Guard 3: new helpers do not read `source_path`, `series_title`, `media_item_id`, or `media_file_id` to determine catalog UI state.
  </behavior>
  <action>In `web/src/lib/mibo-query.ts`, add `miboQueryKeys` entries named exactly `catalogItems`, `catalogItemDetail`, `catalogSeriesSeasons`, `catalogItemProgress`, and `catalogPlayback`, then add query-option helpers named `catalogItemDetailQueryOptions`, `catalogSeriesSeasonsQueryOptions`, and `catalogItemProgressQueryOptions` that call the new catalog methods from Task 1. In `web/src/lib/media-presentation.ts`, preserve `parseMediaDetailView(...)` but add catalog-aware helpers named exactly `formatCatalogTypeLabel`, `formatCatalogAvailabilityLabel`, `getCatalogSelectedImage`, and `deriveCatalogWatchedState`. `getCatalogSelectedImage` must prefer one `selected_images` entry by `image_type`, and `deriveCatalogWatchedState` must map catalog progress to the existing `未看` / `观看中` / `已看` wording without using legacy ids or `source_path` heuristics.</action>
  <acceptance_criteria>
    - `web/src/lib/mibo-query.ts` contains `catalogItems:`.
    - `web/src/lib/mibo-query.ts` contains `catalogItemDetail:`.
    - `web/src/lib/mibo-query.ts` contains `catalogSeriesSeasons:`.
    - `web/src/lib/mibo-query.ts` contains `catalogItemProgress:`.
    - `web/src/lib/mibo-query.ts` contains `catalogPlayback:`.
    - `web/src/lib/mibo-query.ts` contains `catalogItemDetailQueryOptions(`.
    - `web/src/lib/mibo-query.ts` contains `catalogSeriesSeasonsQueryOptions(`.
    - `web/src/lib/mibo-query.ts` contains `catalogItemProgressQueryOptions(`.
    - `web/src/lib/media-presentation.ts` contains `formatCatalogTypeLabel(`.
    - `web/src/lib/media-presentation.ts` contains `formatCatalogAvailabilityLabel(`.
    - `web/src/lib/media-presentation.ts` contains `getCatalogSelectedImage(`.
    - `web/src/lib/media-presentation.ts` contains `deriveCatalogWatchedState(`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>Later feature waves can reuse stable query keys and catalog presenter helpers instead of rebuilding labels and selection logic locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend catalog DTOs -> frontend shared contract layer | backend JSON becomes the canonical source for browse/detail/playback state here |
| shared progress/playback helpers -> feature pages | item and asset identifiers become user-visible navigation and persistence inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | T | shared catalog contract layer | mitigate | keep catalog DTOs additive and explicit so later feature waves cannot silently fall back to legacy `MediaItem` / `MediaFile` fields |
| T-18-02 | I | presentation helpers | mitigate | derive labels, images, and watched state only from catalog DTO fields and progress payloads, not from legacy `source_path` or `series_title` guesses |
</threat_model>

<verification>
- `cd web && pnpm typecheck`
</verification>

<success_criteria>
- Additive catalog frontend types exist beside the legacy contracts.
- Shared query keys and query options exist for catalog detail, seasons, progress, and playback.
- Catalog presentation helpers are available for later browse/detail/playback migration waves.
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-catalog-item-migration/18-01-SUMMARY.md`
</output>
