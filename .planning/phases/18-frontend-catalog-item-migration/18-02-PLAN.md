---
phase: 18-frontend-catalog-item-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - web/src/lib/mibo-query.ts
  - web/src/features/discovery/controls.tsx
  - web/src/features/home/index.tsx
  - web/src/features/home/home-sections.tsx
  - web/src/features/library/index.tsx
  - web/src/features/search/index.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Home, library, and search surfaces render `CatalogListItem` browse units with explicit availability badges instead of legacy `MediaItem` cards."
    - "Browse pages stop assuming `show` plus `series_title` heuristics; catalog `series` item types and progress-derived watched state drive routing and badges."
  artifacts:
    - path: web/src/features/home/index.tsx
      provides: catalog-backed home query orchestration and empty/error states
    - path: web/src/features/home/home-sections.tsx
      provides: hero and rail presentation for catalog browse cards and availability/playability badges
    - path: web/src/features/library/index.tsx
      provides: library grid cut over to catalog item queries and badges
    - path: web/src/features/search/index.tsx
      provides: search results cut over to catalog item queries and catalog-aware route/view logic
    - path: web/src/features/discovery/controls.tsx
      provides: catalog-supported filter model for library and search pages
  key_links:
    - from: web/src/features/home/index.tsx
      to: web/src/lib/mibo-query.ts
      via: shared home query composition and catalog item loaders
      pattern: "homeDataQueryOptions|listCatalogItems"
    - from: web/src/features/library/index.tsx
      to: web/src/lib/media-presentation.ts
      via: catalog labels, watched-state badges, and selected-image helpers
      pattern: "formatCatalogTypeLabel|formatCatalogAvailabilityLabel|deriveCatalogWatchedState"
    - from: web/src/features/search/index.tsx
      to: web/src/features/discovery/controls.tsx
      via: catalog-supported filter shape and history normalization
      pattern: "availability|series"
---

<objective>
Move the browse surfaces to catalog-native items before touching detail and playback.

Purpose: satisfy UI-01 by cutting home, library, and search over to `CatalogListItem` data, catalog type names, explicit availability badges, and progress-derived watched-state badges.
Output: catalog-backed home/list/search loaders plus catalog-aware browse card rendering and filter controls.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-frontend-catalog-item-migration/18-RESEARCH.md
@.planning/phases/18-frontend-catalog-item-migration/18-UI-SPEC.md
@.planning/phases/18-frontend-catalog-item-migration/18-PATTERNS.md
@.planning/phases/18-frontend-catalog-item-migration/18-01-PLAN.md
@web/src/features/discovery/controls.tsx
@web/src/features/home/index.tsx
@web/src/features/home/home-sections.tsx
@web/src/features/library/index.tsx
@web/src/features/search/index.tsx

<interfaces>
Shared browse inputs after 18-01:

```ts
type CatalogItemsQuery = {
  library_id?: number
  parent_id?: number
  q?: string
  type?: string
  availability?: string
  sort?: string
  limit?: number
}

type CatalogListItem = {
  id: number
  library_id: number
  type: string
  title: string
  year?: number
  availability_status: string
  selected_images: CatalogSelectedImage[]
}
```

Current browse shells to preserve visually:

```tsx
<HeroCarousel heroItems={...} />
<LatestLibraryRail latestLibrarySections={...} />
<DiscoveryControls filters={filters} onChange={setFilters} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the home page to catalog browse queries</name>
  <read_first>
    - web/src/lib/mibo-query.ts
    - web/src/features/home/index.tsx
    - web/src/features/home/home-sections.tsx
    - web/src/lib/media-presentation.ts
    - .planning/phases/18-frontend-catalog-item-migration/18-UI-SPEC.md
  </read_first>
  <files>
    - web/src/lib/mibo-query.ts
    - web/src/features/home/index.tsx
    - web/src/features/home/home-sections.tsx
  </files>
  <behavior>
    - Home 1: hero items come from catalog browse results rather than `/api/v1/home/recently-added` legacy `MediaItem` payloads.
    - Home 2: per-library rails come from additive catalog item queries and keep the existing hero + rail layout.
    - Home 3: hero and rail cards always surface an availability badge and disable the primary play CTA when the item is not playable.
  </behavior>
  <action>Update `homeDataQueryOptions` in `web/src/lib/mibo-query.ts` so it builds home data from additive catalog routes: fetch hero items with `api.listCatalogItems({ sort: 'recent', limit: 6 })`, fetch libraries with `api.listLibraries()`, then fetch per-library rails with `Promise.all(libraries.map((library) => api.listCatalogItems({ library_id: library.id, sort: 'recent', limit: 12 })))`. Keep `continueWatching()` only as a count source until a dedicated catalog aggregate route exists, but do not use legacy `MediaItem` payloads for hero cards or rails anymore. In `home-sections.tsx`, switch card rendering from `poster_url` / `backdrop_url` fields to `getCatalogSelectedImage(item, 'poster')`, `getCatalogSelectedImage(item, 'backdrop')`, and `getCatalogSelectedImage(item, 'logo')`, render one availability badge via `formatCatalogAvailabilityLabel(item.availability_status)`, change the route-view check from `item.type === 'show'` to `item.type === 'series'`, and change the primary CTA so `availability_status === 'available'` shows `播放` while every other state shows a disabled `暂不可播放` button. Keep the current empty-home shell, but change its copy from legacy “轮播媒体内容” wording to catalog-neutral text.</action>
  <acceptance_criteria>
    - `web/src/lib/mibo-query.ts` contains `listCatalogItems({ sort: 'recent', limit: 6 })`.
    - `web/src/lib/mibo-query.ts` contains `library_id: library.id`.
    - `web/src/features/home/home-sections.tsx` contains `formatCatalogAvailabilityLabel(`.
    - `web/src/features/home/home-sections.tsx` contains `getCatalogSelectedImage(`.
    - `web/src/features/home/home-sections.tsx` contains `item.type === 'series'`.
    - `web/src/features/home/home-sections.tsx` contains `暂不可播放`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>The home hero and library rails are now catalog-native and expose availability/playability explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate library/search filters and browse cards to catalog items</name>
  <read_first>
    - web/src/features/discovery/controls.tsx
    - web/src/features/library/index.tsx
    - web/src/features/search/index.tsx
    - web/src/lib/mibo-api.ts
    - web/src/lib/media-presentation.ts
    - .planning/phases/18-frontend-catalog-item-migration/18-RESEARCH.md
  </read_first>
  <files>
    - web/src/features/discovery/controls.tsx
    - web/src/features/library/index.tsx
    - web/src/features/search/index.tsx
  </files>
  <behavior>
    - Filter 1: library and search pages use only catalog-supported filters and do not silently ship unsupported legacy discovery params.
    - Card 2: browse cards show catalog type, watched state, and availability badges, and missing/unaired items remain visible instead of being filtered away.
    - Route 3: series cards use catalog `series` type checks when deep-linking to `/media/:id?view=series`.
  </behavior>
  <action>Refactor `web/src/features/discovery/controls.tsx` so `DiscoveryFilters` becomes a catalog-supported shape: `q`, `type: 'all' | 'movie' | 'series'`, `availability: 'all' | 'available' | 'missing' | 'unaired' | 'no_local_media' | 'unavailable'`, and `sort: 'recent' | 'title' | 'year'`. Remove the `genre`, `region`, `year` input, `minRating`, and `watchedState` controls from the rendered form instead of passing unsupported params to the additive catalog route. In `web/src/features/library/index.tsx` and `web/src/features/search/index.tsx`, replace `discoverMedia(...)` with `listCatalogItems(...)`, thread only supported query params (`library_id`, `q`, `type`, `availability`, `sort`, `limit`), then fetch `getCatalogItemProgress(item.id)` for each returned card so you can render watched-state badges through `deriveCatalogWatchedState(...)` without relying on legacy discovery payload wrappers. Normalize saved search-history entries down to the supported catalog filters, update the type guard from `show` to `series`, and render one availability badge via `formatCatalogAvailabilityLabel(item.availability_status)` on every card. Do not filter out `missing`, `unaired`, or `no_local_media` items from the grid or search results.</action>
  <acceptance_criteria>
    - `web/src/features/discovery/controls.tsx` contains `availability:`.
    - `web/src/features/discovery/controls.tsx` contains `'all' | 'movie' | 'series'`.
    - `web/src/features/library/index.tsx` contains `listCatalogItems(`.
    - `web/src/features/library/index.tsx` contains `getCatalogItemProgress(`.
    - `web/src/features/library/index.tsx` contains `formatCatalogAvailabilityLabel(`.
    - `web/src/features/library/index.tsx` contains `item.type === 'series'`.
    - `web/src/features/search/index.tsx` contains `listCatalogItems(`.
    - `web/src/features/search/index.tsx` contains `getCatalogItemProgress(`.
    - `web/src/features/search/index.tsx` contains `formatCatalogAvailabilityLabel(`.
    - `web/src/features/search/index.tsx` contains `item.type === 'series'`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>Library and search now browse catalog items directly, with catalog-supported filters and explicit availability state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog list results -> browse UI | top-level catalog items become user-visible hero cards, rails, and grids here |
| filter controls -> catalog item route | user-controlled filter state maps directly to additive catalog query params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | T | filter mapping | mitigate | narrow `DiscoveryControls` to the additive catalog route's supported params so the UI does not silently depend on removed legacy discovery fields |
| T-18-04 | I | browse-card rendering | mitigate | show explicit availability badges on every card and keep missing/unaired items visible instead of collapsing them into one generic or hidden state |
</threat_model>

<verification>
- `cd web && pnpm typecheck`
</verification>

<success_criteria>
- Home hero and rails render `CatalogListItem` data.
- Library and search call the additive catalog routes instead of legacy discovery payloads.
- Browse cards show explicit availability state and series routing uses catalog `series` semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-catalog-item-migration/18-02-SUMMARY.md`
</output>
