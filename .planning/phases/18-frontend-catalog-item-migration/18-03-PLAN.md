---
phase: 18-frontend-catalog-item-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - web/src/features/media/index.tsx
  - web/src/features/media/components/standalone-media-detail.tsx
  - web/src/features/media/components/standalone-media-detail-hero.tsx
  - web/src/features/media/components/standalone-media-detail-sections.tsx
  - web/src/features/media/components/standalone-media-detail-specs.tsx
  - web/src/features/media/components/standalone-media-detail-utils.ts
autonomous: true
requirements:
  - UI-02
  - UI-03
must_haves:
  truths:
    - "The detail page reads catalog item, season, episode, asset, and progress data directly instead of adapting legacy `MediaItemDetail` and TMDB/local fallback episode data."
    - "Series hierarchy visibly preserves `available`, `missing`, `unaired`, and `no_local_media` states from catalog DTOs rather than collapsing them into a generic unavailable card."
  artifacts:
    - path: web/src/features/media/index.tsx
      provides: catalog detail/progress/season query orchestration plus mutation invalidation and playback navigation wiring
    - path: web/src/features/media/components/standalone-media-detail-hero.tsx
      provides: catalog-native hero actions, progress block, availability badge, and playability messaging
    - path: web/src/features/media/components/standalone-media-detail-sections.tsx
      provides: season/episode hierarchy rails plus asset/version cards and availability state rendering
    - path: web/src/features/media/components/standalone-media-detail-specs.tsx
      provides: catalog fields and selected-asset summary in the existing specs-panel shell
  key_links:
    - from: web/src/features/media/index.tsx
      to: web/src/lib/mibo-query.ts
      via: catalog detail, season, and progress query options
      pattern: "catalogItemDetailQueryOptions|catalogSeriesSeasonsQueryOptions|catalogItemProgressQueryOptions"
    - from: web/src/features/media/components/standalone-media-detail-hero.tsx
      to: web/src/lib/media-presentation.ts
      via: catalog availability/type labels and selected-image helpers
      pattern: "formatCatalogAvailabilityLabel|getCatalogSelectedImage"
    - from: web/src/features/media/components/standalone-media-detail-sections.tsx
      to: web/src/lib/mibo-api.ts
      via: catalog season, episode, and asset DTO shapes
      pattern: "CatalogSeasonDetail|CatalogEpisodeDetail|CatalogAssetDetail"
---

<objective>
Migrate the detail and series surfaces to catalog-native hierarchy and asset data.

Purpose: satisfy UI-02 and UI-03 by replacing legacy detail adapters, TMDB fallback seasons, and primary-file assumptions with catalog detail, progress, seasons, episodes, and assets.
Output: catalog-backed detail loader, catalog-native detail components, and explicit hierarchy/availability rendering.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-frontend-catalog-item-migration/18-RESEARCH.md
@.planning/phases/18-frontend-catalog-item-migration/18-UI-SPEC.md
@.planning/phases/18-frontend-catalog-item-migration/18-PATTERNS.md
@.planning/phases/18-frontend-catalog-item-migration/18-01-PLAN.md
@web/src/features/media/index.tsx
@web/src/features/media/components/standalone-media-detail.tsx
@web/src/features/media/components/standalone-media-detail-hero.tsx
@web/src/features/media/components/standalone-media-detail-sections.tsx
@web/src/features/media/components/standalone-media-detail-specs.tsx

<interfaces>
Catalog detail DTOs to consume:

```ts
type CatalogItemDetail = {
  id: number
  type: string
  title: string
  overview?: string
  availability_status: string
  selected_images: CatalogSelectedImage[]
  child_summary?: CatalogChildSummary
  seasons: CatalogSeasonDetail[]
  episodes: CatalogEpisodeDetail[]
  assets: CatalogAssetDetail[]
}

type CatalogSeasonDetail = {
  id: number
  title: string
  index_number?: number
  availability_status: string
  child_summary?: CatalogChildSummary
  episodes: CatalogEpisodeDetail[]
}
```

Existing detail entrypoint to keep thin:

```tsx
<StandaloneMediaDetail
  item={...}
  progress={...}
  seriesSeasons={...}
  onOpenPlaybackEntry={...}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint media detail loading to catalog detail, progress, and seasons</name>
  <read_first>
    - web/src/features/media/index.tsx
    - web/src/lib/mibo-query.ts
    - web/src/lib/media-presentation.ts
    - .planning/phases/18-frontend-catalog-item-migration/18-RESEARCH.md
    - .planning/phases/18-frontend-catalog-item-migration/18-UI-SPEC.md
  </read_first>
  <files>
    - web/src/features/media/index.tsx
  </files>
  <behavior>
    - Query 1: detail page loads one catalog item detail, one catalog progress state, and one catalog season list, with no TMDB/local fallback episode requests.
    - Mutation 2: detail-page invalidation continues to use shared React Query keys after catalog progress or metadata actions.
    - Route 3: the detail loader uses catalog `series` semantics when deciding whether the route should render a hierarchy view.
  </behavior>
  <action>Rewrite `web/src/features/media/index.tsx` to use the 18-01 shared helpers: replace `mediaItemDetailQueryOptions` with `catalogItemDetailQueryOptions`, replace `mediaItemProgressQueryOptions` with `catalogItemProgressQueryOptions`, and replace `tvSeriesEpisodesQueryOptions` with `catalogSeriesSeasonsQueryOptions`. Remove the `buildPresentedMediaItem(...)` main-path adaptation and stop calling `listTVSeasons`, `listTVSeasonEpisodes`, and `listLocalSeriesEpisodes`; the only hierarchy source should be `/api/v1/series/{id}/seasons`. Use `detailItem.type === 'series'` to decide whether the page is in series mode. Keep metadata-management navigation where it already exists, but remove or disable any reprobe/rematch UI action that still requires legacy `media_file_id` semantics instead of back-converting catalog items to legacy ids. After mutations, invalidate the catalog detail/progress query keys plus home/list query keys through the same `queryClient.invalidateQueries(...)` pattern already used in this file.</action>
  <acceptance_criteria>
    - `web/src/features/media/index.tsx` contains `catalogItemDetailQueryOptions(`.
    - `web/src/features/media/index.tsx` contains `catalogItemProgressQueryOptions(`.
    - `web/src/features/media/index.tsx` contains `catalogSeriesSeasonsQueryOptions(`.
    - `web/src/features/media/index.tsx` contains `detailItem?.type === 'series'` or `item.type === 'series'`.
    - `web/src/features/media/index.tsx` contains `invalidateQueries`.
    - `web/src/features/media/index.tsx` does not contain `listLocalSeriesEpisodes(`.
    - `web/src/features/media/index.tsx` does not contain `listTVSeasonEpisodes(`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>The detail entry feature now loads catalog detail, progress, and hierarchy data directly.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild detail components around catalog item, asset, and hierarchy DTOs</name>
  <read_first>
    - web/src/features/media/components/standalone-media-detail.tsx
    - web/src/features/media/components/standalone-media-detail-hero.tsx
    - web/src/features/media/components/standalone-media-detail-sections.tsx
    - web/src/features/media/components/standalone-media-detail-specs.tsx
    - web/src/features/media/components/standalone-media-detail-utils.ts
    - web/src/lib/mibo-api.ts
    - web/src/lib/media-presentation.ts
  </read_first>
  <files>
    - web/src/features/media/components/standalone-media-detail.tsx
    - web/src/features/media/components/standalone-media-detail-hero.tsx
    - web/src/features/media/components/standalone-media-detail-sections.tsx
    - web/src/features/media/components/standalone-media-detail-specs.tsx
    - web/src/features/media/components/standalone-media-detail-utils.ts
  </files>
  <behavior>
    - Hero 1: detail hero uses catalog selected images, availability state, asset inventory, and progress when deciding its primary/secondary actions.
    - Hierarchy 2: season and episode rails render catalog hierarchy directly and keep `missing`, `unaired`, and `no_local_media` items visible with explicit badges.
    - Specs 3: specs panel preserves the existing shell but prefers catalog fields and selected-asset summary over legacy primary-file details.
  </behavior>
  <action>Update the detail component prop types from `MediaItemDetail`, `ProgressState`, and `TVSeasonWithEpisodes` to the additive catalog types from 18-01. In `standalone-media-detail.tsx` and `standalone-media-detail-hero.tsx`, drive the background/logo/poster from `getCatalogSelectedImage(...)`, add an availability badge through `formatCatalogAvailabilityLabel(...)`, and compute the primary CTA exactly as the UI spec requires: one playable asset -> `播放`, resumable progress -> `继续播放`, multiple playable assets -> `选择版本`, no playable assets -> disabled `暂不可播放`. In `standalone-media-detail-sections.tsx`, replace the old season/episode card assumptions with `CatalogSeasonDetail` and `CatalogEpisodeDetail`, render availability badges on each season header and episode card, keep `missing`, `unaired`, and `no_local_media` rows visible, and add an asset/version section where each asset card shows `display_name`, `edition`, `quality_label`, `duration_seconds`, `status`, `probe_status`, and a `播放此版本` button. In `standalone-media-detail-specs.tsx`, keep the same section structure but replace primary-file codec/bitrate/source-path assumptions with catalog fields plus the selected/default asset summary. Keep cast/trailer areas conditional only if future catalog DTOs expose those fields; do not recover them from raw source-evidence blobs or legacy API calls.</action>
  <acceptance_criteria>
    - `web/src/features/media/components/standalone-media-detail.tsx` contains `CatalogItemDetail`.
    - `web/src/features/media/components/standalone-media-detail.tsx` contains `CatalogSeasonDetail`.
    - `web/src/features/media/components/standalone-media-detail-hero.tsx` contains `formatCatalogAvailabilityLabel(`.
    - `web/src/features/media/components/standalone-media-detail-hero.tsx` contains `选择版本`.
    - `web/src/features/media/components/standalone-media-detail-hero.tsx` contains `暂不可播放`.
    - `web/src/features/media/components/standalone-media-detail-sections.tsx` contains `播放此版本`.
    - `web/src/features/media/components/standalone-media-detail-sections.tsx` contains `availability_status`.
    - `web/src/features/media/components/standalone-media-detail-specs.tsx` contains `CatalogItemDetail`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>The standalone detail experience now presents catalog-native images, assets, seasons, and episode availability states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog detail DTOs -> standalone detail components | item, season, episode, and asset hierarchy become user-visible state here |
| progress and playability state -> hero CTA decisions | catalog progress and asset status control the page's primary actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | I | detail/hierarchy rendering | mitigate | render availability badges directly from catalog DTOs so `missing`, `unaired`, and `no_local_media` states are never collapsed or hidden |
| T-18-06 | T | detail fallback logic | mitigate | remove TMDB/local fallback episode loading and legacy `buildPresentedMediaItem(...)` heuristics so the page cannot drift away from catalog hierarchy truth |
</threat_model>

<verification>
- `cd web && pnpm typecheck`
</verification>

<success_criteria>
- Detail page uses catalog detail, progress, seasons, episodes, and assets.
- Series hierarchy comes only from catalog DTOs.
- Asset/version state and explicit availability are visible in the existing detail shell.
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-catalog-item-migration/18-03-SUMMARY.md`
</output>
