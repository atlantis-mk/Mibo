---
phase: 18-frontend-catalog-item-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 18-01
  - 18-03
files_modified:
  - web/src/routes/play.$id.tsx
  - web/src/features/media/index.tsx
  - web/src/features/play/index.tsx
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Playback requests and progress writes use catalog `item_id` plus optional `asset_id`, never legacy file identifiers."
    - "The player exposes selected-version context and explainable unplayable outcomes while preserving the current theater-style playback shell."
  artifacts:
    - path: web/src/routes/play.$id.tsx
      provides: asset-aware route search parsing while keeping `/play/$id` stable
    - path: web/src/features/media/index.tsx
      provides: detail-page playback navigation with optional `assetId`
    - path: web/src/features/play/index.tsx
      provides: catalog playback loading, asset-aware progress persistence, and unplayable-state rendering
  key_links:
    - from: web/src/features/play/index.tsx
      to: web/src/lib/mibo-api.ts
      via: `getCatalogPlayback` and `updateCatalogProgress`
      pattern: "getCatalogPlayback|updateCatalogProgress"
    - from: web/src/features/play/index.tsx
      to: web/src/lib/mibo-query.ts
      via: catalog playback and progress query keys
      pattern: "catalogPlayback|catalogItemProgress"
    - from: web/src/routes/play.$id.tsx
      to: web/src/features/play/index.tsx
      via: `fromStart` plus `assetId` search params
      pattern: "assetId|fromStart"
---

<objective>
Finish the frontend cutover by moving playback entry and progress persistence onto catalog item and asset identifiers.

Purpose: satisfy UI-04 by making detail-page play actions asset-aware, making the playback route accept an optional asset selection, and persisting progress against `item_id` + `asset_id`.
Output: asset-aware play route parsing, detail-page playback navigation, and catalog-native playback/progress handling in the player.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-frontend-catalog-item-migration/18-RESEARCH.md
@.planning/phases/18-frontend-catalog-item-migration/18-UI-SPEC.md
@.planning/phases/18-frontend-catalog-item-migration/18-PATTERNS.md
@.planning/phases/18-frontend-catalog-item-migration/18-01-PLAN.md
@.planning/phases/18-frontend-catalog-item-migration/18-03-PLAN.md
@.planning/phases/17-playback-item-to-asset-cutover/17-02-PLAN.md
@.planning/phases/17-playback-item-to-asset-cutover/17-03-PLAN.md
@web/src/routes/play.$id.tsx
@web/src/features/play/index.tsx

<interfaces>
Catalog playback route contract:

```ts
type CatalogPlaybackSource = {
  item_id: number
  asset_id: number
  inventory_file_id: number
  display_name?: string
  quality_label?: string
  playable: boolean
  decision: PlaybackDecision
}

type CatalogProgressState = {
  item_id: number
  asset_id?: number
  position_seconds: number
  duration_seconds?: number
  watched: boolean
}
```

Player route search after this wave:

```ts
validateSearch: (search) => ({
  fromStart: boolean,
  assetId: number | undefined,
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add asset-aware playback route parsing and detail-page navigation</name>
  <read_first>
    - web/src/routes/play.$id.tsx
    - web/src/features/media/index.tsx
    - web/src/features/media/components/standalone-media-detail-hero.tsx
    - web/src/features/media/components/standalone-media-detail-sections.tsx
    - .planning/phases/18-frontend-catalog-item-migration/18-UI-SPEC.md
  </read_first>
  <files>
    - web/src/routes/play.$id.tsx
    - web/src/features/media/index.tsx
  </files>
  <behavior>
    - Route 1: `/play/$id` accepts optional `assetId` without changing the path contract.
    - Detail 2: detail-page play actions can navigate with `{ fromStart, assetId }` so default play, resume, and explicit asset-version selection all use one route.
    - Guard 3: series/detail routing stays item-based; only asset choice changes.
  </behavior>
  <action>Extend `web/src/routes/play.$id.tsx` so `validateSearch(...)` parses both `fromStart` and optional `assetId` from string/number input, and pass both props into `PlayExperience`. In `web/src/features/media/index.tsx`, thread optional `assetId` through `onOpenPlaybackEntry`, and when the detail hero or asset cards request playback, navigate with `search: { fromStart: Boolean(options?.fromStart), assetId: options?.assetId }`. Keep the route path `/play/$id` exactly as-is; only the search params change. Do not invent a second playback route or convert the route id away from the catalog item id.</action>
  <acceptance_criteria>
    - `web/src/routes/play.$id.tsx` contains `assetId`.
    - `web/src/routes/play.$id.tsx` contains `PlayExperience` with `assetId`.
    - `web/src/features/media/index.tsx` contains `assetId:`.
    - `web/src/features/media/index.tsx` contains `to: '/play/$id'`.
    - `web/src/features/media/index.tsx` contains `fromStart: Boolean(options?.fromStart)`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>The play route and detail-page navigation now support explicit asset selection without changing the user-visible route path.</done>
</task>

<task type="auto">
  <name>Task 2: Move the player to catalog playback and asset-aware progress</name>
  <read_first>
    - web/src/features/play/index.tsx
    - web/src/lib/mibo-query.ts
    - web/src/lib/mibo-api.ts
    - .planning/phases/17-playback-item-to-asset-cutover/17-02-PLAN.md
    - .planning/phases/17-playback-item-to-asset-cutover/17-03-PLAN.md
  </read_first>
  <files>
    - web/src/features/play/index.tsx
  </files>
  <behavior>
    - Playback 1: player requests catalog playback with `item_id` plus optional `asset_id`, and the query key varies by item + asset choice.
    - Progress 2: player persists and restores progress against the selected/default asset using catalog progress routes.
    - Unplayable 3: player presents a full-page explainable unplayable state instead of attempting to mount the video element when the playback decision says the asset cannot play.
  </behavior>
  <action>Rewrite `web/src/features/play/index.tsx` to use the catalog contracts from 18-01. Replace the custom `['media', 'playback', ...]` query key with `miboQueryKeys.catalogPlayback(queryToken, mediaItemId, assetId ?? 0)` and call `createAuthedMiboApi(token).getCatalogPlayback(mediaItemId, { assetId, clientProfile: 'web' })`. Replace all progress reads/writes with `getCatalogItemProgress(mediaItemId)` and `updateCatalogProgress({ item_id: item.id, asset_id: playback.asset_id, position_seconds, duration_seconds, completed })`. Restore progress only when the saved progress belongs to the currently selected/default asset, and invalidate/reset the catalog progress query key rather than the legacy media-item key. In the player chrome, add a small badge row near the title that shows the selected version label (`display_name` or `quality_label`) plus playability/availability summary, and add a `切换版本` action that returns to `/media/$id`. If `playback.playable` is false or `playback.decision.kind === 'unplayable'`, render the black theater-style error shell with the item title, selected asset label when present, the first decision-reason message, and a CTA back to detail; do not mount `<video>` in that state.</action>
  <acceptance_criteria>
    - `web/src/features/play/index.tsx` contains `getCatalogPlayback(`.
    - `web/src/features/play/index.tsx` contains `catalogPlayback(`.
    - `web/src/features/play/index.tsx` contains `updateCatalogProgress({`.
    - `web/src/features/play/index.tsx` contains `asset_id: playback.asset_id`.
    - `web/src/features/play/index.tsx` contains `切换版本`.
    - `web/src/features/play/index.tsx` contains `decision.kind === 'unplayable'`.
    - `web/src/features/play/index.tsx` contains `inventory_file_id`.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>The player now consumes catalog playback/progress contracts and exposes selected-version context plus explainable unplayable outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| play-route search params -> player loader | untrusted route state chooses whether playback requests a specific asset |
| playback decision payload -> video mount | backend playability decisions determine whether the player should stream or show an error shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | E | asset-aware playback route | mitigate | keep playback item-scoped and pass optional `assetId` only to the catalog playback endpoint, which already validates asset-to-item ownership |
| T-18-08 | D | unplayable player state | mitigate | branch on `playable` / `decision.kind === 'unplayable'` before mounting `<video>` so missing files or invalid assets degrade into a stable error UI instead of repeated player failures |
</threat_model>

<verification>
- `cd web && pnpm typecheck && pnpm build`
</verification>

<success_criteria>
- `/play/$id` accepts optional asset selection.
- Playback and progress use catalog item/asset identifiers end to end.
- Unplayable outcomes are explained in the player shell instead of surfacing as opaque playback failures.
</success_criteria>

<output>
After completion, create `.planning/phases/18-frontend-catalog-item-migration/18-04-SUMMARY.md`
</output>
