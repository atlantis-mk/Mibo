---
phase: 20-legacy-model-retirement-production-hardening
plan: 05
type: execute
wave: 3
depends_on:
  - 20-02
  - 20-03
files_modified:
  - mibo-media-server/internal/playback/service.go
  - mibo-media-server/internal/httpapi/handlers_playback.go
  - mibo-media-server/internal/httpapi/router.go
  - mibo-media-server/internal/httpapi/catalog_runtime_router_test.go
autonomous: true
requirements:
  - PROD-03
  - MIGR-04
must_haves:
  truths:
    - "Normal runtime playback and transport routes are catalog/inventory-only."
    - "Legacy media-item and media-file playback endpoints are no longer part of the normal runtime surface."
    - "Read-only migration status and reports still remain available to operators during cleanup."
  artifacts:
    - path: mibo-media-server/internal/httpapi/router.go
      provides: catalog/inventory-only runtime route registration
    - path: mibo-media-server/internal/playback/service.go
      provides: playback logic with no legacy main-path `MediaItem` / `MediaFile` dependence
    - path: mibo-media-server/internal/httpapi/catalog_runtime_router_test.go
      provides: regression coverage for removed legacy runtime routes and preserved migration visibility
  key_links:
    - from: mibo-media-server/internal/httpapi/router.go
      to: mibo-media-server/internal/httpapi/handlers_playback.go
      via: catalog item / asset / inventory-file routes
      pattern: "/api/v1/items/|/api/v1/assets/|/api/v1/inventory-files/"
    - from: mibo-media-server/internal/httpapi/router.go
      to: mibo-media-server/internal/httpapi/handlers_catalog_migration.go
      via: preserved read-only migration visibility during cleanup
      pattern: "/api/v1/catalog-migration/"
---

<objective>
Finish backend legacy retirement by deleting the remaining playback/file runtime route dependence on legacy media models while keeping migration visibility intact.

Purpose: make the HTTP/runtime surface match the catalog-kernel architecture before frontend and operational cleanup finalize the phase.
Output: catalog/inventory-only playback runtime, removed legacy media playback routes, and compatibility-boundary regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-RESEARCH.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-VALIDATION.md
@.planning/phases/17-playback-item-to-asset-cutover/17-03-PLAN.md

<interfaces>
Expected playback/runtime surface after Phase 17:

```go
GET /api/v1/items/{id}/playback
GET /api/v1/assets/{id}/link
GET /api/v1/inventory-files/{id}/stream
GET /api/v1/inventory-files/{id}/hls/index.m3u8
GET /api/v1/inventory-files/{id}/hls/{name}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression coverage for catalog-only runtime routes</name>
  <read_first>
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/handlers_playback.go
    - mibo-media-server/internal/playback/service.go
    - .planning/phases/17-playback-item-to-asset-cutover/17-03-PLAN.md
  </read_first>
  <files>
    - mibo-media-server/internal/httpapi/catalog_runtime_router_test.go
  </files>
  <behavior>
    - Test 1: catalog playback and inventory-file transport routes remain available.
    - Test 2: legacy `/api/v1/media-items/{id}/playback` and `/api/v1/media-files/{id}/*` routes are absent or explicitly non-runtime after the cutover.
    - Test 3: `GET /api/v1/catalog-migration/runs` and `GET /api/v1/settings/catalog-migration` remain readable for operators.
  </behavior>
  <action>Create `catalog_runtime_router_test.go` with focused route-registration and response-behavior tests. Assert `GET /api/v1/items/{id}/playback`, `GET /api/v1/assets/{id}/link`, and `GET /api/v1/inventory-files/{id}/stream` resolve through the router, while requests to legacy playback/file paths return `404` (or a structured deprecation status if you keep a tiny compatibility handler). Also add a migration-visibility assertion that authenticated `GET /api/v1/catalog-migration/runs` and `GET /api/v1/settings/catalog-migration` still return `200` after the runtime cleanup.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/catalog_runtime_router_test.go` contains `TestCatalogRuntimeUsesCatalogPlaybackRoutes`.
    - `mibo-media-server/internal/httpapi/catalog_runtime_router_test.go` contains `TestCatalogRuntimeLegacyPlaybackRoutesAreGone`.
    - `mibo-media-server/internal/httpapi/catalog_runtime_router_test.go` contains `TestCatalogRuntimeKeepsMigrationVisibility`.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/httpapi ./internal/playback -run 'TestCatalogRuntime' -count=1</automated>
  </verify>
  <done>Failing router tests now define the exact normal-runtime vs compatibility boundary for playback-era legacy retirement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove legacy playback runtime registrations and lookups</name>
  <read_first>
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/handlers_playback.go
    - mibo-media-server/internal/playback/service.go
    - mibo-media-server/internal/httpapi/catalog_runtime_router_test.go
    - .planning/phases/17-playback-item-to-asset-cutover/17-03-PLAN.md
  </read_first>
  <files>
    - mibo-media-server/internal/playback/service.go
    - mibo-media-server/internal/httpapi/handlers_playback.go
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/catalog_runtime_router_test.go
  </files>
  <behavior>
    - Test 1: normal runtime route registration only exposes item/asset/inventory-file playback endpoints.
    - Test 2: playback service has no remaining legacy main-path `MediaItem` / `MediaFile` lookup for catalog runtime behavior.
    - Test 3: migration status and run reports remain visible to operators until cleanup is explicitly complete.
  </behavior>
  <action>Update `router.go` so the normal runtime surface keeps only the catalog/inventory playback routes introduced by Phase 17 and removes the legacy `/api/v1/media-items/{id}/playback` and `/api/v1/media-files/{id}/*` registrations. In `handlers_playback.go` and `playback/service.go`, remove any remaining normal-path lookup or helper that still expects legacy `database.MediaItem` / `database.MediaFile` playback identifiers; the runtime path must resolve through catalog item, asset, and inventory-file identifiers only. Do not remove or hide `GET /api/v1/catalog-migration/runs`, `GET /api/v1/catalog-migration/runs/{id}`, or `GET /api/v1/settings/catalog-migration` — those remain the operator's read-only migration boundary during cleanup.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/items/{id}/playback`.
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/assets/{id}/link`.
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/inventory-files/{id}/stream`.
    - `mibo-media-server/internal/httpapi/router.go` does not contain `GET /api/v1/media-items/{id}/playback`.
    - `mibo-media-server/internal/httpapi/router.go` does not contain `GET /api/v1/media-files/{id}/stream`.
    - `mibo-media-server/internal/playback/service.go` does not contain `loadMediaItemFiles` in the normal runtime path.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/httpapi ./internal/playback -run 'TestCatalogRuntime' -count=1</automated>
  </verify>
  <done>Playback runtime is now catalog/inventory-only, while migration visibility stays available for the final cleanup window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP runtime routes -> playback handlers | lingering legacy routes could keep unreviewed code paths alive after cutover |
| operator migration reads -> cleanup boundary | compatibility data must remain readable without reviving legacy main-path behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | E | lingering legacy playback routes | mitigate | remove legacy media-item/file route registrations from the normal runtime router and lock the absence in focused router tests |
| T-20-10 | T | migration compatibility boundary | mitigate | preserve only read-only migration/report routes under `/api/v1/catalog-migration/*` and `GET /api/v1/settings/catalog-migration` while deleting runtime playback dependencies |
</threat_model>

<verification>
- `cd mibo-media-server && go test ./internal/httpapi ./internal/playback -run 'TestCatalogRuntime' -count=1`
</verification>

<success_criteria>
- Playback and transport runtime routes are catalog/inventory-only.
- Legacy playback/file endpoints are gone from the normal runtime surface.
- Operators still have read-only migration visibility while cleanup remains incomplete.
</success_criteria>

<output>
After completion, create `.planning/phases/20-legacy-model-retirement-production-hardening/20-05-SUMMARY.md`
</output>
