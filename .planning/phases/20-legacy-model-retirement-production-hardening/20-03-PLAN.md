---
phase: 20-legacy-model-retirement-production-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - mibo-media-server/internal/database/database.go
  - mibo-media-server/internal/database/database_open_test.go
  - mibo-media-server/internal/database/database_constraints_test.go
autonomous: true
requirements:
  - PROD-04
must_haves:
  truths:
    - "Empty databases, migrated legacy databases, and repeated startups all succeed with catalog-kernel hardening enabled."
    - "If duplicate rows would violate catalog-kernel unique guarantees, startup fails safely with an actionable error instead of partially hardening the schema."
  artifacts:
    - path: mibo-media-server/internal/database/database.go
      provides: explicit catalog-kernel constraint/index backstops with duplicate preflight checks
    - path: mibo-media-server/internal/database/database_open_test.go
      provides: fresh/legacy/repeated-open startup regression coverage
    - path: mibo-media-server/internal/database/database_constraints_test.go
      provides: duplicate-precheck failure coverage for hardened unique keys
  key_links:
    - from: mibo-media-server/internal/database/database.go
      to: mibo-media-server/internal/database/catalog_models.go
      via: explicit unique/index backstops for catalog-owned tables
      pattern: "idx_catalog_external_identity|idx_metadata_field_state_item_field|idx_user_item_data_user_item_asset"
    - from: mibo-media-server/internal/database/database.go
      to: mibo-media-server/internal/database/inventory_models.go
      via: explicit unique/index backstops for inventory-owned tables
      pattern: "idx_inventory_file_storage_path|idx_asset_items_asset_item_role_segment"
---

<objective>
Finish the startup hardening layer so catalog-kernel uniqueness and lookup guarantees are applied safely in production.

Purpose: make schema hardening trustworthy on both empty and migrated databases before normal runtime drops the remaining legacy paths.
Output: explicit index/constraint backstops plus regression tests for safe startup and actionable duplicate detection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-RESEARCH.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-VALIDATION.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-04-SUMMARY.md

<interfaces>
From `mibo-media-server/internal/database/database.go`:

```go
func Open(cfg config.DatabaseConfig) (*gorm.DB, error)
```

Existing explicit backstop names already in use:

```go
idx_catalog_items_library_type_availability_sort
idx_catalog_items_parent_order
idx_catalog_items_root_type_order
idx_catalog_search_documents_library_type_availability_title
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing startup regression coverage for duplicate-safe hardening</name>
  <read_first>
    - mibo-media-server/internal/database/database.go
    - mibo-media-server/internal/database/database_open_test.go
    - mibo-media-server/internal/database/catalog_models.go
    - mibo-media-server/internal/database/inventory_models.go
  </read_first>
  <files>
    - mibo-media-server/internal/database/database_open_test.go
    - mibo-media-server/internal/database/database_constraints_test.go
  </files>
  <behavior>
    - Test 1: fresh DB, migrated legacy DB, and repeated open still succeed after adding the new hardening backstops.
    - Test 2: if duplicate external identities or duplicate inventory file paths already exist, startup fails with an actionable `catalog kernel constraint precheck failed` error instead of silently continuing.
    - Test 3: the failure path happens before index creation mutates the database schema.
  </behavior>
  <action>Extend `database_open_test.go` and add `database_constraints_test.go` with failing coverage for the remaining hardened uniqueness guarantees. Seed one sqlite database with duplicate `catalog_external_ids(provider, provider_type, external_id)` rows and another with duplicate `inventory_files(storage_provider, storage_path)` rows, then assert `database.Open(...)` fails with a message that names the offending index family. Keep the existing fresh/legacy/repeated-open success cases in the same red test cycle so the new hardening cannot regress startup compatibility while adding failure coverage.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/database/database_constraints_test.go` contains `TestDatabaseOpenFailsOnDuplicateCatalogExternalIdentity`.
    - `mibo-media-server/internal/database/database_constraints_test.go` contains `TestDatabaseOpenFailsOnDuplicateInventoryFilePath`.
    - `mibo-media-server/internal/database/database_open_test.go` still contains startup coverage for fresh, legacy-only, and repeated-open databases.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/database -run 'TestDatabaseOpenCatalogKernel|TestDatabaseOpenFailsOnDuplicate' -count=1</automated>
  </verify>
  <done>Failing regression tests now define the safe success and safe failure startup behavior for Phase 20 hardening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit catalog-kernel constraint and index backstops</name>
  <read_first>
    - mibo-media-server/internal/database/database.go
    - mibo-media-server/internal/database/database_open_test.go
    - mibo-media-server/internal/database/database_constraints_test.go
    - mibo-media-server/internal/database/catalog_models.go
    - mibo-media-server/internal/database/inventory_models.go
    - mibo-media-server/internal/database/models.go
  </read_first>
  <files>
    - mibo-media-server/internal/database/database.go
    - mibo-media-server/internal/database/database_open_test.go
    - mibo-media-server/internal/database/database_constraints_test.go
  </files>
  <behavior>
    - Test 1: hardened startup still succeeds on clean empty and migrated databases.
    - Test 2: duplicate-key violations fail fast with actionable error text.
    - Test 3: the remaining unique/index guarantees are explicitly backstopped instead of relying on implicit automigration behavior alone.
  </behavior>
  <action>Update `database.go` so `Open(...)` runs an explicit catalog-kernel hardening step after `AutoMigrate(...)`. Add a duplicate preflight helper that checks, at minimum, `idx_catalog_external_identity`, `idx_metadata_field_state_item_field`, `idx_asset_items_asset_item_role_segment`, `idx_inventory_file_storage_path`, `idx_asset_files_asset_file_role_part`, `idx_media_stream_file_index`, and `idx_user_item_data_user_item_asset` for pre-existing duplicates. If any duplicates are found, return `catalog kernel constraint precheck failed: <issue>` before creating the corresponding index. For clean databases, explicitly `HasIndex/CreateIndex` the remaining required catalog/inventory unique indexes so SQLite and Postgres startup behavior stays additive and repeat-safe.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/database/database.go` contains `idx_catalog_external_identity`.
    - `mibo-media-server/internal/database/database.go` contains `idx_metadata_field_state_item_field`.
    - `mibo-media-server/internal/database/database.go` contains `idx_asset_items_asset_item_role_segment`.
    - `mibo-media-server/internal/database/database.go` contains `idx_inventory_file_storage_path`.
    - `mibo-media-server/internal/database/database.go` contains `idx_asset_files_asset_file_role_part`.
    - `mibo-media-server/internal/database/database.go` contains `idx_media_stream_file_index`.
    - `mibo-media-server/internal/database/database.go` contains `idx_user_item_data_user_item_asset`.
    - `mibo-media-server/internal/database/database.go` contains `catalog kernel constraint precheck failed`.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/database -run 'TestDatabaseOpenCatalogKernel|TestDatabaseOpenFailsOnDuplicate' -count=1</automated>
  </verify>
  <done>Startup now hardens the remaining catalog-kernel uniqueness/index guarantees safely and predictably across empty, legacy, and repeated-open cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrated database rows -> startup hardening | existing bad data can make production startup unsafe if not detected before index creation |
| hardening helper -> schema mutation | additive index creation must not partially mutate the schema after a failing precheck |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | D | `database.Open` hardening | mitigate | run duplicate preflight checks first and return actionable errors before any new unique index is created |
| T-20-06 | T | catalog/inventory unique guarantees | mitigate | explicitly `HasIndex/CreateIndex` the remaining catalog-kernel index names so production startup does not depend on ORM-specific implicit behavior |
</threat_model>

<verification>
- `cd mibo-media-server && go test ./internal/database -run 'TestDatabaseOpenCatalogKernel|TestDatabaseOpenFailsOnDuplicate' -count=1`
</verification>

<success_criteria>
- Startup hardening succeeds on clean empty and legacy databases.
- Duplicate-key problems fail safely with actionable error text.
- Remaining catalog-kernel unique/index guarantees are explicitly backstopped in startup code.
</success_criteria>

<output>
After completion, create `.planning/phases/20-legacy-model-retirement-production-hardening/20-03-SUMMARY.md`
</output>
