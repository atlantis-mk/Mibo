---
phase: 20-legacy-model-retirement-production-hardening
plan: 06
type: execute
wave: 4
depends_on:
  - 20-04
  - 20-05
files_modified:
  - web/src/lib/mibo-api.ts
  - web/src/lib/media-presentation.ts
  - web/src/features/media/index.tsx
  - web/src/features/play/index.tsx
  - mibo-media-server/scripts/catalog-kernel-production-check.sh
  - docs/media-architecture/catalog-kernel-retirement.md
autonomous: true
requirements:
  - PROD-03
  - PROD-04
must_haves:
  truths:
    - "The frontend no longer compiles against legacy `MediaItem` / `MediaFile` runtime contracts."
    - "Operators have one repeatable production-check command plus a cleanup/runback document before deleting legacy runtime code for good."
  artifacts:
    - path: web/src/lib/mibo-api.ts
      provides: catalog item / asset / inventory playback client contracts without legacy runtime exports
    - path: mibo-media-server/scripts/catalog-kernel-production-check.sh
      provides: repeatable backend/frontend validation command wrapper
    - path: docs/media-architecture/catalog-kernel-retirement.md
      provides: cleanup strategy, validation checklist, and rollback notes
  key_links:
    - from: web/src/features/media/index.tsx
      to: web/src/lib/mibo-api.ts
      via: catalog item and playback contract usage
      pattern: "CatalogItem|asset_id|inventory_file_id"
    - from: mibo-media-server/scripts/catalog-kernel-production-check.sh
      to: docs/media-architecture/catalog-kernel-retirement.md
      via: documented production validation checklist
      pattern: "go test ./...|pnpm typecheck|pnpm build"
---

<objective>
Finish the phase by cleaning the remaining frontend legacy contracts and shipping a repeatable production-validation + cleanup runbook.

Purpose: make the UI and operator workflow match the backend's catalog-only runtime before the legacy main path is considered fully retired.
Output: catalog-only frontend contracts, a production-check script, and a cleanup strategy document.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-RESEARCH.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-VALIDATION.md
@.planning/phases/17-playback-item-to-asset-cutover/17-03-PLAN.md

<interfaces>
Frontend runtime still exposes legacy contracts in `web/src/lib/mibo-api.ts`:

```ts
export type MediaItem = { ... }
export type MediaFile = { ... }
export type PlaybackSource = {
  media_item_id: number
  media_file_id: number
}
```

Phase 17 already defined the intended runtime direction:

```go
type PlaybackSource struct {
    ItemID          uint
    AssetID         uint
    InventoryFileID uint
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove lingering frontend legacy runtime contracts</name>
  <read_first>
    - web/src/lib/mibo-api.ts
    - web/src/lib/media-presentation.ts
    - web/src/features/media/index.tsx
    - web/src/features/play/index.tsx
    - .planning/phases/17-playback-item-to-asset-cutover/17-03-PLAN.md
  </read_first>
  <files>
    - web/src/lib/mibo-api.ts
    - web/src/lib/media-presentation.ts
    - web/src/features/media/index.tsx
    - web/src/features/play/index.tsx
  </files>
  <action>Update the web client contracts to match the catalog-only runtime that earlier phases introduced. In `web/src/lib/mibo-api.ts`, replace lingering exported runtime aliases centered on `MediaItem` / `MediaFile` with catalog item, asset-link, and inventory-playback shapes that use `item_id`, `asset_id`, and `inventory_file_id`. Remove legacy client methods or response typing that still assumes `/api/v1/media-items/*` or `/api/v1/media-files/*` are the main runtime API. Update `media-presentation.ts`, `features/media/index.tsx`, and `features/play/index.tsx` so their props, derived view models, and playback handling consume the catalog-only types and route assumptions instead of legacy media-item/file contracts.</action>
  <acceptance_criteria>
    - `web/src/lib/mibo-api.ts` contains `CatalogItem`.
    - `web/src/lib/mibo-api.ts` contains `asset_id`.
    - `web/src/lib/mibo-api.ts` contains `inventory_file_id`.
    - `web/src/lib/mibo-api.ts` does not export `MediaFile` as the normal runtime playback contract.
    - `web/src/features/play/index.tsx` contains `inventory_file_id` or an equivalent catalog-only playback identifier.
  </acceptance_criteria>
  <verify>
    <automated>cd web && pnpm typecheck</automated>
  </verify>
  <done>The frontend now compiles against catalog-only browse and playback contracts instead of the legacy media-item/media-file runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Add repeatable production validation and cleanup documentation</name>
  <read_first>
    - .planning/phases/20-legacy-model-retirement-production-hardening/20-RESEARCH.md
    - .planning/phases/20-legacy-model-retirement-production-hardening/20-VALIDATION.md
    - docs/media-architecture/milestones.md
  </read_first>
  <files>
    - mibo-media-server/scripts/catalog-kernel-production-check.sh
    - docs/media-architecture/catalog-kernel-retirement.md
  </files>
  <action>Create `mibo-media-server/scripts/catalog-kernel-production-check.sh` as the single repeatable validation entrypoint for Phase 20. It must run, in order: the database startup regression command from this phase, `go test ./... -count=1` from `mibo-media-server/`, `pnpm typecheck`, and `pnpm build` from `web/`, while failing fast on the first non-zero exit. Then add `docs/media-architecture/catalog-kernel-retirement.md` documenting: prerequisites before legacy cleanup, the exact script command, how to validate empty DB startup, legacy DB migration startup, repeated startup, how to review read-only migration status, and what rollback/hold-open conditions should prevent deleting the last legacy runtime code. Keep the document operational and explicit — it should read like a runbook, not a milestone recap.</action>
  <acceptance_criteria>
    - `mibo-media-server/scripts/catalog-kernel-production-check.sh` contains `go test ./... -count=1`.
    - `mibo-media-server/scripts/catalog-kernel-production-check.sh` contains `pnpm typecheck`.
    - `mibo-media-server/scripts/catalog-kernel-production-check.sh` contains `pnpm build`.
    - `docs/media-architecture/catalog-kernel-retirement.md` contains `## Cleanup Preconditions`.
    - `docs/media-architecture/catalog-kernel-retirement.md` contains `## Validation Checklist`.
    - `docs/media-architecture/catalog-kernel-retirement.md` contains `catalog-kernel-production-check.sh`.
  </acceptance_criteria>
  <verify>
    <automated>bash mibo-media-server/scripts/catalog-kernel-production-check.sh</automated>
  </verify>
  <done>Operators now have a repeatable production-check command and a cleanup runbook covering startup, regression, and rollback gates before final legacy retirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend contract changes -> frontend type system | stale frontend aliases can silently reintroduce legacy runtime assumptions |
| operator commands -> production cleanup | an incomplete validation checklist can trigger premature legacy deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-11 | T | frontend runtime contracts | mitigate | remove legacy `MediaItem` / `MediaFile` playback assumptions from `mibo-api.ts` and enforce the new contract with `pnpm typecheck` |
| T-20-12 | D | cleanup procedure | mitigate | ship one fail-fast production-check script plus an explicit runbook so operators cannot skip empty/legacy/repeated-startup and build validation before cleanup |
</threat_model>

<verification>
- `cd web && pnpm typecheck`
- `bash mibo-media-server/scripts/catalog-kernel-production-check.sh`
</verification>

<success_criteria>
- The frontend no longer compiles against legacy media-item/file runtime contracts.
- Operators have a single production-check command.
- Cleanup prerequisites, validation steps, and rollback gates are documented before the final legacy deletion window.
</success_criteria>

<output>
After completion, create `.planning/phases/20-legacy-model-retirement-production-hardening/20-06-SUMMARY.md`
</output>
