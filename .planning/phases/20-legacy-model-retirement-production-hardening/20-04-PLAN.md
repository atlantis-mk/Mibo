---
phase: 20-legacy-model-retirement-production-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-03
files_modified:
  - mibo-media-server/internal/library/query_browse.go
  - mibo-media-server/internal/library/query_browse_catalog_test.go
  - mibo-media-server/internal/search/service.go
  - mibo-media-server/internal/progress/service.go
  - mibo-media-server/internal/progress/catalog_progress_test.go
autonomous: true
requirements:
  - PROD-03
must_haves:
  truths:
    - "Browse, search, continue-watching, and recently-played runtime paths work from catalog tables even if legacy search/progress tables are empty."
    - "Normal runtime no longer writes legacy `PlaybackProgress` or `SearchDocument` rows."
  artifacts:
    - path: mibo-media-server/internal/library/query_browse.go
      provides: catalog-only browse/search query path
    - path: mibo-media-server/internal/progress/service.go
      provides: `user_item_data`-backed progress updates and reads
    - path: mibo-media-server/internal/progress/catalog_progress_test.go
      provides: regression coverage for catalog-only progress behavior
  key_links:
    - from: mibo-media-server/internal/library/query_browse.go
      to: mibo-media-server/internal/database/catalog_models.go
      via: discovery/search reads from `catalog_search_documents` and `user_item_data`
      pattern: "CatalogSearchDocument|UserItemData"
    - from: mibo-media-server/internal/progress/service.go
      to: mibo-media-server/internal/database/catalog_models.go
      via: progress persistence on `user_item_data`
      pattern: "UserItemData"
---

<objective>
Retire the remaining legacy browse/search/progress main-path dependencies so catalog runtime behavior no longer needs legacy search or progress tables.

Purpose: satisfy the non-playback half of Phase 20 legacy retirement before HTTP route cleanup and frontend finalization.
Output: catalog-only discovery/search/progress services and regression tests proving legacy runtime tables are no longer required.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-RESEARCH.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-VALIDATION.md
@.planning/phases/15-series-level-metadata-governance-engine/15-03-PLAN.md

<interfaces>
From `mibo-media-server/internal/database/catalog_models.go`:

```go
type UserItemData struct {
    UserID           uint
    ItemID           uint
    AssetID          *uint
    PositionSeconds  int
    DurationSeconds  *int
    CompletedAt      *time.Time
    LastPlayedAt     *time.Time
}
```

From `mibo-media-server/internal/database/catalog_models.go`:

```go
type CatalogSearchDocument struct {
    ItemID             uint
    LibraryID          uint
    ItemType           string
    Title              string
    OriginalTitle      string
    ProviderIDsText    string
    AvailabilityStatus string
    UpdatedAt          time.Time
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing catalog-only browse and progress regression coverage</name>
  <read_first>
    - mibo-media-server/internal/library/query_browse.go
    - mibo-media-server/internal/search/service.go
    - mibo-media-server/internal/progress/service.go
    - mibo-media-server/internal/progress/service_test.go
    - mibo-media-server/internal/database/catalog_models.go
  </read_first>
  <files>
    - mibo-media-server/internal/library/query_browse_catalog_test.go
    - mibo-media-server/internal/progress/catalog_progress_test.go
  </files>
  <behavior>
    - Test 1: discovery/search queries still return catalog items when `search_documents` is empty but `catalog_search_documents` is populated.
    - Test 2: progress update/state/continue-watching/recently-played still work when `playback_progress` is empty but `user_item_data` is present.
    - Test 3: watch-state ordering and filters derive from `user_item_data`, not `playback_progress`.
  </behavior>
  <action>Create focused failing tests that seed only catalog-side tables (`catalog_items`, `catalog_search_documents`, `user_item_data`, and any required asset links) while leaving legacy `search_documents` and `playback_progress` empty. In `query_browse_catalog_test.go`, assert `DiscoverMediaItems(...)` and search-oriented browse flows still return items and watched-state filters from catalog data. In `catalog_progress_test.go`, assert `Update`, `GetState`, `ContinueWatching`, and `RecentlyPlayed` round-trip through `user_item_data` and preserve completion/last-played semantics without creating any `PlaybackProgress` rows.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/library/query_browse_catalog_test.go` contains `TestDiscoverMediaItemsUsesCatalogSearchDocuments`.
    - `mibo-media-server/internal/progress/catalog_progress_test.go` contains `TestCatalogProgressUsesUserItemData`.
    - `mibo-media-server/internal/progress/catalog_progress_test.go` contains `TestCatalogProgressContinueWatchingUsesUserItemData`.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/library ./internal/progress ./internal/search -run 'Test(DiscoverMediaItemsUsesCatalogSearchDocuments|CatalogProgress)' -count=1</automated>
  </verify>
  <done>Failing regression tests now prove browse/search/progress must work from catalog tables alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove legacy search and progress runtime dependencies</name>
  <read_first>
    - mibo-media-server/internal/library/query_browse.go
    - mibo-media-server/internal/search/service.go
    - mibo-media-server/internal/progress/service.go
    - mibo-media-server/internal/library/query_browse_catalog_test.go
    - mibo-media-server/internal/progress/catalog_progress_test.go
  </read_first>
  <files>
    - mibo-media-server/internal/library/query_browse.go
    - mibo-media-server/internal/search/service.go
    - mibo-media-server/internal/progress/service.go
    - mibo-media-server/internal/library/query_browse_catalog_test.go
    - mibo-media-server/internal/progress/catalog_progress_test.go
  </files>
  <behavior>
    - Test 1: discovery/search use `catalog_search_documents` and catalog item ids as the normal runtime search surface.
    - Test 2: progress persistence uses `user_item_data` keyed by catalog item plus optional asset, not `PlaybackProgress` keyed by legacy media rows.
    - Test 3: watch-state filters and ranking aggregate `user_item_data` instead of `playback_progress`.
  </behavior>
  <action>Update `query_browse.go` so the normal runtime path reads `database.CatalogSearchDocument` and catalog item ids instead of legacy `SearchDocument`, and derive watch-state/ranking from `database.UserItemData` instead of `PlaybackProgress`. Update `progress/service.go` so `Update`, `GetState`, `ContinueWatching`, and `RecentlyPlayed` read/write `database.UserItemData` with catalog item ids and optional asset ids; remove remaining writes to `database.PlaybackProgress` and any call that reindexes legacy `SearchDocument` rows. Update `search/service.go` so the normal runtime search surface no longer builds or saves legacy `database.SearchDocument` rows for catalog-backed paths; keep any compatibility helper isolated and unused by normal runtime after this plan.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/library/query_browse.go` contains `database.CatalogSearchDocument`.
    - `mibo-media-server/internal/library/query_browse.go` contains `database.UserItemData`.
    - `mibo-media-server/internal/progress/service.go` contains `database.UserItemData`.
    - `mibo-media-server/internal/progress/service.go` does not contain `database.PlaybackProgress`.
    - `mibo-media-server/internal/search/service.go` does not contain `Save(&doc)` for `database.SearchDocument` in the normal catalog runtime path.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/library ./internal/progress ./internal/search -run 'Test(DiscoverMediaItemsUsesCatalogSearchDocuments|CatalogProgress)' -count=1</automated>
  </verify>
  <done>Browse/search/progress now trust catalog projections and `user_item_data` instead of legacy search/progress tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog projection rows -> browse/search runtime | stale or missing catalog documents can hide content or mis-rank watched state |
| authenticated progress writes -> `user_item_data` | runtime progress must not keep shadow-writing legacy progress tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | T | browse/search cutover | mitigate | seed catalog-only regression tests and remove the normal-path `SearchDocument` fallback so runtime cannot silently drift back to legacy rows |
| T-20-08 | T | progress cutover | mitigate | move persistence and reads to `UserItemData` and assert no `PlaybackProgress` writes remain in the service |
</threat_model>

<verification>
- `cd mibo-media-server && go test ./internal/library ./internal/progress ./internal/search -run 'Test(DiscoverMediaItemsUsesCatalogSearchDocuments|CatalogProgress)' -count=1`
</verification>

<success_criteria>
- Browse/search work from catalog projection tables alone.
- Continue-watching and recently-played work from `user_item_data`.
- Normal runtime no longer writes legacy progress/search rows.
</success_criteria>

<output>
After completion, create `.planning/phases/20-legacy-model-retirement-production-hardening/20-04-SUMMARY.md`
</output>
