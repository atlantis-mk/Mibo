---
phase: 20-legacy-model-retirement-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mibo-media-server/internal/catalog/consistency.go
  - mibo-media-server/internal/catalog/consistency_test.go
autonomous: true
requirements:
  - PROD-02
must_haves:
  truths:
    - "Operators can run a deterministic catalog consistency audit before enabling stricter production hardening."
    - "The audit reports duplicate keys, stale projections, and availability drift without mutating catalog data."
  artifacts:
    - path: mibo-media-server/internal/catalog/consistency.go
      provides: typed catalog consistency audit contracts and issue classification helpers
    - path: mibo-media-server/internal/catalog/consistency_test.go
      provides: seeded audit regression coverage for duplicate, availability, and projection issues
  key_links:
    - from: mibo-media-server/internal/catalog/consistency.go
      to: mibo-media-server/internal/database/catalog_models.go
      via: duplicate and stale projection queries across catalog-owned tables
      pattern: "CatalogSearchDocument|UserItemData|CatalogExternalID"
    - from: mibo-media-server/internal/catalog/consistency.go
      to: mibo-media-server/internal/database/inventory_models.go
      via: asset/file integrity checks for source-file availability
      pattern: "AssetItem|AssetFile|InventoryFile"
---

<objective>
Define the catalog-kernel consistency audit that Phase 20 uses as the safety net before hardening startup and deleting legacy runtime paths.

Purpose: give operators and later repair jobs a deterministic report of data problems instead of relying on ad hoc SQL or startup failures.
Output: typed audit contracts, issue codes, and regression tests for duplicate keys, missing source files, availability drift, and stale projections.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-RESEARCH.md
@.planning/phases/20-legacy-model-retirement-production-hardening/20-VALIDATION.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-03-SUMMARY.md
@.planning/phases/12-catalog-kernel-contracts-migration-guards/12-06-SUMMARY.md

<interfaces>
From `mibo-media-server/internal/catalog/projections.go`:

```go
func (s *Service) RefreshItemProjection(ctx context.Context, itemID uint) error
func (s *Service) RefreshLibraryProjection(ctx context.Context, libraryID uint, rootPath string) error
```

From `mibo-media-server/internal/settings/catalog_migration.go`:

```go
type CatalogMigrationState struct {
    CatalogBackfillCompletedAt *time.Time
    CatalogReadEnabled         bool
    LegacyCleanupCompletedAt   *time.Time
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression coverage for catalog consistency audits</name>
  <read_first>
    - mibo-media-server/internal/catalog/projections.go
    - mibo-media-server/internal/catalog/backfill.go
    - mibo-media-server/internal/catalog/backfill_movies.go
    - mibo-media-server/internal/catalog/backfill_movies_test.go
    - mibo-media-server/internal/database/catalog_models.go
    - mibo-media-server/internal/database/inventory_models.go
  </read_first>
  <files>
    - mibo-media-server/internal/catalog/consistency_test.go
  </files>
  <behavior>
    - Test 1: duplicate catalog identities and duplicate inventory file paths are surfaced as separate issue codes instead of being collapsed into one generic error.
    - Test 2: an item with missing source-file links or mismatched `availability_status` is reported as an integrity issue.
    - Test 3: missing or stale `item_rollups` / `catalog_search_documents` rows are reported as projection issues without mutating database state.
  </behavior>
  <action>Create `mibo-media-server/internal/catalog/consistency_test.go` first. Seed sqlite fixtures that intentionally create: one duplicate `catalog_external_ids` tuple, one duplicate `inventory_files(storage_provider, storage_path)` tuple, one catalog item whose `availability_status` claims `available` while its linked source file is missing, and one item with stale or absent `item_rollups` / `catalog_search_documents`. Add failing tests for `RunConsistencyAudit(...)` that assert exact issue codes `duplicate_external_identity`, `duplicate_inventory_file_path`, `availability_mismatch`, `missing_primary_source_file`, `missing_item_rollup`, and `missing_catalog_search_document` are returned in deterministic item/id order. Keep the audit read-only: the tests must assert the seeded bad rows still exist after the audit completes.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/catalog/consistency_test.go` contains `TestCatalogConsistencyAuditReportsDuplicateKeys`.
    - `mibo-media-server/internal/catalog/consistency_test.go` contains `TestCatalogConsistencyAuditReportsAvailabilityDrift`.
    - `mibo-media-server/internal/catalog/consistency_test.go` contains `TestCatalogConsistencyAuditReportsStaleProjections`.
    - `mibo-media-server/internal/catalog/consistency_test.go` contains `duplicate_external_identity` and `missing_catalog_search_document`.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/catalog -run 'TestCatalogConsistencyAudit' -count=1</automated>
  </verify>
  <done>Phase-20 integrity expectations are locked into failing catalog audit tests before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed catalog consistency audit contracts</name>
  <read_first>
    - mibo-media-server/internal/catalog/consistency_test.go
    - mibo-media-server/internal/catalog/projections.go
    - mibo-media-server/internal/database/catalog_models.go
    - mibo-media-server/internal/database/inventory_models.go
  </read_first>
  <files>
    - mibo-media-server/internal/catalog/consistency.go
    - mibo-media-server/internal/catalog/consistency_test.go
  </files>
  <behavior>
    - Test 1: the audit emits deterministic issue codes and scoped identifiers.
    - Test 2: the audit never mutates bad rows; it only reports them.
    - Test 3: missing rollups/search docs and availability drift are classified separately so later repair jobs can target them precisely.
  </behavior>
  <action>Implement `mibo-media-server/internal/catalog/consistency.go` with typed contracts such as `ConsistencyScope`, `ConsistencyIssue`, `ConsistencyReport`, and `RunConsistencyAudit(ctx, scope)`. Query catalog-owned tables directly to detect the exact issue families from Task 1: duplicate external identities, duplicate inventory file paths, missing primary source-file links, `catalog_items.availability_status` mismatches against linked `asset_files(role="source")` + `inventory_files.status`, missing `item_rollups`, missing `catalog_search_documents`, and stale projection timestamps where `catalog_search_documents.updated_at` or `item_rollups.updated_at` are older than the owning `catalog_items.updated_at`. Return deterministic issue ordering by `issue_code`, `library_id`, `item_id`, and primary row id, and do not write any fixes in this plan.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/catalog/consistency.go` contains `type ConsistencyIssue struct`.
    - `mibo-media-server/internal/catalog/consistency.go` contains `func (s *Service) RunConsistencyAudit(`.
    - `mibo-media-server/internal/catalog/consistency.go` contains `duplicate_external_identity`.
    - `mibo-media-server/internal/catalog/consistency.go` contains `missing_primary_source_file`.
    - `mibo-media-server/internal/catalog/consistency.go` contains `missing_item_rollup`.
    - `mibo-media-server/internal/catalog/consistency.go` contains `missing_catalog_search_document`.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/catalog -run 'TestCatalogConsistencyAudit' -count=1</automated>
  </verify>
  <done>Catalog consistency can now be audited deterministically before any repair jobs or startup hardening tighten production invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog/inventory rows -> audit classification | stale or malformed rows must be classified accurately before later repair automation acts on them |
| audit scope input -> full database queries | scope narrowing must not accidentally skip or widen libraries/items |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T | `RunConsistencyAudit` | mitigate | keep this plan read-only and emit explicit issue codes so later repair work cannot guess what to mutate |
| T-20-02 | D | audit scope execution | mitigate | sort and scope queries deterministically by library/item identifiers so full-db audits stay predictable and resumable |
</threat_model>

<verification>
- `cd mibo-media-server && go test ./internal/catalog -run 'TestCatalogConsistencyAudit' -count=1`
</verification>

<success_criteria>
- Operators have a deterministic report of duplicate-key, availability, and projection problems.
- The audit is read-only and safe to run before production hardening.
- Issue codes are specific enough for later repair jobs to target without inference.
</success_criteria>

<output>
After completion, create `.planning/phases/20-legacy-model-retirement-production-hardening/20-01-SUMMARY.md`
</output>
