---
phase: 08-native-search-discovery-filters
plan: 04
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - mibo-media-server/internal/httpapi/router_test.go
  - mibo-media-server/internal/metadata/service_test.go
  - mibo-media-server/internal/progress/service_test.go
  - mibo-media-server/internal/worker/worker_test.go
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07
  - SRCH-08
  - FLTR-01
  - FLTR-02
  - FLTR-03
  - FLTR-04
  - FLTR-05
  - FLTR-06
must_haves:
  truths:
    - Automated tests prove search and browse stay consistent after metadata, scan, and progress mutations.
    - Region/rating and watched-state filters are verified end to end, not inferred from passing build/type checks.
    - Phase 8 gap closure has executor-style verification artifacts ready for final execution closeout.
  artifacts:
    - path: mibo-media-server/internal/httpapi/router_test.go
      provides: end-to-end discovery endpoint coverage.
    - path: mibo-media-server/internal/metadata/service_test.go
      provides: metadata-to-projection coverage for region/rating.
    - path: mibo-media-server/internal/worker/worker_test.go
      provides: reindex job dispatch coverage.
  key_links:
    - from: mibo-media-server/internal/httpapi/router_test.go
      to: /api/v1/discovery and /api/v1/libraries/{id}/items
      via: regression assertions on identical filters and mutation scenarios
      pattern: /api/v1/discovery|/api/v1/libraries/
    - from: mibo-media-server/internal/worker/worker_test.go
      to: reindex job kinds
      via: worker executes queued rebuilds
      pattern: reindex_search|reindex_library
gap_closure: true
---

<objective>
Prove the remaining Phase 8 verification gaps are closed with focused regression coverage and phase-level verification commands.

Purpose: The previous verification already showed build/test success was insufficient; this plan adds the mutation-driven tests that specifically guard region/rating confidence and discovery freshness.
Output: Regression tests for metadata, progress, worker dispatch, and endpoint-level discovery consistency.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-native-search-discovery-filters/08-CONTEXT.md
@.planning/phases/08-native-search-discovery-filters/08-VERIFICATION.md
@.planning/phases/08-native-search-discovery-filters/08-02-PLAN.md
@.planning/phases/08-native-search-discovery-filters/08-03-PLAN.md
@.planning/phases/07-metadata-governance-matching/07-SUMMARY.md

<interfaces>
From `mibo-media-server/internal/httpapi/handlers_search.go`:

```go
func (r *Router) handleDiscoverMedia(w http.ResponseWriter, req *http.Request)
func discoveryInputFromRequest(req *http.Request) (library.BrowseMediaItemsInput, error)
```

From `mibo-media-server/internal/httpapi/handlers_libraries.go`:

```go
func (r *Router) handleListLibraryItems(w http.ResponseWriter, req *http.Request)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mutation-driven regression coverage for filter completeness</name>
  <read_first>
    - mibo-media-server/internal/metadata/service_test.go
    - mibo-media-server/internal/progress/service_test.go
    - mibo-media-server/internal/httpapi/router_test.go
    - mibo-media-server/internal/metadata/service_match.go
    - mibo-media-server/internal/progress/service.go
  </read_first>
  <files>mibo-media-server/internal/metadata/service_test.go, mibo-media-server/internal/progress/service_test.go, mibo-media-server/internal/httpapi/router_test.go</files>
  <action>Add regression tests that explicitly close the truths called out in `08-VERIFICATION.md`. Cover: (1) metadata match/refetch populates countries and rating so `region` and `min_rating` filters return the expected items; (2) progress updates move an item through `unwatched -> in_progress -> watched` and both `/api/v1/discovery` and `/api/v1/libraries/{id}/items` reflect the same results; (3) search highlights and media-type distinction still survive the projection-backed read path. Use the existing httptest/TMDB patterns from Phase 7 rather than inventing a new test harness.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/metadata/service_test.go` contains at least one test covering production-country/rating persistence.
    - `mibo-media-server/internal/progress/service_test.go` contains at least one test covering watched-state transitions that trigger projection freshness.
    - `mibo-media-server/internal/httpapi/router_test.go` contains endpoint assertions for `region`, `min_rating`, and `watched_state` filters on discovery/browse routes.
    - At least one router test asserts movie/show distinction and non-empty highlight behavior still appear in `/api/v1/discovery` responses.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/metadata ./internal/progress ./internal/httpapi -run 'Test.*(Discovery|Region|Rating|Watched|Highlight)'</automated>
  </verify>
  <done>The two partially proven requirements (`FLTR-03`, `FLTR-04`) and the watched-state freshness behavior now have explicit automated proof.</done>
</task>

<task type="auto">
  <name>Task 2: Add worker/scan regression proof and run full Phase 8 verification</name>
  <read_first>
    - mibo-media-server/internal/worker/worker_test.go
    - mibo-media-server/internal/httpapi/router_test.go
    - mibo-media-server/internal/library/scan_run.go
    - mibo-media-server/internal/worker/worker.go
    - .planning/phases/08-native-search-discovery-filters/08-VERIFICATION.md
  </read_first>
  <files>mibo-media-server/internal/worker/worker_test.go, mibo-media-server/internal/httpapi/router_test.go</files>
  <action>Add worker-level tests proving the new reindex job kinds are dispatched after scan/targeted-refresh flows, then rerun the Phase 8 verification command set with the new regression suite. The verification notes must specifically demonstrate that the prior gap statement about “explicit lifecycle hooks” is no longer true. Keep this task focused on proof and verification; do not reopen scope for deferred filters or external middleware.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/worker/worker_test.go` contains coverage for the new search reindex job kinds.
    - The verification command in this task includes `go test ./...`, plus the focused discovery packages used by the gap report.
    - Test names or assertions mention scan/targeted-refresh driven projection refresh, not only metadata/progress mutations.
    - No new task content adds deferred items `FLTR-07`, `FLTR-08`, or external search middleware.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/worker ./internal/httpapi && go test ./... </automated>
  </verify>
  <done>The phase has an automated proof set for the previously missing freshness hooks, and the executor can update Phase 8 verification/summary artifacts without improvising new tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated tests → release confidence | Verification claims become trustworthy only if tests model real mutation paths. |
| worker queue → discovery endpoints | Background reindex work must surface correctly in user-facing results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | R | `router_test.go` | mitigate | Add explicit assertions for each previously missing truth so the phase cannot claim closure on generic build/type success alone. |
| T-08-04-02 | D | `worker_test.go` | mitigate | Verify new reindex jobs are exercised in tests to prevent silent worker regressions that would leave search stale under load. |
| T-08-04-03 | I | verification workflow | accept | Tests run against local fixtures/httptest servers only; no production data or secrets cross the verification boundary. |
</threat_model>

<verification>
- `go test ./internal/metadata ./internal/progress ./internal/httpapi ./internal/worker`
- `go test ./...`
- Re-run the Phase 8 verification checklist and update the final summary with the closed gaps.
</verification>

<success_criteria>
- The remaining Phase 8 gaps have dedicated automated tests tied to real mutation paths.
- Search and browse consistency is proven for region, rating, watched-state, highlights, and media-type distinction.
- Executor closeout can produce standard summary/verification artifacts from these numbered plans.
</success_criteria>

<output>
After completion, create `.planning/phases/08-native-search-discovery-filters/08-04-SUMMARY.md`
</output>
