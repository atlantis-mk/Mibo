---
phase: 17-playback-item-to-asset-cutover
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - mibo-media-server/internal/httpapi/router.go
  - mibo-media-server/internal/httpapi/handlers_playback.go
  - mibo-media-server/internal/httpapi/hls.go
  - mibo-media-server/internal/httpapi/inventory_playback_router_test.go
autonomous: true
requirements:
  - PLAY-03
must_haves:
  truths:
    - "Direct stream and HLS playlist routes resolve inventory-file identifiers instead of legacy media-file ids."
    - "When the chosen inventory file is missing or unreadable, item playback returns a clear unplayable decision instead of a server error."
  artifacts:
    - path: mibo-media-server/internal/httpapi/hls.go
      provides: inventory-file keyed HLS artifact generation and lookup
    - path: mibo-media-server/internal/httpapi/handlers_playback.go
      provides: inventory-file direct stream and HLS handlers
    - path: mibo-media-server/internal/httpapi/inventory_playback_router_test.go
      provides: regression coverage for inventory stream/HLS routes and missing-file decisions
  key_links:
    - from: mibo-media-server/internal/httpapi/hls.go
      to: mibo-media-server/internal/database/inventory_models.go
      via: inventory-file source lookup for ffmpeg input resolution
      pattern: "InventoryFile|inventory_files"
    - from: mibo-media-server/internal/httpapi/handlers_playback.go
      to: mibo-media-server/internal/httpapi/hls.go
      via: inventory-file playlist/artifact delegation
      pattern: "EnsurePlaylist|ArtifactPath|rewriteHLSPlaylist"
---

<objective>
Finish the playback cutover by moving direct stream and HLS transport endpoints onto inventory-file identifiers and making unavailable files surface as explainable unplayable results.

Purpose: satisfy the file-resolution half of Phase 17 so every runnable playback URL is rooted in the catalog kernel's inventory boundary.
Output: inventory-file keyed stream/HLS routes, updated HLS service, and route tests for missing-file behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-playback-item-to-asset-cutover/17-RESEARCH.md
@.planning/phases/17-playback-item-to-asset-cutover/17-PATTERNS.md
@.planning/phases/17-playback-item-to-asset-cutover/17-01-PLAN.md
@.planning/phases/17-playback-item-to-asset-cutover/17-02-PLAN.md

<interfaces>
Expected from `mibo-media-server/internal/playback/profile.go` after 17-01:

```go
type PlaybackSource struct {
    ItemID          uint
    AssetID         uint
    InventoryFileID uint
    URL             string
    Playable        bool
    Decision        PlaybackDecision
}
```

Current HLS service shape from `mibo-media-server/internal/httpapi/hls.go`:

```go
func (s *hlsService) PlaylistURL(mediaFileID uint) string
func (s *hlsService) EnsurePlaylist(ctx context.Context, mediaFileID uint) (string, error)
func (s *hlsService) ArtifactPath(mediaFileID uint, name string) (string, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing inventory playback route coverage</name>
  <read_first>
    - mibo-media-server/internal/httpapi/handlers_playback.go
    - mibo-media-server/internal/httpapi/hls.go
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/router_test.go
    - mibo-media-server/internal/playback/service.go
  </read_first>
  <files>
    - mibo-media-server/internal/httpapi/inventory_playback_router_test.go
  </files>
  <behavior>
    - Test 1: direct playback URLs point at `/api/v1/inventory-files/{id}/stream` and HLS fallback URLs point at `/api/v1/inventory-files/{id}/hls/index.m3u8`.
    - Test 2: HLS playlist rewriting uses inventory-file ids for artifact URLs.
    - Test 3: when the selected inventory file is missing, the item-playback route still returns `200 OK` with `decision.kind="unplayable"` and a missing-file reason.
  </behavior>
  <action>Create `inventory_playback_router_test.go` with focused route tests for inventory-file stream/HLS behavior. Reuse the local-storage and auth fixture shape from existing router playback tests, but assert the new URLs now reference `/api/v1/inventory-files/`. Add one missing-file test that deletes the seeded backing file before hitting `/api/v1/items/{id}/playback?client_profile=web`, then assert the JSON payload is still `200 OK`, `playable=false`, and `decision.reasons` includes `inventory_file_missing` or `source_unavailable`. Add one HLS rewrite test that verifies rewritten playlist segment lines use `/api/v1/inventory-files/{id}/hls/{segment}`.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/inventory_playback_router_test.go` contains `TestInventoryPlaybackUsesInventoryFileRoutes`.
    - `mibo-media-server/internal/httpapi/inventory_playback_router_test.go` contains `TestInventoryPlaybackReturnsUnplayableWhenFileMissing`.
    - `mibo-media-server/internal/httpapi/inventory_playback_router_test.go` contains `TestInventoryHLSPlaylistRewritesArtifactURLs`.
    - `mibo-media-server/internal/httpapi/inventory_playback_router_test.go` asserts `/api/v1/inventory-files/`.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/httpapi -run 'TestInventoryPlayback|TestInventoryHLS' -count=1</automated>
  </verify>
  <done>Inventory-file keyed route expectations are locked in failing tests before transport code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move stream and HLS transport to inventory-file ids</name>
  <read_first>
    - mibo-media-server/internal/httpapi/handlers_playback.go
    - mibo-media-server/internal/httpapi/hls.go
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/inventory_playback_router_test.go
    - mibo-media-server/internal/database/inventory_models.go
  </read_first>
  <files>
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/handlers_playback.go
    - mibo-media-server/internal/httpapi/hls.go
    - mibo-media-server/internal/httpapi/inventory_playback_router_test.go
  </files>
  <behavior>
    - Test 1: HLS service and handler helpers use `inventoryFileID` consistently for playlist generation, artifact storage, and rewrite paths.
    - Test 2: direct stream handler loads `database.InventoryFile`, resolves the provider through the file's library/source, and proxies or serves the resulting object.
    - Test 3: item-playback responses stay on the explainable payload path when the underlying inventory file is gone.
  </behavior>
  <action>Update `router.go` to register `GET /api/v1/inventory-files/{id}/stream`, `GET /api/v1/inventory-files/{id}/hls/index.m3u8`, and `GET /api/v1/inventory-files/{id}/hls/{name}`. In `handlers_playback.go`, switch the stream/HLS handlers and `rewriteHLSPlaylist(...)` helper from `mediaFileID` to `inventoryFileID`, and load `database.InventoryFile` instead of `database.MediaFile` for the direct-stream path. In `hls.go`, rename the identifier variables and artifact-keying helpers to inventory-file semantics, load `InventoryFile` rows in `resolveInputSource(...)`, and continue resolving provider access through the library -> media source -> registry chain. Preserve the current ffmpeg locking/artifact behavior; only the identifier source changes. Ensure missing inventory files bubble back to the item-playback route as structured `unplayable` reasons rather than uncaught handler errors.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/inventory-files/{id}/stream`.
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/inventory-files/{id}/hls/index.m3u8`.
    - `mibo-media-server/internal/httpapi/router.go` contains `GET /api/v1/inventory-files/{id}/hls/{name}`.
    - `mibo-media-server/internal/httpapi/hls.go` contains `PlaylistURL(inventoryFileID uint)` or an equivalent renamed signature.
    - `mibo-media-server/internal/httpapi/handlers_playback.go` contains `/api/v1/inventory-files/%d/hls/%s`.
    - `mibo-media-server/internal/httpapi/handlers_playback.go` contains `database.InventoryFile`.
  </acceptance_criteria>
  <verify>
    <automated>cd mibo-media-server && go test ./internal/httpapi -run 'TestInventoryPlayback|TestInventoryHLS' -count=1</automated>
  </verify>
  <done>All playback transport endpoints now resolve inventory-file identifiers, and missing files degrade into explicit unplayable playback decisions instead of opaque failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory-file route params -> HLS/direct handlers | untrusted callers choose which file-backed artifact to access |
| HLS artifact names -> filesystem | playlist artifact names must stay inside the HLS root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | T | inventory-file stream route | mitigate | load `database.InventoryFile` by id and resolve provider access through library/source records instead of trusting any caller-supplied storage path |
| T-17-09 | E | HLS artifact serving | mitigate | keep `filepath.Base(...)` sanitization plus `isWithinRoot(...)` checks when serving `/api/v1/inventory-files/{id}/hls/{name}` artifacts |
| T-17-10 | D | missing inventory files | mitigate | convert missing-source states into structured `unplayable` decisions on the item-playback path so clients receive a stable response instead of retry-triggering 500s |
</threat_model>

<verification>
- `cd mibo-media-server && go test ./internal/httpapi -run 'TestInventoryPlayback|TestInventoryHLS' -count=1`
</verification>

<success_criteria>
- Direct stream and HLS routes are inventory-file keyed.
- Playback URLs emitted by the item-playback route point only at inventory-file endpoints.
- Missing files are explainable to clients through structured unplayable decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/17-playback-item-to-asset-cutover/17-03-SUMMARY.md`
</output>
