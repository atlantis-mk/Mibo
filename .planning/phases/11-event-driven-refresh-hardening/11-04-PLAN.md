---
phase: 11-event-driven-refresh-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 11-02
files_modified:
  - mibo-media-server/internal/httpapi/handlers_storage_events.go
  - mibo-media-server/internal/httpapi/router_test.go
autonomous: true
gap_closure: true
requirements:
  - LIST-01
  - LIST-02
user_setup: []
must_haves:
  truths:
    - Non-local libraries rooted at `/` accept valid absolute child storage-event paths.
    - Accepted OpenList-root child events become listener refresh intent instead of returning `400`.
    - Escaping protections remain active for local and non-root libraries.
  artifacts:
    - path: mibo-media-server/internal/httpapi/handlers_storage_events.go
      provides: provider-aware storage-event path validation that handles non-local `/` roots.
    - path: mibo-media-server/internal/httpapi/router_test.go
      provides: regression proof for OpenList/non-local `/` root child events.
  key_links:
    - from: mibo-media-server/internal/httpapi/handlers_storage_events.go
      to: mibo-media-server/internal/listener/service.go
      via: accepted `/MovieA.2024.mkv` event delegated to `RecordStorageEvent`
      pattern: RecordStorageEvent
    - from: mibo-media-server/internal/httpapi/router_test.go
      to: mibo-media-server/internal/httpapi/handlers_storage_events.go
      via: route-level `POST /api/v1/storage-events` regression for OpenList root `/`
      pattern: TestStorageEvent.*OpenList.*Root
---

<objective>
Close the LIST-01 ingress gap where valid OpenList/non-local child paths under a `/` library root are rejected before listener work is created.

Purpose: Phase 11 cannot fully satisfy storage listener semantics while the default OpenList root `/` rejects valid child events like `/MovieA.2024.mkv`.
Output: A provider-aware root-path fix plus route regression coverage proving accepted listener refresh intent.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-event-driven-refresh-hardening/11-CONTEXT.md
@.planning/phases/11-event-driven-refresh-hardening/11-VERIFICATION.md
@.planning/phases/11-event-driven-refresh-hardening/11-02-SUMMARY.md

<interfaces>
From `mibo-media-server/internal/httpapi/handlers_storage_events.go`:

```go
func validateStorageEventPath(providerName string, libraryRoot string, candidate string) (string, error)
```

Current non-local branch to fix:

```go
cleanRoot := filepath.Clean("/" + strings.TrimLeft(strings.TrimSpace(libraryRoot), "/"))
cleanCandidate := filepath.Clean("/" + strings.TrimLeft(trimmed, "/"))
if cleanCandidate != cleanRoot && !strings.HasPrefix(cleanCandidate, cleanRoot+"/") {
    return "", fmt.Errorf("path %s is outside library root %s", cleanCandidate, cleanRoot)
}
return cleanCandidate, nil
```

From `mibo-media-server/internal/httpapi/router_test.go`:

```go
func mustDecodeStorageEventResponseJob(t *testing.T, raw []byte) database.Job
func mustDecodeListenerRefreshPayload(t *testing.T, raw string) listenerPayloadView
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove OpenList root child storage events are accepted</name>
  <files>mibo-media-server/internal/httpapi/router_test.go</files>
  <read_first>
    - mibo-media-server/internal/httpapi/router_test.go
    - mibo-media-server/internal/httpapi/handlers_storage_events.go
    - mibo-media-server/internal/config/config.go
    - .planning/phases/11-event-driven-refresh-hardening/11-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: an authenticated `POST /api/v1/storage-events` payload `{"library_id": <id>, "kind": "update", "path": "/MovieA.2024.mkv"}` for a non-local `openlist` media source and library rooted at `/` returns `202` per LIST-01.
    - Test 2: the accepted response `data.kind` is `apply_storage_event_refresh` and the persisted listener payload has `root_path == "/"` and `fallback_full_sync == false` per D-01 and D-03.
    - Test 3: the test constructs the OpenList-root source/library directly in sqlite so it proves route validation and listener enqueue behavior without calling a real OpenList server.
  </behavior>
  <action>Add a route regression named `TestStorageEventEndpointAcceptsOpenListRootChildPath` near the existing storage-event tests. Create a sqlite test DB, config with `Storage.Provider = "openlist"` and `OpenList.RootPath = "/"`, real auth/jobs/library/listener/router services, then insert `database.MediaSource{Provider: "openlist", RootPath: "/", StorageRef: "/"}` and `database.Library{RootPath: "/", Status: "active", ScannerEnabled: true}` directly with GORM. Authenticate with `createAuthHeader`, POST `kind:"update"` and `path:"/MovieA.2024.mkv"`, assert status `202`, assert the response job kind is `listener.JobKindApplyStorageEventRefresh`, load the latest queued job, decode with `mustDecodeListenerRefreshPayload`, and assert `RootPath == "/"` and `FallbackFullSync == false`. This test covers the verification gap that OpenList defaults to `/` in `config.go`.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/router_test.go` contains `func TestStorageEventEndpointAcceptsOpenListRootChildPath(`
    - The new test request body contains `"path":"/MovieA.2024.mkv"`
    - The new test asserts `recorder.Code != http.StatusAccepted` failure text or equivalent `202` expectation
    - The new test asserts `listener.JobKindApplyStorageEventRefresh`
    - The new test asserts decoded listener payload `RootPath` equals `/` and `FallbackFullSync` is false
  </acceptance_criteria>
  <verify>
    <automated>cd /root/Mibo/mibo-media-server && go test ./internal/httpapi -run 'TestStorageEventEndpointAcceptsOpenListRootChildPath'</automated>
  </verify>
  <done>The OpenList-root regression exists and fails before the validation fix, then passes after Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix non-local `/` root path validation without weakening boundary checks</name>
  <files>mibo-media-server/internal/httpapi/handlers_storage_events.go, mibo-media-server/internal/httpapi/router_test.go</files>
  <read_first>
    - mibo-media-server/internal/httpapi/handlers_storage_events.go
    - mibo-media-server/internal/httpapi/router_test.go
    - .planning/phases/11-event-driven-refresh-hardening/11-CONTEXT.md
    - .planning/phases/11-event-driven-refresh-hardening/11-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: non-local root `/` accepts `/MovieA.2024.mkv` and delegates to listener refresh intent per LIST-01 and LIST-02.
    - Test 2: non-root non-local roots still require either exact root equality or `cleanRoot + "/"` prefix.
    - Test 3: local provider validation still uses `filepath.Rel` and continues rejecting paths outside the local library root.
  </behavior>
  <action>Update only the non-local branch of `validateStorageEventPath`. Keep the existing `cleanRoot` and `cleanCandidate` normalization. Add an explicit branch immediately after computing them: when `cleanRoot == string(filepath.Separator)` or `cleanRoot == "/"`, return `cleanCandidate, nil` because every normalized absolute candidate is inside a root library. Leave the existing prefix check unchanged for all non-root values, and leave the local-provider `filepath.Rel` branch unchanged. This preserves D-13 by fixing Mibo-side validation and does not add provider-specific behavior to `OpenList`.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/handlers_storage_events.go` contains `cleanRoot == string(filepath.Separator)` or `cleanRoot == "/"`
    - `mibo-media-server/internal/httpapi/handlers_storage_events.go` still contains `filepath.Rel(cleanRoot, cleanCandidate)` in the local provider branch
    - `mibo-media-server/internal/httpapi/handlers_storage_events.go` still contains `!strings.HasPrefix(cleanCandidate, cleanRoot+"/")` for non-root non-local validation
    - `mibo-media-server/internal/httpapi/router_test.go` contains `TestStorageEventEndpointRejectsEscapingPath` unchanged or still present
  </acceptance_criteria>
  <verify>
    <automated>cd /root/Mibo/mibo-media-server && go test ./internal/httpapi -run 'TestStorageEvent'</automated>
  </verify>
  <done>Valid OpenList-root child events create listener jobs, while existing auth and path boundary regressions still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated caller → `/api/v1/storage-events` | Untrusted storage paths enter the backend listener workflow. |
| HTTP path validation → listener service | Bad validation can either reject valid default-provider events or accept out-of-scope local paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | D | `validateStorageEventPath` non-local `/` root | mitigate | Special-case normalized root `/` so valid OpenList child events are accepted and tested with `TestStorageEventEndpointAcceptsOpenListRootChildPath`. |
| T-11-04-02 | T | local provider path validation | mitigate | Keep the existing `filepath.Rel` local branch and run all `TestStorageEvent` regressions so outside-root local paths still return `400`. |
| T-11-04-03 | R | storage-event route behavior | mitigate | Assert accepted listener job kind and payload in route tests so default-provider listener enqueue behavior is auditable. |
</threat_model>

<verification>
- `cd /root/Mibo/mibo-media-server && go test ./internal/httpapi -run 'TestStorageEvent'`
- `cd /root/Mibo/mibo-media-server && go test ./...`
</verification>

<success_criteria>
- LIST-01 default OpenList root events no longer fail before listener enqueue.
- LIST-02 remains true because accepted events still return `apply_storage_event_refresh` listener intent.
- Existing escaping-path and auth protections remain covered by automated route tests.
</success_criteria>

<output>
After completion, create `.planning/phases/11-event-driven-refresh-hardening/11-04-SUMMARY.md`
</output>
