---
phase: 11-event-driven-refresh-hardening
plan: 05
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-03
files_modified:
  - mibo-media-server/internal/database/models.go
  - mibo-media-server/internal/database/database.go
  - mibo-media-server/internal/listener/service.go
  - mibo-media-server/internal/listener/service_test.go
autonomous: true
gap_closure: true
requirements:
  - LIST-03
  - LIST-04
user_setup: []
must_haves:
  truths:
    - Concurrent duplicate storage events produce one active `apply_storage_event_refresh` listener row for the same library change window.
    - Concurrent reconciliation coverage calls produce one active `listener_reconcile` row per library.
    - Completed historical jobs remain possible while active listener intent uniqueness is enforced separately.
  artifacts:
    - path: mibo-media-server/internal/database/models.go
      provides: database model for active listener intent uniqueness.
    - path: mibo-media-server/internal/database/database.go
      provides: AutoMigrate registration for the active intent guard table.
    - path: mibo-media-server/internal/listener/service.go
      provides: atomic listener refresh and reconcile job creation guarded by active intent keys.
    - path: mibo-media-server/internal/listener/service_test.go
      provides: concurrent regressions for refresh and reconcile duplicate prevention.
  key_links:
    - from: mibo-media-server/internal/listener/service.go
      to: mibo-media-server/internal/database/models.go
      via: `JobActiveIntent` unique `intent_key` guard before listener job insert/update
      pattern: JobActiveIntent|intent_key
    - from: mibo-media-server/internal/listener/service_test.go
      to: mibo-media-server/internal/listener/service.go
      via: concurrent `RecordStorageEvent` and `EnsureReconcileCoverage` calls
      pattern: Concurrent.*(RecordStorageEvent|EnsureReconcileCoverage)
---

<objective>
Close the LIST-03/LIST-04 concurrency gap by making active listener refresh and reconcile intent creation atomic under bursty concurrent calls.

Purpose: Sequential coalescing is already implemented, but Phase 11 remains incomplete until concurrent event storms cannot bypass check-then-insert listener and reconcile flows.
Output: A durable active-intent guard table, listener service updates, and concurrent regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-event-driven-refresh-hardening/11-CONTEXT.md
@.planning/phases/11-event-driven-refresh-hardening/11-VERIFICATION.md
@.planning/phases/11-event-driven-refresh-hardening/11-01-SUMMARY.md
@.planning/phases/11-event-driven-refresh-hardening/11-03-SUMMARY.md

<interfaces>
From `mibo-media-server/internal/listener/service.go`:

```go
func (s *Service) RecordStorageEvent(ctx context.Context, input EventIngestInput) (database.Job, error)
func (s *Service) EnsureReconcileCoverage(ctx context.Context, libraries []database.Library) error
func refreshJobKey(libraryID uint, rootPath string, fallback bool) string
func reconcileJobKey(libraryID uint) string
```

From `mibo-media-server/internal/database/models.go`:

```go
type Job struct {
    ID          uint      `gorm:"primaryKey" json:"id"`
    JobKey      string    `gorm:"size:255;index" json:"job_key"`
    Kind        string    `gorm:"size:128;not null;index" json:"kind"`
    Status      string    `gorm:"size:64;not null;index" json:"status"`
    PayloadJSON string    `gorm:"type:text" json:"payload_json"`
    AvailableAt time.Time `gorm:"not null;index" json:"available_at"`
}
```

Do not make `Job.JobKey` globally unique: completed historical listener jobs reuse logical keys on later windows. Enforce uniqueness through a separate active-intent guard instead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add concurrent listener duplicate regressions</name>
  <files>mibo-media-server/internal/listener/service_test.go</files>
  <read_first>
    - mibo-media-server/internal/listener/service_test.go
    - mibo-media-server/internal/listener/service.go
    - mibo-media-server/internal/database/models.go
    - .planning/phases/11-event-driven-refresh-hardening/11-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: 20 concurrent `RecordStorageEvent` calls with the same library, kind `update`, and same movie path return no errors and leave exactly one active queued/running `apply_storage_event_refresh` row.
    - Test 2: 20 concurrent `EnsureReconcileCoverage` calls for the same active library return no errors and leave exactly one active queued/running `listener_reconcile` row.
    - Test 3: both tests count active rows using `status IN (jobs.StatusQueued, jobs.StatusRunning)` so completed history is not treated as an active duplicate.
  </behavior>
  <action>Add two tests near the existing listener service tests: `TestRecordStorageEventConcurrentDuplicatesKeepOneActiveIntent` and `TestEnsureReconcileCoverageConcurrentCallsKeepOneActiveIntent`. Use `sync.WaitGroup`, an error channel sized to the goroutine count, `context.Background()`, and the existing `newListenerTestService(t)` helper. Set `svc.now` to one fixed UTC value before launching goroutines. After all goroutines finish, fail on any returned error. Query the sqlite DB for active rows with `Where("kind = ? AND status IN ?", listenerKind, []string{jobs.StatusQueued, jobs.StatusRunning})` and assert the count is exactly `1`. These tests encode the verification gap from `11-VERIFICATION.md` and must fail against the current check-then-insert implementation before Task 2 completes.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/listener/service_test.go` contains `TestRecordStorageEventConcurrentDuplicatesKeepOneActiveIntent`
    - `mibo-media-server/internal/listener/service_test.go` contains `TestEnsureReconcileCoverageConcurrentCallsKeepOneActiveIntent`
    - The new tests import and use `sync.WaitGroup`
    - The new tests count rows with `status IN ?` and `jobs.StatusQueued`, `jobs.StatusRunning`
    - The new tests assert exactly one active row remains for `JobKindApplyStorageEventRefresh` and `JobKindListenerReconcile`
  </acceptance_criteria>
  <verify>
    <automated>cd /root/Mibo/mibo-media-server && go test ./internal/listener -run 'Test.*Concurrent.*Intent'</automated>
  </verify>
  <done>Concurrent gap regressions exist and pass after the active-intent guard is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic active-intent guards for listener jobs</name>
  <files>mibo-media-server/internal/database/models.go, mibo-media-server/internal/database/database.go, mibo-media-server/internal/listener/service.go, mibo-media-server/internal/listener/service_test.go</files>
  <read_first>
    - mibo-media-server/internal/database/models.go
    - mibo-media-server/internal/database/database.go
    - mibo-media-server/internal/listener/service.go
    - mibo-media-server/internal/listener/service_test.go
    - mibo-media-server/internal/jobs/service.go
    - .planning/phases/11-event-driven-refresh-hardening/11-CONTEXT.md
    - .planning/phases/11-event-driven-refresh-hardening/11-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: concurrent storage-event bursts serialize through one active refresh intent per library window and merge into one queued listener job per D-04 and D-05.
    - Test 2: concurrent reconciliation seeding serializes through one active reconcile intent per library per D-10 and D-11.
    - Test 3: repeated windows still work after prior jobs complete because active uniqueness is separate from `Job.JobKey` history.
  </behavior>
  <action>Add a new database model `JobActiveIntent` in `models.go` with fields `ID uint`, `IntentKey string` tagged `gorm:"size:255;not null;uniqueIndex"`, `Kind string` tagged `gorm:"size:128;not null;index"`, `JobID uint` tagged `gorm:"not null;index"`, plus `CreatedAt` and `UpdatedAt`. Add `&JobActiveIntent{}` to `database.Open` AutoMigrate immediately after `&Job{}`. In `listener/service.go`, import `gorm.io/gorm/clause` and implement active intent keys `listener-refresh-active:<library_id>` and `listener-reconcile-active:<library_id>`. Inside the existing `RecordStorageEvent` transaction, upsert the refresh active-intent row with `clause.OnConflict` on `intent_key`, reload it inside the transaction, then create or update exactly one queued `apply_storage_event_refresh` job for that library window and update `JobActiveIntent.JobID` to the keeper job ID. Preserve existing merge rules: same library active jobs are decoded, merged with `mergeRefreshPayload`, duplicate active rows are deleted, `available_at` remains `WindowEndsAt`, and the final `job_key` is still `refreshJobKey(payload.LibraryID, payload.RootPath, payload.FallbackFullSync)`. Inside `EnsureReconcileCoverage`, use the reconcile active-intent key before checking/creating jobs so concurrent calls create or reuse one queued/running `listener_reconcile` row per library, then update `JobActiveIntent.JobID` to that row. Do not make `database.Job.JobKey` unique; completed history must remain compatible with recurring six-hour reconciliation.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/database/models.go` contains `type JobActiveIntent struct`
    - `mibo-media-server/internal/database/models.go` contains `uniqueIndex` on `IntentKey`
    - `mibo-media-server/internal/database/database.go` AutoMigrates `&JobActiveIntent{}`
    - `mibo-media-server/internal/listener/service.go` imports `gorm.io/gorm/clause`
    - `mibo-media-server/internal/listener/service.go` contains `listener-refresh-active` and `listener-reconcile-active`
    - `mibo-media-server/internal/database/models.go` keeps `JobKey` as `gorm:"size:255;index"` rather than changing it to a global unique index
  </acceptance_criteria>
  <verify>
    <automated>cd /root/Mibo/mibo-media-server && go test ./internal/listener -run 'Test.*(Concurrent|Merge|Ancestor|Reconcile)' && go test ./internal/worker -run 'Test.*Reconcile'</automated>
  </verify>
  <done>Concurrent listener refresh and reconcile creation is guarded by a durable active-intent key, and existing listener/worker regressions still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent storage-event callers → listener service | Many untrusted-but-authenticated events can arrive at the same time for one library. |
| worker coverage loop → listener reconcile seeding | Multiple worker loops or retries can attempt to seed the same reconcile window. |
| listener service → jobs table | Duplicate active rows can amplify into noisy refresh and reconciliation work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | D | `RecordStorageEvent` concurrent creation | mitigate | Serialize active refresh creation through `JobActiveIntent.IntentKey = listener-refresh-active:<library_id>` and verify with concurrent tests. |
| T-11-05-02 | D | `EnsureReconcileCoverage` concurrent creation | mitigate | Serialize active reconcile creation through `JobActiveIntent.IntentKey = listener-reconcile-active:<library_id>` and verify one active row per library. |
| T-11-05-03 | T | `database.Job` history | mitigate | Keep `Job.JobKey` non-unique and place active uniqueness in a dedicated guard table so recurring reconciliation does not corrupt historical job rows. |
</threat_model>

<verification>
- `cd /root/Mibo/mibo-media-server && go test ./internal/listener`
- `cd /root/Mibo/mibo-media-server && go test ./internal/worker -run 'Test.*Reconcile'`
- `cd /root/Mibo/mibo-media-server && go test ./...`
</verification>

<success_criteria>
- Concurrent duplicate storage events leave one active listener refresh row.
- Concurrent reconcile coverage calls leave one active listener reconcile row per library.
- Sequential merge, ancestor promotion, fallback, and worker reconcile tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-event-driven-refresh-hardening/11-05-SUMMARY.md`
</output>
