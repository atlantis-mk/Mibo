---
phase: 11-event-driven-refresh-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - mibo-media-server/internal/worker/worker.go
  - mibo-media-server/internal/worker/worker_test.go
autonomous: true
requirements:
  - LIST-02
  - LIST-04
must_haves:
  truths:
    - Debounced listener jobs eventually fan into the existing `targeted_refresh` or `sync_library` queue work.
    - Listener-triggered work uses the same worker lifecycle and status handling as other jobs.
    - Periodic reconciliation keeps active libraries on a repeating safety scan even if listener events are missed.
  artifacts:
    - path: mibo-media-server/internal/worker/worker.go
      provides: worker dispatch for listener refresh and listener reconciliation jobs.
    - path: mibo-media-server/internal/worker/worker_test.go
      provides: regression proof for coalesced refresh execution and recurring reconciliation seeding.
  key_links:
    - from: mibo-media-server/internal/worker/worker.go
      to: mibo-media-server/internal/listener/service.go
      via: listener job dispatch and reconcile coverage seeding
      pattern: ApplyStorageEventRefresh|RunReconcile|EnsureReconcileCoverage
    - from: mibo-media-server/internal/worker/worker.go
      to: mibo-media-server/internal/library/scan_run.go
      via: queued `targeted_refresh` and `sync_library` jobs executed by the existing scan pipeline
      pattern: JobKindTargetedRefresh|JobKindSyncLibrary
---

<objective>
Teach the worker to execute listener-domain jobs so coalesced refresh intent and periodic reconciliation actually keep libraries in sync.

Purpose: Phase 11 is not complete until delayed listener jobs turn into real scan work and every active library keeps a repeating reconciliation path that survives missed storage events.
Output: Listener-aware worker dispatch and end-to-end regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-event-driven-refresh-hardening/11-CONTEXT.md
@.planning/phases/11-event-driven-refresh-hardening/11-RESEARCH.md
@.planning/phases/10-scheduled-operations-control/10-05-SUMMARY.md

<interfaces>
From `mibo-media-server/internal/worker/worker.go`:

```go
type Runner struct {
    jobs     *jobs.Service
    library  *library.Service
    metadata *metadata.Service
    probe    *probe.Service
    schedule *schedule.Service
}

func (r *Runner) RunOnce(ctx context.Context)
```

From Plan 11-01 listener foundation:

```go
func (s *Service) EnsureReconcileCoverage(ctx context.Context, libraries []database.Library) error
func (s *Service) ApplyStorageEventRefresh(ctx context.Context, job database.Job) error
func (s *Service) RunReconcile(ctx context.Context, job database.Job) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dispatch debounced listener refresh jobs through the existing scan queue</name>
  <files>mibo-media-server/internal/worker/worker.go, mibo-media-server/internal/worker/worker_test.go</files>
  <read_first>
    - mibo-media-server/internal/worker/worker.go
    - mibo-media-server/internal/worker/worker_test.go
    - mibo-media-server/internal/listener/service.go
    - mibo-media-server/internal/library/scan_run.go
  </read_first>
  <behavior>
    - Test 1: `apply_storage_event_refresh` jobs enqueue `targeted_refresh` when the payload has a safe root per D-01 and D-03.
    - Test 2: `apply_storage_event_refresh` jobs enqueue `sync_library` when the payload is marked `fallback_full_sync=true` per D-02 and D-08.
    - Test 3: listener-triggered work stays on the existing queue/worker lifecycle instead of mutating rows inline per D-03 and D-07.
  </behavior>
  <action>Extend `worker.Runner` to accept the listener service as an injected dependency and add a `handleJob` branch for `apply_storage_event_refresh`. The branch must delegate to `listener.ApplyStorageEventRefresh(...)`, which in turn enqueues existing `targeted_refresh` or `sync_library` jobs only. Do not short-circuit into scan code directly from the listener job branch; the queue remains the single execution path so LIST-02 and D-03 stay true.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/worker/worker.go` contains listener dependency injection
    - `mibo-media-server/internal/worker/worker.go` contains `case listener.JobKindApplyStorageEventRefresh:`
    - `mibo-media-server/internal/worker/worker_test.go` asserts resulting queued jobs are `targeted_refresh` or `sync_library`
  </acceptance_criteria>
  <verify>
    <automated>cd /root/Mibo/mibo-media-server && go test ./internal/worker -run 'Test.*(StorageEvent|TargetedRefresh)'</automated>
  </verify>
  <done>Coalesced listener jobs are executable and only fan into the existing scan queue, never into direct canonical row mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed and reseed periodic listener reconciliation for every active library</name>
  <files>mibo-media-server/internal/worker/worker.go, mibo-media-server/internal/worker/worker_test.go</files>
  <read_first>
    - mibo-media-server/internal/worker/worker.go
    - mibo-media-server/internal/worker/worker_test.go
    - mibo-media-server/internal/listener/service.go
    - mibo-media-server/internal/library/service_libraries.go
  </read_first>
  <behavior>
    - Test 1: `RunOnce` ensures one future `listener_reconcile` job exists for each active library before normal queue processing per D-10 and D-11.
    - Test 2: a due reconcile job enqueues a library resync and reseeds the next 6-hour reconcile window per D-10 through D-12.
    - Test 3: the fallback path remains library-scoped and worker-owned rather than becoming a second scheduler or UI-managed workflow per D-12 and D-14.
  </behavior>
  <action>Before normal job claiming, have `RunOnce` call `library.ListActiveLibraries(...)` and `listener.EnsureReconcileCoverage(...)` so every active library keeps one future-dated reconcile job. Add a `handleJob` branch for `listener_reconcile` that delegates to `listener.RunReconcile(...)`, queues the normal `sync_library` job for that library, and then reseeds the next 6-hour reconcile window. Keep the fallback scope at the library level per D-11; do not add admin-facing schedule CRUD, dashboards, or a second timer loop.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/worker/worker.go` calls `EnsureReconcileCoverage(` before the main claim loop
    - `mibo-media-server/internal/worker/worker.go` contains `case listener.JobKindListenerReconcile:`
    - `mibo-media-server/internal/worker/worker_test.go` contains tests matching `Test.*Reconcile`
    - worker tests assert the next reconcile job is future-dated after a reconcile run completes
  </acceptance_criteria>
  <verify>
    <automated>cd /root/Mibo/mibo-media-server && go test ./internal/worker -run 'Test.*Reconcile' && go test ./internal/worker</automated>
  </verify>
  <done>Every active library has periodic reconciliation coverage, and completing a reconcile job both queues a normal library sync and schedules the next fallback window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| listener jobs → worker dispatch | Delayed listener intent becomes real scan work here. |
| worker reconciliation seeding → jobs queue | Bad reseeding logic can starve or flood fallback reconciliation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | D | listener refresh dispatch | mitigate | Route `apply_storage_event_refresh` through the listener service so each claimed job enqueues one bounded `targeted_refresh` or `sync_library`, not repeated direct scans. |
| T-11-03-02 | D | reconcile reseeding | mitigate | Ensure only one future `listener_reconcile` job exists per active library and verify future-dated reseeding in worker tests. |
| T-11-03-03 | T | canonical media state | mitigate | Keep worker branches limited to queueing existing scan jobs; canonical row settlement remains inside `library` scan/reconcile flows per D-09. |
</threat_model>

<verification>
- `cd /root/Mibo/mibo-media-server && go test ./internal/worker -run 'Test.*(StorageEvent|TargetedRefresh|Reconcile)'`
- `cd /root/Mibo/mibo-media-server && go test ./internal/worker`
</verification>

<success_criteria>
- Debounced listener jobs reliably become real refresh work.
- Reconciliation fallback is periodic, library-scoped, and self-sustaining.
- Worker integration preserves the single queue-driven execution model for all listener-triggered behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/11-event-driven-refresh-hardening/11-03-SUMMARY.md`
</output>
