---
phase: 11-event-driven-refresh-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - mibo-media-server/internal/app/app.go
  - mibo-media-server/internal/httpapi/router.go
  - mibo-media-server/internal/httpapi/handlers_storage_events.go
  - mibo-media-server/internal/httpapi/router_test.go
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
must_haves:
  truths:
    - Storage-event ingress turns `create/update/delete/move` into accepted listener refresh intent instead of immediate canonical mutations.
    - Safe move or rename events coalesce to a common ancestor root; unsafe ones fall back to full sync.
    - Escaping paths and unauthenticated requests are rejected before any listener work is created.
  artifacts:
    - path: mibo-media-server/internal/httpapi/handlers_storage_events.go
      provides: thin storage-event ingress delegating to the listener service.
    - path: mibo-media-server/internal/httpapi/router_test.go
      provides: regression proof for auth, boundary validation, move normalization, and fallback behavior.
    - path: mibo-media-server/internal/app/app.go
      provides: application wiring for the listener service into HTTP construction.
  key_links:
    - from: mibo-media-server/internal/httpapi/handlers_storage_events.go
      to: mibo-media-server/internal/listener/service.go
      via: validated event input delegated to `RecordStorageEvent`
      pattern: RecordStorageEvent
    - from: mibo-media-server/internal/app/app.go
      to: mibo-media-server/internal/httpapi/router.go
      via: injected listener service instance
      pattern: listener.NewService|httpapi.New
---

<objective>
Route `/api/v1/storage-events` through the new listener service so ingress behavior is conservative, authenticated, and coalescing-aware.

Purpose: LIST-01 and LIST-02 are only delivered when external change notifications reach the listener domain through a stable API contract that validates library boundaries and preserves the locked targeted-refresh semantics.
Output: Listener-aware app wiring, HTTP handler delegation, and route regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-event-driven-refresh-hardening/11-CONTEXT.md
@.planning/phases/11-event-driven-refresh-hardening/11-RESEARCH.md

<interfaces>
From `mibo-media-server/internal/httpapi/router.go`:

```go
type Router struct {
    db      *gorm.DB
    auth    *auth.Service
    library *library.Service
}

func New(cfg config.Config, db *gorm.DB, registry *providers.Registry, authSvc *auth.Service, librarySvc *library.Service, jobsSvc *jobs.Service, playbackSvc *playback.Service, progressSvc *progress.Service, searchSvc *search.Service, metadataSvc *metadata.Service, settingsSvc *settings.Service, args ...any) http.Handler
```

From Plan 11-01 listener foundation:

```go
func (s *Service) RecordStorageEvent(ctx context.Context, input EventIngestInput) (database.Job, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject the listener service into app and HTTP routing, then delegate storage-event intake</name>
  <files>mibo-media-server/internal/app/app.go, mibo-media-server/internal/httpapi/router.go, mibo-media-server/internal/httpapi/handlers_storage_events.go</files>
  <read_first>
    - mibo-media-server/internal/app/app.go
    - mibo-media-server/internal/httpapi/router.go
    - mibo-media-server/internal/httpapi/handlers_storage_events.go
    - mibo-media-server/internal/listener/service.go
    - .planning/phases/11-event-driven-refresh-hardening/11-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `create`, `update`, and `delete` requests enqueue listener intent for targeted refresh by default per D-01.
    - Test 2: `move` / `rename` requests use the common ancestor when both paths normalize safely, otherwise they store full-sync fallback intent per D-02.
    - Test 3: the handler only produces listener jobs and never mutates canonical media rows directly per D-03 and D-07.
  </behavior>
  <action>Instantiate `listener.NewService(...)` in `app.New`, pass it into `httpapi.New(...)`, add the listener dependency to `Router`, and refactor `handleStorageEvent` so it stays thin: require auth, decode JSON, validate path scope, then call `listener.RecordStorageEvent(...)`. Keep the existing path-validation protections, but move refresh-intent creation out of the handler. Return the accepted listener job row from the handler. Do not queue `targeted_refresh` or `sync_library` directly from HTTP anymore; the listener service owns the debounce window per D-04 and D-05.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/app/app.go` contains `listener.NewService(`
    - `mibo-media-server/internal/httpapi/router.go` includes a listener dependency on `Router`
    - `mibo-media-server/internal/httpapi/handlers_storage_events.go` contains `RecordStorageEvent(`
    - `mibo-media-server/internal/httpapi/handlers_storage_events.go` no longer calls `QueueTargetedRefresh(` directly
  </acceptance_criteria>
  <verify>
    <automated>cd /root/Mibo/mibo-media-server && go test ./internal/httpapi -run 'TestStorageEvent'</automated>
  </verify>
  <done>The app wires one listener service instance into the router, and the storage-event endpoint delegates all refresh-intent creation to that service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand route regression tests for conservative normalization and boundary safety</name>
  <files>mibo-media-server/internal/httpapi/router_test.go</files>
  <read_first>
    - mibo-media-server/internal/httpapi/router_test.go
    - mibo-media-server/internal/httpapi/handlers_storage_events.go
    - mibo-media-server/internal/listener/service.go
  </read_first>
  <behavior>
    - Test 1: unauthenticated storage-event requests return `401` and create no listener jobs.
    - Test 2: escaping paths still return `400` even for unsupported kinds.
    - Test 3: safe move/rename events return an accepted listener job keyed to a common ancestor, while unsafe or unsupported kinds fall back to full-sync intent.
  </behavior>
  <action>Extend `router_test.go` so storage-event route coverage proves the locked HTTP semantics end-to-end: auth required, path escaping rejected before enqueue, targeted-listener intent for safe `create/update/delete`, common-ancestor promotion for safe `move/rename`, and `fallback_full_sync=true` for unsupported or unsafe normalization. Assert against the accepted listener job kind and payload shape instead of only checking the old direct `targeted_refresh` / `sync_library` kinds.</action>
  <acceptance_criteria>
    - `mibo-media-server/internal/httpapi/router_test.go` contains tests matching `TestStorageEvent.*Move` or `TestStorageEvent.*Rename`
    - storage-event route tests assert `apply_storage_event_refresh`
    - storage-event route tests still assert `401` for missing auth and `400` for escaping paths
  </acceptance_criteria>
  <verify>
    <automated>cd /root/Mibo/mibo-media-server && go test ./internal/httpapi -run 'TestStorageEvent'</automated>
  </verify>
  <done>HTTP regression coverage proves listener ingress remains authenticated, library-bounded, and conservative across all supported event kinds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated caller → `/api/v1/storage-events` | Untrusted kind/path payload crosses into durable listener intent. |
| HTTP handler → listener service | Handler must not bypass validation or create direct canonical mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | T | `handlers_storage_events.go` path validation | mitigate | Preserve boundary validation for both `path` and `old_path` before calling `RecordStorageEvent`, rejecting escaping payloads with `400`. |
| T-11-02-02 | E | storage-event route auth | mitigate | Keep `requireUser` at route entry so only authenticated callers can enqueue listener work. |
| T-11-02-03 | R | listener ingress semantics | mitigate | Return the accepted listener job and cover safe move/fallback behavior with route tests so handler behavior is auditable and deterministic. |
</threat_model>

<verification>
- `cd /root/Mibo/mibo-media-server && go test ./internal/httpapi -run 'TestStorageEvent'`
- Confirm the route only returns listener job rows, not direct media mutations.
</verification>

<success_criteria>
- `/api/v1/storage-events` is listener-aware and conservative.
- Safe moves normalize to common ancestors; unsafe ones fall back cleanly.
- HTTP regressions prove auth and boundary protections still hold.
</success_criteria>

<output>
After completion, create `.planning/phases/11-event-driven-refresh-hardening/11-02-SUMMARY.md`
</output>
