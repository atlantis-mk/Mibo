# Quick Task 260423-4h5 Summary

## Outcome

Completed a pragmatic first migration slice from `web/` to `web-new/`: the new TanStack Start app now has a setup-aware root shell, a migrated initialization wizard, persisted API/session client config, and a migration landing page instead of the starter placeholder.

## What Changed

- Added shared client bootstrap modules in `web-new/src/lib/` for setup status, API calls, and browse search helpers.
- Added `web-new/src/components/app-providers.tsx` to supply theme, tooltip, and toast providers in the new framework.
- Added `web-new/src/components/setup-gate.tsx` so non-setup routes are redirected to `/setup` until the backend reports `can_enter_app=true`.
- Added `web-new/src/components/setup-wizard.tsx` plus `web-new/src/features/setup/constants.ts` to port the first-run flow for backend URL, admin creation, media source creation, and first library creation.
- Replaced the starter routes with a real app shell in `web-new/src/routes/__root.tsx`, a migration home page in `web-new/src/routes/index.tsx`, and a dedicated `web-new/src/routes/setup.tsx` route.

## Validation

- `pnpm build` in `web-new/` passed.

## Assumptions

- Kept this quick migration scoped to the bootstrap/setup path rather than attempting a broad port of browse, playback, and settings pages in one step.
- Reused the existing demo-oriented default paths and local API/session storage keys so the new app stays aligned with the shipped `web/` behavior.

## Commit

- Code: `c209e6f` `feat(web-new): port setup shell to TanStack Start`
