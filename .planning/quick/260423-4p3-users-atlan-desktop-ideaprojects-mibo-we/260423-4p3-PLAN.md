# Quick Task 260423-4p3: 继续迁移 /Users/atlan/Desktop/IdeaProjects/Mibo/web/ 到新框架 /Users/atlan/Desktop/IdeaProjects/Mibo/web-new/ 的主应用路由骨架

## Plan Summary

Port the authenticated browse/settings route skeleton into `web-new/` by wiring TanStack Start file routes to the existing `web/` app shell and controller, keeping the slice pragmatic and deferring playback-specific migration.

## Tasks

### 1. Bridge `web-new/` routes to the existing app shell
- files: `web-new/tsconfig.json`, `web-new/src/features/app/legacy-app-shell.tsx`, `web-new/src/lib/app-route-search.ts`
- action: Add aliasing and client-only wrappers so `web-new/` can reuse the shipped app pages/controller from `web/src/features/app` without rewriting business logic.
- verify: TypeScript resolves `@/features/app/*` from `web-new/` and the wrappers avoid SSR-time access to browser-only APIs.
- done: `web-new/` has reusable bridge components for authenticated app surfaces.

### 2. Replace the temporary landing page with real file routes
- files: `web-new/src/routes/index.tsx`, `web-new/src/routes/movies.tsx`, `web-new/src/routes/shows.tsx`, `web-new/src/routes/library.$libraryId.tsx`, `web-new/src/routes/settings.tsx`, `web-new/src/routes/media.$mediaItemId.tsx`
- action: Register the main authenticated surfaces as TanStack Start file routes and feed them validated browse/detail search state.
- verify: Home, movies, shows, library, settings, and media detail routes render the migrated app shell.
- done: `web-new/` no longer depends on a placeholder landing page for main app navigation.

### 3. Align frontend assets and validate the slice
- files: `web-new/package.json`, `web-new/src/styles/app.css`, `.planning/quick/260423-4p3-users-atlan-desktop-ideaprojects-mibo-we/260423-4p3-SUMMARY.md`, `.planning/STATE.md`
- action: Add any missing frontend dependency/styles required by the reused shell, build `web-new/`, and record outcome in quick-task artifacts/state tracking.
- verify: `pnpm build` succeeds in `web-new/`.
- done: Migration scope and validation are captured for follow-up work.
