# Quick Task 260423-4p3 Summary

## Outcome

Extended the `web-new/` migration from setup-only bootstrap into a working authenticated route skeleton: the TanStack Start app now mounts the existing Mibo app shell for the main browse and settings surfaces instead of stopping at a temporary landing page.

## What Changed

- Replaced the temporary `web-new/src/routes/index.tsx` landing page with a real home route backed by the existing `web/` app shell.
- Added file routes for `movies`, `shows`, `library/$libraryId`, `settings`, and `media/$mediaItemId` in `web-new/src/routes/`.
- Added `web-new/src/features/app/legacy-app-shell.tsx` to bridge `web-new/` routes into the shipped `web/src/features/app/pages/*` pages and controller while guarding browser-only code until mount.
- Added `web-new/src/lib/app-route-search.ts` so the new file routes preserve the same browse/detail search normalization used by the old router.
- Added Vite aliasing plus `web-new/src/types/legacy-web.d.ts` so `web-new/` can reuse `web/` feature code at runtime without pulling the entire old source tree into `web-new`'s TypeScript check.
- Updated `web-new/src/styles/app.css`, `web-new/package.json`, and `web-new/pnpm-lock.yaml` to support the reused authenticated shell, including Swiper styles/assets.
- Adjusted `web-new/src/components/setup-wizard.tsx`, `web-new/src/components/DefaultCatchBoundary.tsx`, and `web-new/src/components/NotFound.tsx` for the new required home-route search typing.

## Validation

- `pnpm build` in `web-new/` passed.

## Assumptions

- Kept playback-specific TanStack Start routes out of this slice to keep the migration focused on browse/settings surfaces.
- Reused the existing `web/` app shell and controller pragmatically rather than starting a parallel rewrite inside `web-new/`.

## Commit

- Code: `55129cb` `feat(web-new): port authenticated route skeleton`
