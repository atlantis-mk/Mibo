## 1. Title Role API

- [x] 1.1 Audit current production call sites that read or write `TitleCandidate`, movie work keys, `title`, `search_title`, `sort_title`, `raw_work_title`, and `file_title`.
- [x] 1.2 Complete `internal/titleclean` role helpers for movie identity, movie search, movie display, raw work provenance, resource file stem, sort fallback, and weak movie identity detection.
- [x] 1.3 Add focused titleclean tests for role boundaries, release-only filenames, fullwidth separators, localized titles, raw provenance, file stems, and sort-title fallback.

## 2. Directory Shape Title Policy

- [x] 2.1 Move folder-vs-file movie title source selection into one content-shape policy table keyed by directory shape.
- [x] 2.2 Apply directory-title fallback for single movie, multipart movie, and multi-version movie scopes when file-derived identity is weak.
- [x] 2.3 Preserve per-movie identity behavior for movie collection scopes, including per-file title-year, per-child-directory, sidecar, and catalog/external-ID identities.
- [x] 2.4 Enforce nested directory boundaries so direct files follow their directory shape and child directories use their own shape policy.
- [x] 2.5 Add content-shape profile, plan, assignment, and materialization tests for weak file titles, folder title fallback, movie collections, nested child folders, extras, and review-required scopes.

## 3. Recognition And Materialization

- [x] 3.1 Migrate recognition key generation, candidate generation, manifest building, multipart handling, and recognition-unit construction to title role helpers.
- [x] 3.2 Materialize `metadata_items.title`, `metadata_items.search_title`, and `metadata_items.sort_title` from their designated roles without overwriting provider-managed or user-governed fields.
- [x] 3.3 Materialize `resources.raw_work_title`, `resources.work_title_source`, and `resources.file_title` from provenance and file-stem roles.
- [x] 3.4 Store title-source evidence that distinguishes directory, file, sidecar, catalog ID, provider, and fallback sources.
- [x] 3.5 Add recognition/materializer integration tests for historical weak-year collisions and for resources retaining raw file stems.

## 4. Metadata And Catalog Boundaries

- [x] 4.1 Update automatic metadata matching to build provider queries from search-role titles and compatible provider/user titles only.
- [x] 4.2 Update match scoring to normalize local and remote titles through the search-title policy before exact or partial title scoring.
- [x] 4.3 Verify catalog projection and resource queries expose role-appropriate values without recomputing identity, search, or resource file titles.
- [x] 4.4 Add metadata matcher and catalog tests covering directory-derived search titles, weak file-title exclusion, provider title preservation, and sort-title behavior.

## 5. Cleanup And Simplification

- [x] 5.1 Remove obsolete generic title cleanup wrappers or make them private behind the role API after call-site migration.
- [x] 5.2 Delete duplicate raw title formatting, folder title candidate, and search title helper implementations that now belong to `titleclean`.
- [x] 5.3 Reduce scanparse responsibilities to extracting evidence and release tokens, leaving persistence-ready title decisions to title roles and content-shape policy.
- [x] 5.4 Add a static search or compile-time guard test that production code no longer bypasses role helpers for scanner-managed title persistence.

## 6. Repair Existing Local Data

- [x] 6.1 Identify scanner-derived local metadata/resource rows with weak year-only or release-only title values generated by older normalization.
- [x] 6.2 Implement an explicit repair or forced-rescan path that recomputes safe local title fields and links from the new role policy.
- [x] 6.3 Preserve provider-applied and user-locked title fields during repair, including governed `title`, `original_title`, `sort_title`, and external IDs.
- [x] 6.4 Add repair tests for weak local movie rows, governed provider metadata, resource `file_title`, and collision-safe relinking.

## 7. Verification

- [x] 7.1 Run targeted backend tests for `internal/titleclean`, `internal/scanparse`, `internal/library`, `internal/recognition`, `internal/metadata`, and `internal/catalog`.
- [x] 7.2 Run `go test ./...` in `mibo-media-server`.
- [x] 7.3 Re-scan representative movie folder, multipart movie, multi-version movie, movie collection, nested child directory, series/season, extras, and release-only filename fixtures against PostgreSQL or a test database.
- [x] 7.4 Inspect resulting metadata/resource rows to confirm different folders no longer collapse into one weak movie and title fields match their documented roles.
